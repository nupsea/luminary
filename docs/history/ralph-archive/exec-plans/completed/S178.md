# Exec Plan: S178 -- Study tab consolidation: merge health panels, Smart Generate button, fix default deck label

**Phase:** V3 Phase 3 - UX Polish and Learner Focus
**Priority:** 18
**Status:** in-progress

## Goal

Replace the three-button generation mode selector in the Study tab with a single 'Smart Generate' button that auto-selects mode based on mastery thresholds, merge DeckHealthPanel and HealthReportPanel into one collapsed 'Deck Status' accordion, and fix the display of 'default' deck names so learners see the document title instead of the internal label. No backend changes are needed -- all changes are frontend-only pure logic and UI restructuring.

## Files to Touch

### Backend
None. All changes are frontend-only.

### Frontend
- `frontend/src/lib/studyUtils.ts` -- NEW: two pure functions: `selectSmartMode(masteredPct)` and `getDeckDisplayName(deckName, docTitle, totalDecksForDoc)`
- `frontend/src/lib/studyUtils.test.ts` -- NEW: Vitest unit tests for both pure functions; tests for DeckStatusAccordion collapsed-by-default behavior documented as a note (component tests require jsdom, not node env -- see pattern below)
- `frontend/src/pages/Study.tsx` -- major refactor: (1) replace GeneratePanel mode-toggle buttons + per-mode controls with SmartGenerateButton + AdvancedOptions disclosure; (2) replace side-by-side DeckHealthPanel + HealthReportPanel with merged DeckStatusAccordion; (3) apply getDeckDisplayName in All Decks list; (4) add empty state message

### Other
- `scripts/smoke/S178.sh` -- NEW: smoke test verifying flashcard endpoints used by Study tab still respond 200
- `scripts/ralph/prd-v3.json` -- set `passes: true` when all gates pass

## Step Sequence

1. **Discovery:** Read Study.tsx lines 700-1145 (DeckHealthPanel + HealthReportPanel full bodies), lines 1204-1450 (GeneratePanel full body), lines 2169-2300 (All Decks list + document selector render), and lines 2268-2420 (flashcard section render including panel placement). Identify all state variables and mutation references that will be touched.
2. Create `frontend/src/lib/studyUtils.ts` with two pure exports:
   - `selectSmartMode(masteredPct: number): "basic" | "feynman" | "cloze"` -- returns "basic" if < 0.30, "feynman" if < 0.70, "cloze" otherwise
   - `getDeckDisplayName(deckName: string, docTitle: string | undefined, totalDecksForDoc: number): string` -- returns docTitle if deckName === "default" and totalDecksForDoc === 1 and docTitle is defined, otherwise returns deckName
3. Create `frontend/src/lib/studyUtils.test.ts` with Vitest tests (node env safe -- no React/store imports):
   - `selectSmartMode`: threshold boundary tests at 0%, 29%, 30%, 69%, 70%, 100%
   - `getDeckDisplayName`: alias when only deck, no alias when multiple decks, no alias when name is not 'default', no alias when docTitle undefined
4. Modify `GeneratePanel` in Study.tsx:
   - Add `masteredPct: number` prop to `GeneratePanelProps`
   - Add state: `advancedOpen: boolean` (default false), `smartGenerating: boolean`, `smartModeLabel: string | null`
   - Replace the three-button mode toggle with a single 'Smart Generate' button
   - Button click: call `selectSmartMode(masteredPct)`, set `smartModeLabel` to the chosen mode string, dispatch the appropriate mutation (text -> `onGenerate`, entities -> `onGenerateFromGraph`, cloze -> `onGenerateCloze` -- pick first section for cloze smart mode)
   - Show subtitle under button during generation: e.g. "Generating Feynman-style questions..." keyed to the active mode label
   - Render an `<details>` / disclosure triangle labeled "Advanced options" that wraps the original three-button mode toggle and all existing per-mode controls (count, scope, difficulty, pairs K, cloze section selector)
5. Create `DeckStatusAccordion` component inside Study.tsx (co-located, not exported):
   - Props: `documentId: string`
   - `isOpen` state defaults to `false`
   - Header: "Deck Status (X cards, Y% mastered)" -- derive X from `report.total_cards` (bloom coverage) and Y from `(report_health.mastered / totalCards * 100)` -- load both queries only when `isOpen === true` (existing `enabled: isOpen` pattern already used by both panels)
   - Body: combined content from DeckHealthPanel (Bloom bar chart + gaps) and HealthReportPanel (orphaned/mastered/stale/uncovered/hotspot) sections, rendered sequentially with a visual divider
   - Loading: combined skeleton (reuse the existing patterns from both panels)
   - Error: combined error banner
6. In the `Study` default export render:
   - Replace `<DeckHealthPanel documentId={activeDocumentId} />` and `<HealthReportPanel documentId={activeDocumentId} />` with `<DeckStatusAccordion documentId={activeDocumentId} />`
   - Compute `masteredPct` from `cards` array: `masteredPct = cards.filter(c => c.fsrs_state === "mastered" || c.reps >= 5).length / Math.max(cards.length, 1)` -- pass to GeneratePanel
   - Fix All Decks list: compute `decksPerDoc` map (document_id -> count) from deckList; replace `{deck.deck}` display with `{getDeckDisplayName(deck.deck, docList.find(d => d.id === deck.document_id)?.title, decksPerDoc.get(deck.document_id ?? "") ?? 1)}`
   - Add empty state: when `cards.length === 0 && !cardsLoading`, render `<p>No flashcards yet -- click Smart Generate to create some</p>` instead of the filter bar and card list
7. Run: `cd /Users/sethurama/DEV/LM/learning-mate/frontend && npx tsc --noEmit` -- fix all type errors
8. Run Vitest: `cd /Users/sethurama/DEV/LM/learning-mate/frontend && npx vitest run src/lib/studyUtils.test.ts`
9. Run full frontend type check again after any fixes
10. Create `scripts/smoke/S178.sh` -- curl the flashcard and deck endpoints used by Study tab
11. Run: `bash scripts/smoke/S178.sh`
12. Set `passes: true` in prd-v3.json, commit, move plan to `docs/exec-plans/completed/`

## Test Strategy

- Unit tests (Vitest, node env): `selectSmartMode` pure function -- all threshold boundary cases; `getDeckDisplayName` pure function -- alias/no-alias cases
- No component-level DOM tests: DeckStatusAccordion collapsed-by-default is verified by reading `useState(false)` initial value; component tests would require jsdom env. Per invariant I-13 and the Vitest node env + Zustand pattern in patterns.md, test only the extractable pure logic.
- Smoke: verify `GET /flashcards/{doc_id}`, `GET /flashcards/decks`, and `GET /flashcards/health/{doc_id}` all return 200 (these are the endpoints Study tab depends on for the new merged accordion)

## AC Coverage Map

| AC | Implementation |
|---|---|
| Three generation mode buttons replaced with single 'Smart Generate' button auto-selecting mode based on mastery thresholds (< 30% basic, 30-70% Feynman, >= 70% cloze) | `selectSmartMode()` in `studyUtils.ts`; replace mode-toggle buttons in `GeneratePanel` in `Study.tsx`; compute `masteredPct` from `cards` array and pass as prop |
| Chosen generation mode displayed as subtitle under the Smart Generate button during generation | `smartModeLabel` state in GeneratePanel; conditional `<p>` subtitle rendered below button when `smartGenerating` is true |
| 'Advanced options' disclosure reveals original mode selector and override controls for power users | `<details>` / `advancedOpen` state wrapping original three-button toggle + count/scope/difficulty/pairs-K controls in GeneratePanel |
| DeckHealthPanel and HealthReportPanel merged into a single 'Deck Status' accordion section, collapsed by default | New `DeckStatusAccordion` component with `useState(false)`; replaces the two separate panel renders in Study's return JSX |
| Deck Status accordion header shows summary: 'Deck Status (X cards, Y% mastered)' | Header string built from `report.total_cards` and computed `masteredPct` inside DeckStatusAccordion header button |
| GoalsPanel removed from Study tab (handled by S177) | GoalsPanel code stays in Study.tsx as an export (Progress.tsx imports it); confirmed it is not rendered in the Study tab's JSX -- no JSX call site for GoalsPanel in Study's return; already removed in S177 |
| Deck named 'default' that is the only deck for a document displays the document title as its display name (DB value unchanged) | `getDeckDisplayName()` in `studyUtils.ts`; applied in All Decks list render in Study.tsx; document_id from DeckItem used to look up title from docList |
| Empty state when no cards: 'No flashcards yet -- click Smart Generate to create some' | Guard in Study's flashcards section render: `if cards.length === 0 && !cardsLoading` show empty state message |
| npx tsc --noEmit exits 0; uv run ruff check . exits 0; uv run pytest exits 0 | No backend changes so ruff/pytest trivially pass; tsc verified in step 7 |
| scripts/smoke/S178.sh exits 0 | Smoke script curls flashcard + deck + health endpoints and asserts 200 |

## Notes on GoalsPanel

GoalsPanel is exported from Study.tsx (lines 1628-1632 confirmed) and imported by Progress.tsx (line 28 confirmed). It is NOT rendered in the Study tab's main JSX return -- the only render sites are at lines 1758 inside GoalsPanel's own internal render and Progress.tsx line 446. S177 already removed the GoalsPanel render from Study's top-level JSX. This AC is verified by reading the file, not by code changes.

## Cross-Story Risk

- S177 (Progress tab): imports `GoalsPanel` from Study.tsx -- the export must remain intact. No risk as long as the export is not removed.
- S169 (deck list): DeckItem type and `fetchDecks()` are read by this story -- no changes to those types.
- None of the backend endpoints are modified so no cross-story backend risk.
