# Ralph Progress Log
Started: Fri  6 Mar 2026 23:08:42 AEST
Archived: S75-S188 entries moved to archive/progress-v3-S75-S188.txt (2026-04-02)
---

## Codebase Patterns
Patterns are now maintained in `scripts/ralph/patterns.md` -- read that file instead of this section.

## 2026-03-30 - S189
- Implemented: OrganizationPlanDialog frontend component, POST /notes/cluster/suggestions/batch-accept endpoint, expanded GET /notes/cluster/suggestions to include all note_ids, drag-and-drop note reassignment persists via note_ids override in batch-accept
- Files changed: backend/app/services/clustering_service.py, backend/app/routers/notes.py, backend/tests/test_clustering.py, frontend/src/components/OrganizationPlanDialog.tsx (new), frontend/src/pages/Notes.tsx, scripts/smoke/S189.sh (new)
- **Learnings for future iterations:**
  - Batch-accept endpoints should accept optional field overrides (e.g. note_ids) to persist frontend drag-and-drop reordering, not just name changes
  - Empty catch blocks in async handlers violate I-10; always show error state or toast on failure
  - Smoke tests must exercise the full user journey (trigger -> query -> action), not just the new endpoint
---

## 2026-03-30 - S190
- What was implemented: Tag search type-ahead in Notes sidebar TagTree component
- Completed partial implementation: added 150ms debounce (useRef-based timer), clear (x) button, "No tags matching" empty state, Enter key to apply first match, recursive TagSubtree rendering for arbitrary depth, auto-expand matching branches during search, placeholder "Search tags..."
- Added S190 unit tests: filterTagTree substring match (5 tests), parent chain for deeply nested tags (2 tests), highlightMatch segments (6 tests)
- Created smoke script S190.sh
- Files changed: frontend/src/components/TagTree.tsx, frontend/src/components/TagTree.test.ts, scripts/smoke/S190.sh, scripts/ralph/prd-v3.json, docs/ralph-run-flow.md, docs/exec-plans/active/S190.md -> completed/
- **Learnings for future iterations:**
  - Partial commits (eb9999d) leave gaps that must be systematically checked against all ACs before declaring done
  - TagTree was only rendering 2 levels deep (hardcoded depth 0 and 1) -- recursive TagSubtree component needed for arbitrary nesting
  - useRef-based debounce avoids adding a custom hook: store timer ref, clear on unmount via useEffect cleanup
  - debouncedQuery drives filtering while searchQuery drives input display -- keeps input responsive while preventing re-renders
---

## 2026-03-31T01:55:00Z - S191
- Implemented: Document action menu (three-dot MoreVertical) on DocumentCard with 5 actions: Read, Chat about this, Study flashcards, View notes, View in graph
- Each action sets Zustand store state and dispatches luminary:navigate DOM event
- App.tsx onLuminaryNavigate handler extended for chat, study, viz tabs with documentId support
- Notes tab gains notesDocumentId store field for document-scoped filtering with clear chip
- Pure utility docActionUtils.ts with DOC_ACTIONS constant and buildDocActionDetail function
- 7 Vitest tests for pure utility
- Menu visible always on mobile, hover on desktop (sm:opacity-0 sm:group-hover:opacity-100)
- Fixed pre-existing ruff line-too-long in ingestion.py
- Files changed: frontend/src/lib/docActionUtils.ts (new), frontend/src/lib/docActionUtils.test.ts (new), frontend/src/components/library/DocumentCard.tsx, frontend/src/pages/Learning.tsx, frontend/src/pages/Notes.tsx, frontend/src/store.ts, frontend/src/App.tsx, backend/app/workflows/ingestion.py, scripts/smoke/S191.sh (new), docs/exec-plans/completed/S191.md
- **Learnings for future iterations:**
  - Mobile visibility requires sm: breakpoint prefix on opacity classes (not just hover); touch devices don't trigger CSS hover
  - Notes tab uses its own store fields (activeCollectionId, activeTag) not activeDocumentId; added notesDocumentId for document-scoped filtering
  - Simple div-based dropdown with useRef + outside-click works well without shadcn DropdownMenu component
  - Existing luminary:navigate handler must be extended for each new tab target; check coverage in I-11 pattern
---

## 2026-03-31T03:00:00Z - S192
- Implemented: DocumentReader notes panel with auto-collection per document
- NoteCollectionModel gains auto_document_id nullable string column with index
- GET /collections/by-document/{document_id} returns auto-collection or 404
- POST /collections/auto/{document_id} creates auto-collection idempotently, named '{title} Notes'
- SummaryPanel: One-liner tab replaced with Notes tab as first tab
- NotesReaderPanel component: fetches/creates auto-collection on mount, lists notes sorted by section_order, inline add/edit/delete, click-to-scroll-to-section, section heading display
- Default tab logic: Notes if document has notes, Key Points otherwise (via onNoteCountKnown callback)
- Edit form includes tags (chips with add/remove) per AC9
- 5 pytest tests for auto-collection endpoints
- Files changed: backend/app/models.py, backend/app/db_init.py, backend/app/routers/collections.py, backend/tests/test_auto_collections.py (new), frontend/src/components/reader/types.ts, frontend/src/components/reader/DocumentReader.tsx, frontend/src/components/reader/NotesReaderPanel.tsx (new), scripts/smoke/S192.sh (new), docs/ralph-run-flow.md
- **Learnings for future iterations:**
  - asyncio.run() instead of asyncio.get_event_loop().run_until_complete() in test helpers avoids event loop ordering issues across test files
  - Auto-collection pattern: one collection per document via auto_document_id field, idempotent POST endpoint
  - Default tab conditional logic: use onNoteCountKnown callback from child to parent to avoid flash of wrong tab
  - Section sorting in frontend: fetch document sections separately to get section_order, build lookup map
  - Route ordering: /by-document and /auto must be registered BEFORE /{collection_id} wildcard
---

## 2026-03-31T10:30:00Z - S193
- What was implemented:
  - GlossaryTermModel in models.py with UniqueConstraint on (document_id, term)
  - ExplainService: extract_glossary with DB persistence (_upsert_terms), get_cached_glossary, delete_term, robust _parse_glossary_json with regex fallback
  - Three new endpoints: GET /explain/glossary/{doc_id}/cached, POST /explain/glossary/{doc_id}, DELETE /explain/glossary/{doc_id}/terms/{term_id}
  - GlossaryPanel rewrite in DocumentReader.tsx: cached load on mount, category badges, search filter, sort toggle, regenerate button, section scroll link, loading/error/empty states
  - 5 new tests in test_glossary_s193.py
  - Smoke test scripts/smoke/S193.sh
- Files changed: backend/app/models.py, backend/app/db_init.py, backend/app/database.py, backend/app/services/explain.py, backend/app/routers/explain.py, backend/tests/test_glossary_s193.py, backend/tests/test_explain.py, backend/tests/test_auto_collections.py, frontend/src/components/reader/DocumentReader.tsx, scripts/smoke/S193.sh
- **Learnings for future iterations:**
  - StaticPool for in-memory SQLite is incompatible with pool_size/max_overflow/pool_timeout/pool_recycle -- must conditionally skip these params in make_engine
  - Small LLMs (mistral 7B) ignore system prompts with large text context (32K chars) -- reduced to 16K chars and added explicit JSON format example at the end of the user prompt
  - LLM JSON output parsing needs regex fallback: re.search(r'\[.*\]', raw, re.DOTALL) to extract array from mixed text
  - Pre-existing test_auto_collections assertion was stale after S192 naming convention change (_make_collection_name)
---

## 2026-04-01T08:00:00Z - S194
- What was implemented: URL validation for web references. WebReferenceModel gains is_valid (nullable bool) and last_checked_at (nullable datetime). New ReferenceValidatorService performs async HEAD requests (httpx, 5s timeout, Semaphore(5), max 20 URLs). POST /references/documents/{id}/validate endpoint. GET endpoint gains include_invalid query param. Reference extraction validates URLs before persisting. Frontend ReferencesPanel shows three tiers (verified/unchecked/unavailable) with auto-validation on mount and refresh button.
- Files changed: backend/app/models.py, backend/app/db_init.py, backend/app/services/reference_validator.py (new), backend/app/services/reference_enricher.py, backend/app/routers/references.py, backend/tests/test_reference_validator.py (new), backend/tests/test_reference_enricher.py, frontend/src/components/reader/ReferencesPanel.tsx, scripts/smoke/S194.sh (new)
- **Learnings for future iterations:**
  - When adding URL validation to an existing enrichment service, existing tests that assert "no HTTP calls" will break because the new validation calls httpx. Mock _validate_urls in those tests.
  - Avoid double-validation in refresh handlers: if enrich() already validates internally, don't call validate_references() again afterward.
  - Reviewer Warning: auto-validation useEffect with [hasUnchecked] dependency relies on staleTime to prevent re-triggering. A useRef guard would be more robust.
---

## 2026-04-01T12:00:00Z - S195
- What was implemented: Bloom-progressive LLM-powered chat suggestions with dedup and history tracking
  - SuggestionService: LLM generation with Bloom taxonomy targeting, Jaccard dedup, history persistence, template fallback
  - ChatSuggestionHistoryModel (already existed in models.py)
  - GET /chat/suggestions returns {id, text} objects; POST /chat/suggestions/{id}/asked returns 204
  - Frontend: SuggestionPills updated for new shape, POST /asked on click, query invalidation after done event
  - 10 tests: 6 existing updated for new response shape + 4 new (history dedup, bloom decrease, LLM fallback, Jaccard filter)
- Files changed: backend/app/services/suggestion_service.py (line length fixes, format string escaping), backend/tests/test_suggestions.py (full rewrite for new shape + new tests), frontend/src/pages/Chat.tsx (SuggestionsResponse interface, SuggestionPills, query invalidation), scripts/smoke/S195.sh (new)
- **Learnings for future iterations:**
  - Python str.format() interprets curly braces in prompt strings; use double braces {{...}} for JSON examples in format templates
  - Lazy import patch target: when function is imported lazily via `from app.services.X import fn`, patch at source module `app.services.X.fn` not destination
  - Tests exercising LLM path require SummaryModel to be seeded; without executive summary the router guards skip the LLM call entirely
  - Backend service and router were already partially implemented from prior partial commit; always check existing state before creating files
---

## 2026-04-01 - S196
- What was implemented: Non-destructive scope clear in Chat.tsx. Clicking X on scope selector now inserts a divider instead of clearing conversation. All->single transition also uses divider. SuggestionPills render after scope-change dividers.
- Files changed: frontend/src/pages/Chat.tsx, scripts/smoke/S196.sh, scripts/ralph/prd-v3.json, docs/ralph-run-flow.md
- **Learnings for future iterations:**
  - SuggestionPills render condition must account for scope-change dividers (not just empty messages)
  - Frontend-only stories need minimal smoke (tsc only) -- no backend endpoints to curl
  - The divider pattern from S186 (single->single doc switch) was reusable for all scope transitions
---

## 2026-04-01T00:00:00Z - S197 - Compare notes with book: auto-collection gap analysis
- Implemented: notes_gap_node auto-fetches from NoteCollectionModel (auto_document_id) + NoteCollectionMemberModel instead of NoteModel.document_id; count-based card messages for 0 and <3 notes; auto_collection_id in gap result card payload; GapDetectDialog quick/advanced modes with auto-collection auto-populate; GapResultCard "Take a note" per gap with luminary:navigate dispatch; DocumentReader "Compare my notes" button (noteCount >= 3, autoSubmit chatPreload); App.tsx navigate handler for prefilledContent+collectionId; notePreload store field; NoteReaderSheet initialContent/initialCollectionId props
- Files changed: backend/app/runtime/chat_graph.py, backend/tests/test_chat_graph_gap.py, frontend/src/App.tsx, frontend/src/components/GapDetectDialog.tsx, frontend/src/components/GapResultCard.tsx, frontend/src/components/NoteReaderSheet.tsx, frontend/src/components/reader/DocumentReader.tsx, frontend/src/pages/Chat.tsx, frontend/src/pages/Notes.tsx, frontend/src/store.ts, scripts/smoke/S197.sh
- **Learnings for future iterations:**
  - Auto-collection query is two-step: first find NoteCollectionModel by auto_document_id, then fetch NoteCollectionMemberModel.note_id by collection_id
  - chatPreload autoSubmit flag requires setTimeout to defer sendMessage() until scope/docId state updates are applied by React
  - When adding a new Zustand store field consumed by a useEffect that opens a sheet, clearing the preload with queueMicrotask ensures the sheet reads the value before it's cleared
  - _mock_get_session_factory can be extended with list-of-lists pattern for sequential execute calls (side_effect)
---

## 2026-04-02 - S198
- Completed partial S198 implementation: fixed resolveSourceRefUtils.ts to guard `instanceof Element` and `instanceof HTMLElement` checks for Vitest node environment compatibility; fixed test mock to not use `Object.setPrototypeOf(sectionDiv, HTMLElement.prototype)` (unavailable in node env); created smoke test scripts/smoke/S198.sh
- Files changed: frontend/src/components/reader/resolveSourceRefUtils.ts (fixed), frontend/src/components/reader/resolveSourceRefUtils.test.ts (fixed), scripts/smoke/S198.sh (new), scripts/ralph/prd-v3.json (passes=true), docs/ralph-run-flow.md (pass), docs/exec-plans/active/S198.md -> completed/
- **Learnings for future iterations:**
  - `instanceof Element` and `instanceof HTMLElement` are not available in Vitest node environment -- guard with `typeof Element !== "undefined"` before using instanceof
  - For the manual DOM walk fallback in node env, use `"dataset" in node` property check instead of instanceof HTMLElement
  - Test mocks for DOM nodes in node env should use plain objects with `dataset` property, not `Object.setPrototypeOf(obj, HTMLElement.prototype)`
  - Reviewer warnings about out-of-scope changes from partial commits are non-blocking
---

## 2026-04-02 - S199
- What was implemented: Naming convention normalization for tags (lower-case, hyphen-separated, '/' preserved) and collections (UPPER-CASE, hyphen-separated). Backend naming.py with normalize_collection_name and normalize_tag_slug. Applied in collections.py (_make_collection_name, create_collection, update_collection), notes.py (_sync_tag_index), tags.py (create_tag, autocomplete). Migration endpoints POST /tags/migrate-naming and POST /collections/migrate-naming merge duplicates. Frontend normalizers in tagUtils.ts with live preview in TagAutocomplete and CreateCollectionDialog. 25 new tests (11 pure unit + 5 integration).
- Files changed: backend/app/services/naming.py, backend/app/routers/collections.py, backend/app/routers/tags.py, backend/app/routers/notes.py, backend/tests/test_S199.py (new), frontend/src/lib/tagUtils.ts, frontend/src/components/TagAutocomplete.tsx, frontend/src/components/CreateCollectionDialog.tsx, scripts/smoke/S199.sh, scripts/ralph/prd-v3.json, docs/ralph-run-flow.md
- **Learnings for future iterations:**
  - Most implementation was done in a prior partial commit; this iteration only needed the test file
  - Migration endpoint note_count can over-count when _sync_tag_index increments on INSERT CONFLICT during rename path; set initial note_count=0 when recreating canonical tags to avoid double-counting
  - Reviewer warning: lazy import of naming.py in notes.py is unnecessary (no circular risk) but harmless
---

## 2026-04-02 - S200
- What was implemented: PDF Viewer link handling extracted to pdfLinkService.ts. GoTo action dictionaries (dest and D key) now unwrapped and resolved. GoToR actions open external URLs. External links (http/https/mailto/tel) always get target='_blank' regardless of newWindow flag. --scale-factor CSS variable synced on canvas and annotation layer divs (not just text layer). Annotation layer z-index (3) set above text layer z-index (2) so link annotations are not blocked by highlight marks.
- Files changed: frontend/src/components/reader/pdfLinkService.ts (new), frontend/src/components/reader/pdfLinkService.test.ts (new, 12 tests), frontend/src/components/reader/PDFViewer.tsx (modified), scripts/smoke/S200.sh (new), scripts/ralph/prd-v3.json (passes=true), docs/ralph-run-flow.md (pass), docs/exec-plans/active/S200.md -> completed/
- **Learnings for future iterations:**
  - isExternalUrl protocol matching must include the colon (use `url.startsWith("http:")` not `url.startsWith("http")`) to avoid false positives on protocol lookalikes (e.g. httpx, telnet)
  - In Vitest node env, `window` is not defined -- use `vi.stubGlobal("window", { open: openSpy })` to mock window.open
  - pdfjs AnnotationLayer CSS already sets pointer-events:none on the container and pointer-events:all on individual link elements -- no explicit pointer-events style needed on the div
  - Reviewer warnings: console.log debug statements and as-any type assertions will be addressed in S202
---
