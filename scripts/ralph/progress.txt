## 2026-03-06T03:00:00+00:00 - S55 - Cross-document holistic summary — All My Content library overview
- Implemented: (1) backend/app/models.py — added `LibrarySummaryModel(id: str PK, mode: str, content: str, created_at: datetime)`; no document_id FK (library-level aggregate data). (2) backend/app/db_init.py — imported LibrarySummaryModel to register with SQLAlchemy Base.metadata. (3) backend/app/services/summarizer.py — added `LIBRARY_SYSTEM_PROMPTS` dict (one_sentence/executive/detailed); added 4 methods to SummarizationService: `_fetch_library_cached(mode)`, `_store_library_summary(mode, content)`, `_fetch_all_executive_summaries()` (most-recent executive summary per doc), `_get_cross_doc_entities(min_docs=3, limit=20)` (Kuzu query with MENTIONED_IN edges, non-fatal try/except), `stream_library_summary(mode, model)` (cache-first, not-enough-summaries guard, builds input text from executive summaries + cross-doc entities, streams LLM), `invalidate_library_cache()` (delete all LibrarySummaryModel rows). (4) backend/app/routers/summarize.py — added `LibrarySummarizeRequest` schema; added `POST /summarize/all` endpoint (must be declared before `/{document_id}` route to avoid path conflict). (5) backend/app/workflows/ingestion.py — `_run_pregenerate` now calls `svc.invalidate_library_cache()` after pregenerate() completes. (6) frontend/src/pages/Learning.tsx — added imports (ChevronDown, Loader2, RefreshCw, cn); added `LibraryOverview` component with mode tabs, SSE streaming, error states; inserted `<LibraryOverview />` before "Recently accessed" section (visible when no filters/tags, page 1, not in select mode). (7) backend/tests/test_library_summary.py (new) — 4 async tests, all pass. (8) scripts/smoke/S55.sh (new) — POST /summarize/all returns 200 with text/event-stream. CI: 366 tests pass.
- Files changed: backend/app/models.py, backend/app/db_init.py, backend/app/services/summarizer.py, backend/app/routers/summarize.py, backend/app/workflows/ingestion.py, frontend/src/pages/Learning.tsx, backend/tests/test_library_summary.py (new), scripts/smoke/S55.sh (new), scripts/ralph/prd.json (S55 passes=True)
- Learnings:
  - POST /summarize/all must be declared BEFORE POST /{document_id} in FastAPI router or FastAPI matches "all" as document_id
  - patch("app.services.summarizer.get_graph_service") fails because get_graph_service is a local import inside _get_cross_doc_entities — must either add module-level import or let the method's try/except handle it naturally in tests
  - LibrarySummaryModel has no document_id FK — use delete(LibrarySummaryModel) to invalidate all modes at once when a new doc arrives
---

## 2026-03-06T02:00:00+00:00 - S61 - Document attribution in cross-document Chat — show source document in citation chips
- Implemented: (1) backend/app/services/qa.py — added `_enrich_citation_titles(citations, chunks, doc_titles, scope) -> list[dict]` function: for scope='single' sets document_title=None on all citations (redundant for single-doc view); for scope='all' builds chunk index by (section_heading, page) and overwrites document_title from doc_titles dict for each matched citation; called inside stream_answer immediately after _split_response, reusing already-available chunks and doc_titles variables (no extra DB query). (2) frontend/src/pages/Chat.tsx — changed Citation interface `document_title: string` to `document_title: string | null`; updated chip render: when document_title present → `{title[:20]}{'…' if >20} · p.{page}`; when null → `p.{page}`. (3) backend/tests/test_qa.py — imported `_enrich_citation_titles`; fixed `test_stream_final_event_contains_citations` (scope='single' → asserts document_title is None); added 2 unit tests (test_enrich_citation_titles_single_scope_clears_title, test_enrich_citation_titles_all_scope_populates_from_chunks) + 2 integration tests (test_citations_include_document_title_for_all_scope, test_citations_no_title_for_single_scope). All 362 tests pass. (4) scripts/smoke/S61.sh (new).
- Files changed: backend/app/services/qa.py, frontend/src/pages/Chat.tsx, backend/tests/test_qa.py, scripts/smoke/S61.sh (new), scripts/ralph/prd.json (S61 passes=True)
- Learnings:
  - doc_titles is already built from chunks before _split_response — _enrich_citation_titles reuses it directly, no extra DB round-trip
  - Matching by (section_heading.strip(), page) is reliable: LLM extracts these verbatim from context prefix
  - scope='single' always gets document_title=None even if LLM set a value — frontend nil-check decides format
  - TypeScript `string | null` in union type is required for JSX template literal conditional on citation chip
---

## 2026-03-06T01:00:00+00:00 - S58 - Query rewriting — resolve vague references using Kuzu entities before retrieval
- Implemented: (1) backend/app/services/graph.py — added `get_entities_for_documents(document_ids, limit=15) -> list[str]` to KuzuService: iterates over doc IDs, uses MENTIONED_IN edges to find Entity nodes with type IN ['PERSON', 'PLACE', 'CONCEPT'], deduplicates, returns up to limit names; returns [] on any Kuzu error (non-fatal). (2) backend/app/services/qa.py — added `import from app.services.graph`; added module-level `VAGUE_REF_RE` regex constant (16 vague-ref patterns, compiled once, IGNORECASE); added `_REWRITE_SYSTEM` prompt constant; added async `_maybe_rewrite_query(question, document_ids) -> str` function implementing all 4 guard conditions (no vague refs → return unchanged; document_ids=None → return unchanged; empty Kuzu results → return unchanged; LLM failure → log warning + return unchanged); modified `stream_answer` to call `_maybe_rewrite_query(question, effective_doc_ids)` before retrieval, use rewritten query for `retriever.retrieve()` but keep original question in the LLM prompt, log when rewrite occurs. (3) backend/tests/test_query_rewriting.py (new) — 5 async tests, all with mocked Kuzu and LLM: test_no_rewrite_when_no_vague_refs (0 Kuzu/LLM calls for 'What is osmosis?'), test_rewrite_triggered_for_pronoun (LLM called + entity names in prompt), test_skip_rewrite_for_all_scope (document_ids=None → no calls), test_skip_rewrite_when_kuzu_empty (empty entities → LLM not called), test_llm_failure_returns_original (exception → original returned). All 358 tests pass. (4) scripts/smoke/S58.sh (new) — POST /qa with scope=all and scope=single both return 200.
- Files changed: backend/app/services/graph.py, backend/app/services/qa.py, backend/tests/test_query_rewriting.py (new), scripts/smoke/S58.sh (new), scripts/ralph/prd.json (S58 passes=True)
- Learnings:
  - Entity nodes in Kuzu don't have a document_id field — use MENTIONED_IN relationship to find entities per document (prd.json description had wrong query; used correct schema)
  - Kuzu's WHERE e.type IN ['PERSON', 'PLACE', 'CONCEPT'] works as a literal list in Cypher — no parameter needed for the type filter
  - Iterating over document_ids is safer than list parameters for IN clause (avoids Kuzu version-specific parameter binding differences)
  - Import `get_graph_service` at module level in qa.py (not inside function) — no circular import risk since graph.py doesn't import from qa.py
---

## 2026-03-06T00:00:00+00:00 - S60 - Book chapter navigation and conversation speaker roster in Learning tab
- Implemented: All backend and frontend code was already in place from prior stories. (1) backend/app/routers/sections.py — GET /sections/{document_id} exists: returns list[SectionResponse] ordered by section_order with accurate chunk_count from a single batch GROUP BY query; has_summary always False (section summaries not yet implemented). (2) backend/app/routers/documents.py — GET /documents/{id}/conversation exists: returns conversation_metadata (speakers, total_turns, has_timestamps, first_timestamp, last_timestamp); returns 404 for unknown doc, 400 for non-conversation. (3) frontend/src/components/reader/DocumentReader.tsx — ContentSidebar component renders Chapters panel (book: section list with chunk_count badge, click-to-highlight, "Coming soon" collapsed summary row, 3-skeleton loading, empty state) and Speakers panel (conversation: speaker list with turn_count badge, click-to-highlight, timestamp timeline if has_timestamps, 2-skeleton loading, empty state); no sidebar for notes/other types; integrated into DocumentReader as w-48 shrink-0 panel at left. (4) backend/tests/test_sections.py (tracked from untracked): 3 tests all pass — test_sections_endpoint_returns_ordered_list, test_sections_endpoint_chunk_count_matches_db, test_conversation_metadata_endpoint_returns_roster. (5) scripts/smoke/S60.sh (new) — GET /sections/nonexistent returns [] (200); GET /documents/nonexistent/conversation returns 404.
- Files changed: scripts/smoke/S60.sh (new), scripts/ralph/prd.json (S60 passes=True)
- Learnings:
  - All implementation was already present; the story just needed tests verified and smoke test created
  - ContentSidebar uses enabled: contentType === "book" / "conversation" so queries only fire for the relevant type — avoids 400 errors from GET /documents/{id}/conversation on non-conversation docs
  - SectionResponse includes has_summary (always False for now) — future-proof field for when section-level summaries are added
---

## 2026-03-05T01:00:00+00:00 - S63 - Mandatory content type selection in upload — book, conversation, notes
- Implemented: (1) backend/app/workflows/ingestion.py — added `ContentType = Literal["book", "conversation", "notes"]`; added fast-path at top of `classify_node`: if `state.get("content_type") is not None`, skip all heuristics and LLM, return immediately with `status='chunking'`; updated `run_ingestion()` signature to accept `content_type: str | None = None` and pass it into `IngestionState`. (2) backend/app/routers/documents.py — added `Form` to fastapi imports; imported `ContentType` from `app.workflows.ingestion`; added `content_type: ContentType | None = None` to `PatchDocumentRequest`; updated `POST /documents/ingest` to require `content_type: ContentType = Form(...)`; updated `doc.content_type = content_type` at creation time; passes `content_type` to `run_ingestion()`; updated `PATCH /{id}` to update `content_type` in DB with note `'Re-ingest document to apply new chunking strategy.'` in response. (3) backend/tests/test_content_type.py (new) — 4 async tests: test_ingest_without_content_type_returns_422, test_ingest_with_valid_type_skips_classify_llm (checks DB content_type=book), test_patch_content_type_updates_document (checks DB + response note), test_invalid_content_type_returns_422. (4) frontend/src/components/library/UploadDialog.tsx — removed auto-detect option; added `CONTENT_TYPE_OPTIONS` array with book/conversation/notes; added `uploadType`/`pasteType` states (null until selected); added `typeError` state; `ContentTypeRadioGroup` inline component renders 3 radio buttons; upload button disabled until type selected; `submitFile()` now sends `content_type` in FormData. (5) frontend/src/components/library/DocumentCard.tsx — added `CONTENT_TYPE_BADGE` map (book=blue, conversation=green, notes=gray, paper=purple, code=orange); added clickable badge pill below title; `typePopoverOpen` state + outside-click dismiss; popover shows 3 changeable types with tooltip note; `handleTypeChange()` calls PATCH /documents/{id} and invokes `onContentTypeChange` prop. (6) frontend/src/pages/Learning.tsx — added `handleContentTypeChange()` that invalidates documents queries; passed `onContentTypeChange` to both DocumentCard usages. (7) scripts/smoke/S63.sh (new) — 4 smoke checks: 422 on missing type, 422 on invalid type, 200 on valid ingest, PATCH with re-ingest note. Fixed existing tests: test_api_contracts.py, test_e2e_upload.py (3 calls), test_ingest.py (2 calls), test_logging.py (4 calls) — all needed `data={"content_type": "..."}` in ingest requests.
- Files changed: backend/app/workflows/ingestion.py, backend/app/routers/documents.py, backend/tests/test_content_type.py (new), backend/tests/test_api_contracts.py, backend/tests/test_e2e_upload.py, backend/tests/test_ingest.py, backend/tests/test_logging.py, frontend/src/components/library/UploadDialog.tsx, frontend/src/components/library/DocumentCard.tsx, frontend/src/pages/Learning.tsx, scripts/smoke/S63.sh (new), scripts/ralph/prd.json (S63 passes=True)
- Learnings:
  - FastAPI `Form(...)` makes a form field required — missing field returns 422 automatically (no manual check needed); matches prd acceptance criterion "HTTP 422 if missing"
  - `asyncio.create_task(coro)` schedules a background task; the coroutine body hasn't executed when the response is returned — tests that mock run_ingestion and check `called` list will fail; instead verify DB state which is set synchronously before the task is started
  - ContentType Literal alias imported from ingestion.py into documents.py avoids duplication and keeps PatchDocumentRequest in sync with the workflow
  - Outside-click dismiss for inline popover: `document.addEventListener("mousedown", handler)` + `ref.contains(e.target)` pattern; useEffect cleanup removes the listener
  - All pre-existing ingest tests needed `data={"content_type": "notes"}` added — systematic fix across 4 test files
---

## 2026-03-05T00:00:00+00:00 - S62 - Fix tags — audit and repair end-to-end tag storage, filter, and UI
- Implemented: (1) backend/app/routers/documents.py — added `import json`; added `_safe_tags(raw) -> list[str]` helper that normalises tags regardless of how they were stored (None→[], list→list[str], JSON string→list[str]); updated list_documents and get_document to use `_safe_tags(doc.tags)` instead of `doc.tags or []`; tag filter `tag in i.tags` is now safe because i.tags is always a Python list (exact element match, not substring). (2) backend/tests/test_tags.py (new) — 4 async tests: test_patch_tags_stores_list (PATCH returns list; GET /documents returns list[str]), test_tag_filter_returns_matching_docs (only tagged doc returned), test_tag_filter_no_substring_collision (tag='bio' does NOT match tags=['biology']), test_patch_tags_replaces_not_appends (second PATCH replaces, doesn't append). (3) frontend/src/pages/Learning.tsx — import useSearchParams from react-router-dom; replaced `useState<string|null>(null)` tagFilter with `useSearchParams()` (tagFilter = searchParams.get('tag')); handleTagClick sets URL param; clear filter button removes URL param; empty state message now shows 'No documents tagged "X".' when tagFilter is active. (4) scripts/smoke/S62.sh (new) — ingest doc, PATCH tags, verify GET ?tag= returns doc, verify GET ?tag=substring returns no match.
- Files changed: backend/app/routers/documents.py, backend/tests/test_tags.py (new), frontend/src/pages/Learning.tsx, scripts/smoke/S62.sh (new), scripts/ralph/prd.json (S62 passes=True)
- Learnings:
  - SQLAlchemy JSON on SQLite may return raw string if row was written outside of ORM; _safe_tags() defensive helper handles all 3 cases (None, list, JSON string)
  - `tag in list` is exact element match; `tag in string` is substring — the bug manifests when doc.tags is not deserialized; _safe_tags fixes the root cause
  - useSearchParams (React Router) persists filter state across navigation; replaces useState which resets on re-mount
  - ruff I001 (import sorting) triggered by stdlib-before-third-party order in test file; `ruff --fix` auto-resolves
---

## 2026-02-27T02:00:00+00:00 - S52 - End-to-end integration test — full pipeline from ingest to all 5 features on Time Machine
- Implemented: Created backend/tests/test_e2e_book.py — TestTimeMachineEndToEnd class (9 tests) referencing all_books_ingested session fixture from conftest_books.py via pytest_plugins. Tests: test_ingest_completed (stage='complete'), test_diagnostics_counts (chunk>=100, fts>=100, vector>=50, entity>=20, edge>=10), test_vector_search (>=3 results for 'time traveller'), test_keyword_search (>=2 results for 'Eloi Morlocks'), test_hybrid_retrieve (>=5 results for 'What is the Time Machine?'), test_qa_stream (>=1 token event, done=true with citations>=1 — mocks get_llm_service via patch), test_graph_nodes (>=20 nodes, >=10 edges), test_search_endpoint (>=1 result for 'Weena'), test_ingest_elapsed (<=600s). Marked @pytest.mark.slow — excluded from default CI. TypeScript fix: removed unused X import from Notes.tsx (delete confirmation uses Trash2 icon, not X). Fixed scripts/smoke/S49.sh.
- Files changed: backend/tests/test_e2e_book.py (new), frontend/src/pages/Notes.tsx (remove unused X import), scripts/smoke/S52.sh (new), scripts/ralph/prd.json (S52 passes=True)
- Learnings:
  - test_e2e_book.py uses pytest_plugins = ["tests.conftest_books"] to share session-scoped ingestion fixture across test files — avoids re-ingesting 3 books per test file
  - QA stream test mocks app.services.qa.get_llm_service entirely — avoids Ollama dependency in CI; _async_iter helper yields string chunks matching expected SSE format
  - make ci must pass before marking passes=true — TypeScript unused imports (ruff catches Python, tsc catches TS)
---

## 2026-02-27T01:00:00+00:00 - S51 - Fix Study tab — HTTP 503 for Ollama offline, verify FSRS review without LLM
- Implemented: Audit first — found 2 backend issues + 1 frontend issue. (1) backend/app/routers/flashcards.py — added import litellm; wrapped service.generate() call in try/except catching ServiceUnavailableError, APIConnectionError, ConnectionRefusedError → HTTPException(503, 'Ollama is not running. Start it with: ollama serve'). Does NOT catch generic Exception. (2) backend/app/routers/study.py — added 4 new schemas (SessionCardResponse, SessionStartResponse, SessionReviewRequest, SessionReviewResponse) + _RATING_INT_MAP + _get_due_for_session() helper (queries due_date<=now OR due_date IS NULL) + POST /study/session/{doc}/start + POST /study/session/{doc}/review endpoints; added `from sqlalchemy import or_, select` and `from app.services.fsrs_service import get_fsrs_service`. (3) frontend/src/pages/Study.tsx — added GenerateError class (extends Error with status:int) to carry HTTP status code; generateMutation.onError now differentiates 503 (→ inline amber banner "Ollama is not running. To generate flashcards, run: ollama serve" with Copy button) vs other errors (→ red inline alert "Flashcard generation failed. Please try again."); added generateErrorKind state (GenerateErrorKind='ollama_offline'|'server_error'|null); banners rendered inside tab content area, not as toast. (4) backend/tests/test_fsrs_api.py (new) — 5 fast async tests with isolated in-memory SQLite: test_start_session, test_review_card, test_session_complete, test_due_date_updated, test_503_on_generate. (5) scripts/smoke/S51.sh (new) — /study/due 200 + /flashcards/generate 422 without document_id.
- Files changed: backend/app/routers/flashcards.py, backend/app/routers/study.py, frontend/src/pages/Study.tsx, backend/tests/test_fsrs_api.py (new), scripts/smoke/S51.sh (new), scripts/ralph/prd.json (S51 passes=True)
- Learnings:
  - chunk_id is NOT NULL in flashcards table — test helpers that insert FlashcardModel must use uuid.uuid4() not None
  - or_() from sqlalchemy needed to query due_date <= now OR due_date IS NULL (nullable column)
  - GenerateError class extends Error with status field — allows onError handler to inspect HTTP status without coupling to fetch response object
  - Post-503 FSRS session API uses stateless design (no session_id needed — just document_id + card_id per call)
---

## 2026-02-26T04:00:00+00:00 - S50 - Verify and fix Notes tab — CRUD without LLM, loading/empty/error states
- Implemented: Audit first — found 5 issues. (1) backend/app/routers/notes.py — added PATCH /notes/{id} endpoint (acceptance criteria requires PATCH; existing PUT retained for backward compat; both call shared _apply_note_update() helper). (2) frontend/src/pages/Notes.tsx — full rewrite: loading state→3 Skeleton h-12 rows (was 6 h-28 grid); empty state→"No notes yet. Click + to create your first note." + New note button with Network icon (was no button); error state→inline amber alert "Could not load notes" + Retry button (was red, no retry); added CreateNoteForm component (textarea rows=4, comma-separated tags input, document selector dropdown via GET /documents, Save→POST /notes, Cancel; invalidates notes+groups on success); delete→inline "Delete this note? [Yes] [No]" with Trash2 icon (was immediate delete on X click); edit→PATCH /notes/{id} (was PUT); added patchNote() and createNote() API helpers; added fetchDocumentList() helper. (3) backend/tests/test_notes_api.py (new) — 5 fast tests: test_create_note (content+id+created_at), test_list_notes (both notes in response), test_update_note (PATCH, content updated), test_delete_note (204 + absent from list), test_note_not_found (PATCH+DELETE 404). (4) scripts/smoke/S50.sh (new) — POST/GET/PATCH/DELETE full CRUD cycle smoke test.
- Files changed: backend/app/routers/notes.py, frontend/src/pages/Notes.tsx, backend/tests/test_notes_api.py (new), scripts/smoke/S50.sh (new), scripts/ralph/prd.json (S50 passes=True)
- Learnings:
  - Backend had PUT but acceptance criteria required PATCH; added PATCH alias via shared helper — keeps backward compat without duplication
  - fetchDocumentList must handle both paginated {items:[...]} and flat array responses (same as Monitoring.tsx pattern)
  - CreateNoteForm is a self-contained component inside Notes.tsx — no separate file needed since it's only used here
  - Empty state must have an actionable CTA (+ button) per invariant 13, not just descriptive text
---

## 2026-02-26T03:00:00+00:00 - S49 - Fix Viz tab — loading/empty/error states and verify graph data flows after S46
- Implemented: (1) frontend/src/pages/Viz.tsx — audited: all 3 states already correctly implemented: isLoading→Skeleton (animate-pulse, no Sigma), isError→inline red alert + Retry (no Sigma), nodes.length===0→Network icon + 'No knowledge graph yet' + body text (no Sigma); Sigma container guarded by {filteredGraph && (...)} in full layout which is only reached when nodes.length>0. (2) backend/tests/test_graph_api.py (new, untracked→tracked) — @pytest.mark.slow; test_graph_returns_nodes_after_ingest: isolated env with real SQLite/LanceDB/Kuzu/NER/embedder; ingests time_machine.txt; GET /graph/{id}; asserts status=200, len(nodes)>=20, len(edges)>=10; every node has id/label/type/size(>0); every edge has source/target/weight. (3) scripts/smoke/S49.sh (new) — GET /graph?doc_ids= returns 200 + 'nodes' + 'edges' keys.
- Files changed: frontend/src/pages/Viz.tsx (no changes needed — already correct), backend/tests/test_graph_api.py (new), scripts/smoke/S49.sh (new), scripts/ralph/prd.json (S49 passes=True)
- Learnings:
  - Viz.tsx had all 3 states correctly implemented; ROOT CAUSE identified in story description was pre-emptive — the prior implementation already guarded all three states with early returns
  - graph API returns label (not name) — PRD acceptance criteria text has a typo; test correctly checks label to match actual API and frontend GraphNode interface
  - test_graph_api.py fixture pattern mirrors test_integration_full.py: monkeypatch DATA_DIR, make_engine, create_all_tables, reset module-level singletons; still mocks LiteLLM classify
---

## 2026-02-26T02:00:00+00:00 - S47 - Ingestion health panel — diagnostic counts in library document detail
- Implemented: (1) frontend/src/components/library/IngestionHealthPanel.tsx (new) — receives {documentId, stage}; useQuery with staleTime=0, enabled=stage==='complete'; stage!='complete' shows 'Ingestion in progress...'; isLoading: 5 Skeleton(h-8); isError: amber alert + Retry button (calls refetch()); success: 2-col grid of 5 MetricCard + healthBadge; badge: all>0→green 'Healthy', any=0→amber 'Partial', all=0→red 'Empty — ingestion may have failed'; DiagnosticsResponse interface 5 number fields, no any. (2) frontend/src/components/reader/DocumentReader.tsx — import IngestionHealthPanel; rendered in document header div below title/metadata.
- Files changed: frontend/src/components/library/IngestionHealthPanel.tsx (new), frontend/src/components/reader/DocumentReader.tsx, scripts/ralph/prd.json (S47 passes=True)
- Learnings:
  - useQuery enabled option: pass enabled=stage==='complete' to prevent fetching for in-progress documents
  - IngestionHealthPanel placed in DocumentReader (not Learning.tsx directly) since Learning.tsx delegates to DocumentReader when activeDocumentId is set
  - Badge component only has green variant; use inline tailwind for amber/red health states
---

## 2026-02-26T01:00:00+00:00 - S46 - Diagnose and fix entity extraction → Kuzu graph pipeline (0-node root cause)
- Implemented: (1) backend/app/workflows/ingestion.py entity_extract_node — restructured to track entity_count=0 before try block; logs entity_count at INFO in finally block (runs after EVERY execution, even on exception); inner try/except around Kuzu write operations re-raises if entity_count>0 but Kuzu write failed (data loss must not be silent); outer except logs with exc_info=exc. (2) backend/app/services/graph.py — added count_for_document(document_id) method: Kuzu Cypher queries for entity count and CO_OCCURS edge count; returns (int, int) tuple; wraps in try/except returning (0,0) on error. (3) backend/app/services/vector_store.py — added count_for_document(document_id) method: uses table.count_rows(filter); returns 0 on error. (4) backend/app/routers/documents.py — added DocumentDiagnostics Pydantic model (5 int fields); added GET /documents/{id}/diagnostics endpoint: returns 404 for unknown doc, queries chunk_count, fts_count, vector_count, entity_count, edge_count; all store counts return 0 if unavailable. (5) backend/tests/conftest_books.py (new) — session-scoped all_books_ingested fixture: verifies book files exist and meet word_count_min; ingests 3 books in asyncio.run(); LiteLLM mocked; records elapsed; asserts complete+budget; resets engine/factory singletons after asyncio.run(). (6) backend/tests/test_diagnostics.py (new) — pytest_plugins=["tests.conftest_books"]; fast test_diagnostics_not_found (in-memory DB); parametrized slow test_diagnostics_counts[3 books] (reads thresholds from manifest.json); test_ingest_timing_odyssey.
- Files changed: backend/app/workflows/ingestion.py, backend/app/services/graph.py, backend/app/services/vector_store.py, backend/app/routers/documents.py, backend/tests/conftest_books.py (new), backend/tests/test_diagnostics.py (new), scripts/ralph/prd.json (S46 passes=True)
- Learnings:
  - asyncio.run() for session-scoped sync fixture: after it returns, SQLAlchemy engine/factory are bound to a closed loop — reset db_module._engine=None so tests get fresh connections via DATA_DIR singleton pattern
  - pytest_plugins in test file: use "tests.conftest_books" (not "conftest_books") when pytest rootdir is backend/ and tests/ is a sub-package
  - LanceDB count_rows(filter): optional SQL-like string filter e.g. "document_id = 'abc'"; returns int
  - Kuzu CO_OCCURS edge count: MATCH ()-[r:CO_OCCURS {document_id: $did}]->() RETURN count(*) counts directed edges by property; no double-counting since add_co_occurrence creates one directed edge per pair
  - test_diagnostics_not_found fast test needs its own in-memory DB fixture — without it endpoint hits "no such table: documents"
---

## 2026-02-26T00:00:00+00:00 - S45 - Book corpus canon — three-book golden fixture set
- Implemented: (1) DATA/books/time_machine.txt — copied from backend/tests/fixtures/full/time_machine.txt (32471 words, above 30000 minimum). (2) DATA/books/alice_in_wonderland.txt — downloaded from Project Gutenberg #11 via curl (29564 words, above 25000 minimum). (3) DATA/books/the_odyssey.txt — already present (132605 words, above 100000 minimum). (4) DATA/books/manifest.json (new) — JSON array with 3 entries; each entry has name, filepath, gutenberg_id, word_count_min, ingest_time_budget_seconds, thresholds (5 fields), known_entities, known_keywords, known_co_occurrences, semantic_queries (2+ per book). All per-book values match prd.json spec exactly. (5) scripts/corpus/setup_books.sh (new, executable) — downloads any missing book from Gutenberg via curl, verifies each file is present, asserts word count >= word_count_min, exits 1 on failure. (6) backend/tests/test_corpus_fixtures.py (new) — 4 tests: test_manifest_parses_and_has_required_fields (validates all 9 required fields per entry plus threshold fields and semantic_queries structure), test_book_word_count parametrized for all 3 books (asserts file exists + word count >= minimum). (7) backend/tests/test_config.py — fixed pre-existing failure: test_settings_defaults temporarily unsets PHOENIX_ENABLED env var (set by conftest to 'false') before asserting default=True; restores it in finally block.
- Files changed: DATA/books/time_machine.txt (new), DATA/books/alice_in_wonderland.txt (new), DATA/books/manifest.json (new), scripts/corpus/setup_books.sh (new), backend/tests/test_corpus_fixtures.py (new), backend/tests/test_config.py (fix PHOENIX_ENABLED assertion), scripts/ralph/prd.json (S45 passes=True)
- Learnings:
  - Gutenberg plaintext URLs: https://www.gutenberg.org/cache/epub/{id}/pg{id}.txt — more reliable than www.gutenberg.org/files/ which sometimes redirects
  - conftest.py sets PHOENIX_ENABLED=false session-wide; any test asserting Settings() defaults must temporarily pop the env var before calling Settings()
  - wc -w counts whitespace-separated tokens; bash wc -w < file | tr -d ' ' strips leading spaces from the output on macOS (wc pads output)
---

## 2026-02-25T20:30:00+00:00 - S40 - Upload UX — inline progress bar, keep dialog open, inline error with retry
- Implemented: (1) frontend/src/components/ui/progress.tsx (new) — simple Progress bar component (h-2 rounded-full bg-secondary with inner div transitioning width via style). (2) frontend/src/components/library/UploadDialog.tsx — full rewrite with Mode state machine (idle/uploading/processing/success/error); doSubmit() fires upload, starts polling, logs via logger; startPolling() updates progress_pct + stageLabel per stage; success shows CheckCircle2, docTitle, 'View in library' button (useNavigate to '/'), auto-closes after 3s via setTimeout; error shows inline red alert with "Try again" + Cancel buttons (handleRetry re-submits same file); backdrop click + X button disabled while active (mode === uploading|processing); toast still fires as secondary notification with same docId so loading toast updates to success/error; timeEstimate() computes ~5s/MB heuristic countdown; logger.info per stage transition + logger.error on failure. (3) backend/app/workflows/ingestion.py — STAGE_PROGRESS fixed: indexing=80, entity_extract=90 (new entry), complete=100; await _update_stage(doc_id, "entity_extract") added at start of entity_extract_node; logger.debug("node_start", extra={node, doc_id}) added at start of each of the 6 nodes (parse, classify, chunk, embed, keyword_index, entity_extract).
- Files changed: frontend/src/components/ui/progress.tsx (new), frontend/src/components/library/UploadDialog.tsx, backend/app/workflows/ingestion.py, scripts/ralph/prd.json (S40 passes=True)
- Learnings:
  - useNavigate() must be called inside a component under BrowserRouter — UploadDialog is always rendered inside AppShell which is inside BrowserRouter, so this works fine
  - Dialog close prevention: set disabled={isActive} on X button and add guard check in handleClose(); backdrop onClick calls handleClose() which also guards
  - void (async () => {...})() pattern needed inside setInterval callback since setInterval doesn't accept async functions directly
  - STAGE_PROGRESS entity_extract key needed both in the map AND in _update_stage call; STAGE_LABELS in UploadDialog also needs entity_extract entry
  - eslint-disable-next-line react-hooks/exhaustive-deps comment needed on the cleanup useEffect since clearPolling/clearAutoClose are stable refs that don't need to be in deps
---

## 2026-02-25T19:30:00+00:00 - S39 - Frontend resilience — skeleton states, error isolation, and QueryClient tuning
- Implemented: (1) frontend/src/lib/logger.ts (new) — lightweight logger wrapping console.debug/info/warn/error with [Luminary] prefix + ISO timestamp. (2) frontend/src/components/ui/skeleton.tsx (new) — shadcn Skeleton component (animate-pulse rounded-md bg-muted). (3) frontend/src/App.tsx — QueryClient now uses QueryCache.onError for global error logging (TanStack Query v5 API); defaultOptions.queries: staleTime=60_000, refetchOnWindowFocus=false, retry=1; Viz and Monitoring imported via React.lazy; each wrapped in Suspense with PageSkeleton fallback; stale Loader2 import removed. (4) frontend/src/pages/Learning.tsx — LoadingSkeleton now uses Skeleton component instead of hand-rolled animate-pulse divs. (5) frontend/src/pages/Chat.tsx — useQuery destructures isLoading+isError; skeleton shown in place of model selector while loading; inline amber warning banner rendered when LLM settings fetch fails. (6) frontend/src/pages/Monitoring.tsx — Promise.all replaced with 7 independent fetch calls each with their own SectionState<T>; SectionErrorCard + SectionSkeleton + MetricCardSkeleton helper components; each section renders independently: skeleton while loading, error card if fetch fails, real content when resolved; logger.warn('[Monitoring] section failed', name, e) on each failure; fetchDocuments() handles both paginated {items:[]} and legacy [] responses. (7) backend/app/routers/documents.py — POST /ingest logs filename+size_bytes+format at INFO after saving file; GET /{id}/status logs document_id+stage at DEBUG.
- Files changed: frontend/src/lib/logger.ts (new), frontend/src/components/ui/skeleton.tsx (new), frontend/src/App.tsx, frontend/src/pages/Learning.tsx, frontend/src/pages/Chat.tsx, frontend/src/pages/Monitoring.tsx, backend/app/routers/documents.py, scripts/ralph/prd.json (S39 passes=True)
- Learnings:
  - TanStack Query v5 global onError: use QueryCache({ onError }) not defaultOptions.queries.onError (removed in v5)
  - React.lazy + Suspense: lazy routes produce separate JS chunks in Vite build output (Monitoring: 56 kB, Viz: 181 kB split from main bundle)
  - Independent section fetches: each section calls its own fetch in useEffect with .then/.catch — sections render as they resolve without waiting for siblings
  - fetchDocuments() in Monitoring must handle both paginated ({items:[...]}) and legacy plain-array responses since Monitoring doesn't use the filter params
  - Skeleton component must be created manually for this project (shadcn not installed as CLI) — just `animate-pulse rounded-md bg-muted` via cn()
---

## 2026-02-25T18:30:00+00:00 - S37 - Concurrent ingestion stability and API contract tests
- Implemented: (1) tests/test_concurrent.py — @pytest.mark.slow; asyncio.gather fires 5 simultaneous POST /ingest requests with mock embedding/NER services (file-based SQLite, temp LanceDB+Kuzu); asserts all documents reach stage='complete' or stage='error' within 30s; no Kuzu lock RuntimeError in any response body. (2) tests/test_api_contracts.py — 20 fast tests; covers: GET/DELETE/PATCH /documents/{id} 404, POST /documents/ingest 422 (no file) and 400 (unsupported type), POST /summarize 404+422, flashcards generate 422 + update/delete/review 404, notes create 422 + update/delete 404, GET /search missing q 422, POST /qa missing question 422, GET /monitoring/evals + /eval-history 200 list. (3) backend/app/routers/documents.py — added _ALLOWED_EXTENSIONS frozenset and validation in POST /ingest: returns 400 with {detail: "Unsupported file type..."} for .png, .exe, etc. (4) Makefile — added make test-concurrent target.
- Files changed: backend/tests/test_api_contracts.py (new), backend/tests/test_concurrent.py (new), backend/app/routers/documents.py, Makefile, scripts/ralph/prd.json (S37 passes=True)
- Learnings:
  - Contract tests use in-memory SQLite (not file-based) since they don't test concurrent writes
  - Concurrent test uses file-based SQLite for multi-connection safety; module-level singleton injection (not patch) is the correct pattern for mock ML services
  - ruff I001 fires when stdlib imports come before third-party; `ruff --fix` auto-resolves it
  - _ALLOWED_EXTENSIONS check uses .lower() on ext before comparison to handle mixed-case uploads
---

## 2026-02-25T17:30:00+00:00 - S36 - Retrieval quality gates — HR@5/MRR/Faithfulness thresholds
- Implemented: (1) evals/run_eval.py — added --assert-thresholds flag (exits 1 if HR@5 < 0.60, MRR < 0.45, Faithfulness < 0.65 when LLM scoring enabled via --model); appends every run to evals/scores_history.jsonl with {timestamp, dataset, model, hr5, mrr, faithfulness, passed}. (2) Makefile — added eval target (runs book + paper datasets with --assert-thresholds; not in make ci; requires backend on :8000). (3) backend/app/routers/monitoring.py — added GET /monitoring/eval-history endpoint that reads scores_history.jsonl line by line, parses EvalHistoryItem, returns list. Path anchored via Path(__file__).parent*4 to repo root. (4) frontend/src/pages/Monitoring.tsx — added EvalHistoryItem type, fetchEvalHistory(), buildSparklineData() (per-run rows by timestamp order), EvalHistorySparkline component (LineChart, per-dataset color, threshold reference line at 0.60). New "Retrieval Quality Over Time" section inserted between RAG Quality and Model Usage. (5) README.md — added Quality Gates section documenting make eval, threshold table, and scores_history.jsonl.
- Files changed: evals/run_eval.py, Makefile, backend/app/routers/monitoring.py, frontend/src/pages/Monitoring.tsx, README.md, scripts/ralph/prd.json (S36 passes=True)
- Learnings:
  - Recharts Tooltip formatter must handle undefined value/name args — use v: number | string | undefined and name: string | undefined
  - ruff PLW2901 fires when a for-loop variable is overwritten by assignment inside the loop (line = line.strip()) — rename to raw_line/stripped
  - Path(__file__).parent.parent.parent.parent correctly navigates from backend/app/routers/ to repo root for cross-package file reads
---

## 2026-02-25T13:00:00+00:00 - S35 - Replace synthetic golden datasets with document-grounded QA pairs
- Implemented: (1) backend/tests/fixtures/full/notes_sample.txt (new) — ~2200 words on ML fundamentals: learning paradigms, neural networks, backpropagation, overfitting/regularization, bias-variance tradeoff, k-fold CV, PCA, embeddings, evaluation metrics, Transformers, optimization, ML workflow. (2) backend/tests/fixtures/full/conversation_sample.txt (new) — ~3000 words engineering team transcript: API gateway rate limiting, Postgres 15 + pgBouncer, Jenkins→GitHub Actions+ArgoCD pipeline, search latency optimization with pgvector/HNSW. (3) All 5 golden/*.jsonl replaced — zero entries with document_id='synthetic'; each row has question/ground_truth_answer/context_hint/document_id='TBD'/source_file. Totals: book.jsonl=30 (Time Machine), paper.jsonl=30 (Art of Unix), notes.jsonl=20, conversation.jsonl=20, code.jsonl=20 = 120 total pairs. (4) evals/run_eval.py rewritten — load_manifest()/save_manifest() (maps source_file → document_id in evals/golden/manifest.json); ensure_ingested() auto-ingests source_file, polls until complete, caches ID; search_chunks() uses GET /search (not POST /qa); compute_hit_rate_5() uses context_hint[:80] substring match; LLM scoring optional via --model.
- Files changed: backend/tests/fixtures/full/notes_sample.txt (new), backend/tests/fixtures/full/conversation_sample.txt (new), evals/golden/book.jsonl, evals/golden/paper.jsonl, evals/golden/notes.jsonl, evals/golden/conversation.jsonl, evals/golden/code.jsonl, evals/run_eval.py, scripts/ralph/prd.json (S35 passes=True)
- Learnings:
  - context_hint[:80] substring match is more reliable than ground_truth[:50] — context_hint is verbatim from source; ground_truth may be rephrased
  - run_eval.py uses GET /search for retrieval quality (not POST /qa) — /search returns raw chunk texts; /qa adds LLM synthesis that obscures retrieval signal
  - manifest.json caches source_file → document_id to avoid re-ingesting on every eval run; must save incrementally after each new document
  - code.jsonl context_hints use literal code lines (e.g. MODEL_NAME = "BAAI/bge-m3") — exact substring matches after chunking
---

## 2026-02-25T12:00:00+00:00 - S34 - Full-document ingestion tests with real ML services
- Implemented: (1) backend/tests/fixtures/full/time_machine.txt (full ~185k chars from Project Gutenberg #35, not truncated). (2) backend/tests/fixtures/full/art_of_unix.txt (~109k chars, plain text converted from TAOUP HTML via latin-1 decode + HTML stripping across philosophy chapter, preface, multiple technical chapters). (3) backend/tests/test_integration_full.py (new) — @pytest.mark.slow; full_integration_db fixture uses real SQLite file in tmp_path (not in-memory), real LanceDB and Kuzu in tmp_path, does NOT inject mock EmbeddingService or EntityExtractor; still mocks LiteLLM classify via monkeypatch; _ingest_full() helper mirrors _ingest_fixture() from test_integration.py but with real ML; test_full_ingest_fiction asserts stage='complete', >=50 chunks, >=10 entities, >=3 search hits for 'time traveller'; test_full_ingest_technical asserts stage='complete', >=100 chunks, >=15 entities, >=3 search hits for 'Unix philosophy'. (4) backend/pyproject.toml [tool.pytest.ini_options]: added addopts="-m 'not slow'" (excludes slow tests from make test) and markers declaration. (5) Makefile: added test-full target (cd backend && uv run pytest tests/test_integration_full.py -v -m slow); added test-full to .PHONY. (6) README.md: added 'Full Integration Tests' section documenting make test-full, fixture files, model download behavior, and what each test asserts.
- Files changed: backend/tests/fixtures/full/time_machine.txt (new), backend/tests/fixtures/full/art_of_unix.txt (new), backend/tests/test_integration_full.py (new), backend/pyproject.toml (slow marker + addopts), Makefile (test-full target), README.md (Full Integration Tests section), scripts/ralph/prd.json (S34 passes=True)
- Learnings:
  - catb.org TAOUP HTML pages use latin-1 encoding — curl | python3 -c "sys.stdin.buffer.read().decode('latin-1')" avoids UnicodeDecodeError
  - pyproject.toml addopts = "-m 'not slow'" excludes @pytest.mark.slow from default pytest run; make test-full overrides with -m slow
  - Full integration test uses real SQLite FILE (not in-memory) because in-memory SQLite doesn't need LanceDB alignment, but real SQLite ensures the pipeline uses the same database as the search endpoint
  - 2 slow tests correctly deselected by pytest (260 passed, 2 deselected) — CI passes cleanly
---

## 2026-02-25T11:00:00+00:00 - S33 - Delete document UI, cascade cleanup, and seed data removal script
- Implemented: (1) backend/app/routers/documents.py: DELETE /documents/{id} — replaced Kuzu stub comment with actual lazy import of get_graph_service() + delete_document() call wrapped in try/except; same Kuzu cleanup added to POST /documents/bulk-delete loop. (2) backend/tests/test_documents.py: test_delete_document_calls_graph_service — patches app.services.graph.get_graph_service (lazy import site) and asserts delete_document(doc_id) called once. (3) scripts/seed_cleanup.py (new) — standalone Python 3.x script; argparse with --confirm (off by default = dry-run), --dry-run, --data-dir; finds documents whose title contains test/sample/seed/fixture/untitled (case-insensitive); dry-run prints list; --confirm cascade-deletes: SQLite FTS5 + child tables + document row, then tries lancedb.delete (graceful import error), then tries kuzu DELETE (graceful import error). (4) frontend/src/components/library/DocumentCard.tsx: added onDelete? prop; added confirmDelete state; added Trash2 icon in header (opacity-0 group-hover:opacity-100 — visible on card hover, hidden otherwise); added inline confirmation div at card bottom (cancel / delete buttons). (5) frontend/src/pages/Learning.tsx: added deleteDocument(id) API helper; added deleteDocumentMutation (useMutation, invalidates documents + documents-recent); added handleDeleteDocument handler; passed onDelete={!selectMode ? handleDeleteDocument : undefined} to both DocumentCard instances (recently-accessed grid + all-documents grid).
- Files changed: backend/app/routers/documents.py (Kuzu cleanup in delete + bulk-delete), backend/tests/test_documents.py (graph service test), scripts/seed_cleanup.py (new), frontend/src/components/library/DocumentCard.tsx (trash icon + confirmation), frontend/src/pages/Learning.tsx (deleteDocumentMutation + onDelete wiring), scripts/ralph/prd.json (S33 passes=True)
- Learnings:
  - Lazy import `from app.services.graph import get_graph_service` inside endpoint body: must patch at app.services.graph.get_graph_service (the source module), NOT at app.routers.documents.get_graph_service (which doesn't exist in the module namespace)
  - Tailwind group/group-hover pattern: add `group` class to Card, `opacity-0 group-hover:opacity-100` to trash button — visibility tied to card hover without JS
  - seed_cleanup.py uses sqlite3 stdlib (no SQLAlchemy) — no backend imports needed; lancedb and kuzu imports are optional (try/except ImportError)
  - argparse: --dry-run as default (store_true + default=True), --confirm overrides it — avoids need for mutually exclusive group
---

## 2026-02-25T10:00:00+00:00 - S32 - Integration tests using Project Gutenberg and an open technical book
- Implemented: backend/tests/fixtures/time_machine.txt (first 10000 chars downloaded from Project Gutenberg #35); backend/tests/fixtures/art_of_unix_ch1.txt (10k chars representative of TAOUP Ch1 content, covers all 17 Unix rules); backend/tests/test_integration.py (3 tests: test_ingest_fiction, test_ingest_technical, test_search_after_ingest). Integration fixture `integration_db` resets all 5 module-level singletons (DB engine, factory, lancedb, graph, retriever) and injects _MockEmbeddingService (returns [0.1]*1024 for any input) and _MockEntityExtractor (returns 1 synthetic CONCEPT entity per doc). LiteLLM mocked per-test in _ingest_fixture() helper via monkeypatch.setattr(litellm, "acompletion", AsyncMock). All 3 tests pass: stage='complete', ≥5 chunks in SQLite, ≥1 graph node in Kuzu, ≥1 search result via FTS5 keyword retrieval. README updated with integration test section under Running Tests. CI (259 tests) passes.
- Files changed: backend/tests/fixtures/time_machine.txt (new), backend/tests/fixtures/art_of_unix_ch1.txt (new), backend/tests/test_integration.py (new), README.md (integration test docs), scripts/ralph/prd.json (S32 passes=True)
- Learnings:
  - integration_db fixture must reset retriever_module._retriever = None in addition to lancedb/graph/embedder — HybridRetriever holds lazy refs to singletons; stale retriever gets wrong lancedb table path
  - _MockEmbeddingService returns [0.1]*1024 (not [0.0]) — cosine similarity of zero vector is undefined, LanceDB would error; [0.1]*1024 gives deterministic cos_sim=1.0 between query and all stored chunks
  - conftest.py session fixture resets singletons to None once; integration_db function fixture must also reset them per-test to handle the per-test tmp_path switching
  - FTS5 keyword search for "time machine" as two tokens works as AND search — "Time Machine" appears multiple times in the Project Gutenberg text
---

## 2026-02-25T09:00:00+00:00 - S31 - Fix LiteLLM/Ollama classify_node error crashing document ingestion
- Implemented: (1) backend/tests/conftest.py (new) — session-scoped autouse fixture `isolated_data_dir` sets DATA_DIR to tmp_path_factory temp dir before any test runs; clears get_settings.cache, resets _engine/_session_factory/_graph_service/_lancedb_service singletons to None at session start; removes DATA_DIR env var at teardown. (2) backend/app/main.py: Ollama startup health-check in lifespan — pings OLLAMA_URL/api/tags with 3s timeout; logs INFO if reachable, WARNING with URL if non-200 or unreachable (exception); ensures operators see early feedback before accepting uploads. (3) backend/tests/test_ingest.py: added test_classify_node_mocked_llm_returns_valid_type (mocks litellm.acompletion to return "paper", asserts classify_node returns valid content_type without network call) and test_classify_node_llm_unavailable_falls_back_to_heuristic (mocks to raise ConnectionError, asserts graceful fallback to "notes"). (4) README.md: added note in Running Tests section that conftest.py makes it safe to run pytest alongside a live dev backend.
- Files changed: backend/tests/conftest.py (new), backend/app/main.py (Ollama health check), backend/tests/test_ingest.py (2 new tests), README.md (test isolation note), scripts/ralph/prd.json (S31 passes=True)
- Learnings:
  - classify_node already used litellm.acompletion (async) with two levels of try/except — it never crashed ASGI; the real risk was the Kuzu file lock when pytest ran alongside dev backend
  - conftest.py session-scoped autouse fixture must use tmp_path_factory (not tmp_path) since tmp_path is only available in function-scoped fixtures
  - Resetting _engine/_session_factory to None in conftest.py is safe — individual test_db fixtures capture orig_engine=None and restore it correctly
  - monkeypatch.setattr(litellm, "acompletion", AsyncMock(...)) + resetting llm_module._llm_service = None forces the LLM service to use the monkeypatched function
---

## 2026-02-25T08:00:00+00:00 - S30 - Colorized dev log script for backend and frontend
- Implemented: scripts/dev-logs.sh (new) — starts backend (LOG_LEVEL=DEBUG) and frontend with colorized prefixed output; backend lines cyan [BACKEND], frontend lines green [FRONTEND]; uses awk with fflush() for real-time output without buffering; SIGINT/TERM forwarded to both child processes via trap _stop INT TERM; waits for clean shutdown. Makefile already had `logs: bash scripts/dev-logs.sh` target and `.PHONY: logs`. README already had the make logs Quickstart note from S29. Fixed original script's two bugs: (1) used stdbuf which is not available on macOS by default — replaced with awk fflush(); (2) ANSI codes were single-quoted shell variables that never expanded to real ESC sequences — embedded directly in awk string literals (\033[0;36m) which awk interprets correctly.
- Files changed: scripts/dev-logs.sh, scripts/ralph/prd.json (S30 passes=True)
- Learnings:
  - stdbuf is GNU coreutils — not available on macOS by default; use `awk '{ print p $0; fflush() }'` for real-time line prefixing on macOS
  - ANSI escape codes in awk: embed as \033[...m in awk string literals (BEGIN{p="..."}); do NOT pass via shell variables with single-quoted \033 (those are literal backslash-0-3-3, not ESC)
  - set -euo pipefail in a script with background processes: remove -e to avoid abort when a background pipeline exits non-zero; keep -uo
  - $! after a pipeline `cmd | awk ... &` captures awk's PID; killing awk closes the pipe causing cmd to get SIGPIPE and exit
---

## 2026-02-25T07:00:00+00:00 - S29 - README overhaul — quickstart, architecture overview, test and eval guide
- Implemented: README.md full rewrite — (1) product description (local-first, AI-powered knowledge and learning); (2) Architecture Overview with ASCII layer diagram (Types→Config→Repo→Service→Runtime→API) and tab list; (3) Tech Stack table (backend: Python 3.13, FastAPI, LangGraph, LanceDB, SQLite FTS5, Kuzu, GLiNER, LiteLLM; frontend: React 18, TypeScript 5, Vite 5, shadcn/ui, Sigma.js v3); (4) Quickstart with prerequisites (uv, Node 20+, Ollama + mistral), make backend / make frontend / make dev commands; (5) Running Tests (make test + tsc --noEmit); (6) Running Evaluations (docker-compose langfuse + cd evals && uv run python run_eval.py); (7) Monitoring (Monitoring tab + Phoenix localhost:6006 link). Also fixed pre-existing ruff E501 in backend/app/workflows/ingestion.py line 290.
- Files changed: README.md, backend/app/workflows/ingestion.py (line-length fix), scripts/ralph/prd.json (S29 passes=True)
- Learnings:
  - evals script is run_eval.py (not run_evals.py) — always check actual filenames before writing docs
  - ruff E501 in ingestion.py was pre-existing; CI caught it because it was the first line checked before the README story was blocked
---

## 2026-02-25T06:00:00+00:00 - S28 - Enhanced library catalog — tags, bulk actions, and pagination
- Implemented: backend/app/routers/documents.py: DocumentListResponse(items, total, page, page_size) Pydantic model; updated GET /documents to accept content_type (comma-sep), tag, sort (newest|oldest|alphabetical|most-studied|last_accessed), page, page_size query params; filtering/sorting done in Python after fetch; POST /documents/bulk-delete accepts {ids} and deletes each with full cascade; PATCH /documents/{id}/tags dedicated tags-only endpoint. backend/tests/test_documents.py: updated 6 existing tests to use resp.json()["items"] instead of direct list access; added test_list_documents_filter_by_content_type, test_list_documents_filter_by_tag, test_list_documents_pagination, test_bulk_delete_removes_docs, test_bulk_delete_skips_missing, test_patch_tags_endpoint, test_patch_tags_endpoint_404 (7 new tests). frontend/src/components/library/types.ts: added last_accessed to SortOption union; added DocumentListResponse interface. frontend/src/components/library/DocumentCard.tsx: full rewrite — tag chips (click to filter), inline tag editor (pencil icon → input, Enter to add, X to remove, Escape to close), checkbox for bulk select, selected state highlighted with border-primary. frontend/src/pages/Learning.tsx: full rewrite — server-side filtering via fetchDocuments(params); separate fetchRecentlyAccessed query (sort=last_accessed, page_size=5); patchTags useMutation (PATCH /documents/{id}/tags); bulkDeleteMutation (POST /documents/bulk-delete); Select mode toggle; per-card onSelect handler; handleSelectAll/handleDeselectAll; bulk action bar fixed at bottom with delete button; AlertDialog confirmation modal; pagination Prev/page/Next controls; active tag filter chip with dismiss.
- Files changed: backend/app/routers/documents.py, backend/tests/test_documents.py, frontend/src/components/library/types.ts, frontend/src/components/library/DocumentCard.tsx, frontend/src/pages/Learning.tsx, scripts/ralph/prd.json (S27+S28 passes=True)
- Learnings:
  - POST /documents/bulk-delete must be declared BEFORE /documents/{id} routes in FastAPI router (declaration order matters for path matching with prefix)
  - Wait, bulk-delete uses POST not GET, so no ambiguity; but PATCH /documents/{id}/tags after PATCH /documents/{id} works fine since FastAPI matches longest prefix
  - sort=last_accessed is internal-only (not shown in SortSelect UI dropdown) — used only by fetchRecentlyAccessed; adding to SortOption union satisfies TypeScript
  - useMutation onSuccess: void queryClient.invalidateQueries(...) — must use void to satisfy noFloatingPromises-style TSC checks
  - Two-query pattern: main list query (with filters) + separate recently-accessed query avoids needing all docs in memory
---

## 2026-02-25T05:00:00+00:00 - S27 - Code repository ingestion with tree-sitter and call graph
- Implemented: backend/app/services/code_parser.py (CodeParser — detect_language(), parse_file() extracts function/class definitions via tree-sitter v0.25 AST, build_call_edges() substring matching, is_code_file(), get_code_parser() singleton); supported languages: Python/JS/TS/Go/Rust. backend/app/services/graph.py: CALLS edge table added to Kuzu schema; add_call_edge(caller_id, callee_id, document_id) + get_call_graph(document_id) methods. backend/app/routers/graph.py: ?type=call_graph query param on GET /{document_id} routes to get_call_graph(). backend/app/workflows/ingestion.py: _chunk_code_file() helper (one chunk per function/class, fallback to RecursiveTextSplitter); updated chunk_node to branch on content_type=='code'; _build_call_graph() builds call edges in Kuzu; entity_extract_node calls _build_call_graph for code docs. frontend/src/pages/Viz.tsx: viewMode state (knowledge_graph|call_graph); Knowledge/Call Graph toggle in controls panel; fetchGraphData(viewMode) passes ?type param. backend/tests/test_code_parser.py: 22 tests — all passing. Also fixed pre-existing test_stats_current_streak flakiness by using started_at.date() instead of ended_at.date() for streak calculation (ended_at + 10min crossing midnight caused off-by-one).
- Files changed: backend/app/services/code_parser.py (new), backend/app/services/graph.py, backend/app/routers/graph.py, backend/app/workflows/ingestion.py, backend/tests/test_code_parser.py (new), backend/app/routers/study.py (streak fix), backend/pyproject.toml (tree-sitter deps), frontend/src/pages/Viz.tsx, scripts/ralph/prd.json (S27 passes=True)
---

## 2026-02-25T04:00:00+00:00 - S26 - Monitoring tab complete metrics dashboard
- Implemented: frontend/src/pages/Monitoring.tsx (full rewrite with 6 sections): (1) System Status row — StatusIndicator (green/red dot + Online/Offline label) for Ollama/Phoenix/Langfuse, ModelBadge for active model, fetches /settings/llm for Ollama status; (2) RAG Quality — Recharts BarChart grouped by dataset showing HR@5/MRR/Faithfulness/Answer Rel./Context Prec. with ReferenceLine at 0.65 and 0.9 (dashed); (3) Model Usage — PieChart (local vs cloud call ratio) + BarChart (calls by model), both using modelUsage state from GET /monitoring/model-usage; (4) Ingestion Queue — filters docs where stage != 'complete', renders amber stage badge; (5) Recent Traces (preserved from S24b); (6) Eval Runs table with HR@5 row coloring (preserved from S25b). backend/app/routers/monitoring.py: GET /monitoring/model-usage (groups qa_history by model_used via SQLAlchemy GROUP BY, returns [{model, call_count, avg_latency_ms:None}]). backend/tests/test_monitoring.py: 2 new tests — model_usage_aggregates_call_counts, model_usage_empty_when_no_qa_history.
- Files changed: backend/app/routers/monitoring.py (model-usage endpoint), backend/tests/test_monitoring.py (2 new tests), frontend/src/pages/Monitoring.tsx (full rewrite), scripts/ralph/prd.json (S26 passes=True)
- Learnings:
  - Recharts Tooltip formatter: value param can be `undefined` — type as `number | string | undefined` or TS fails with strict mode
  - Recharts PieChart label prop: `name` and `percent` in PieLabelRenderProps are optional — destructure with `?:` types or TS fails
  - GET /monitoring/model-usage groups by model_used using SQLAlchemy `func.count().label("call_count")` + `select(QAHistoryModel.model_used, func.count().label("call_count")).group_by(...)`
  - result.all() returns Row objects — access named columns as `row.model_used`, `row.call_count` (not dict)
---

## 2026-02-25T03:00:00+00:00 - S25b - RAGAS evaluation runner, eval_runs storage, and GET /monitoring/evals
- Implemented: evals/pyproject.toml (uv project with ragas + datasets); evals/run_eval.py (argparse CLI: --dataset, --model, --backend-url; loads JSONL from evals/golden/{dataset}.jsonl; POSTs to /qa per row collecting question/answer/contexts/ground_truths; computes hit_rate_5 and mrr from contexts; calls ragas.evaluate() for faithfulness/answer_relevancy/context_precision/context_recall with graceful fallback when LLM unavailable; POSTs to /monitoring/evals/store; prints table). backend/app/routers/monitoring.py: EvalRunCreate and EvalRunResponse Pydantic schemas; POST /monitoring/evals/store (creates EvalRunModel, returns 201 with id/run_at); GET /monitoring/evals (returns last 10 per dataset sorted by run_at desc). backend/tests/test_monitoring.py: 5 new tests. frontend/src/pages/Monitoring.tsx: EvalRun interface, fetchEvalRuns(), evalRuns state, Eval Runs table with HR@5/MRR/Faithfulness columns, row bg red if HR@5<0.5, green if >0.7.
- Files changed: evals/pyproject.toml (new), evals/run_eval.py (new), backend/app/routers/monitoring.py, backend/tests/test_monitoring.py, frontend/src/pages/Monitoring.tsx, scripts/ralph/prd.json (S25b passes=True)
- Learnings:
  - ragas.evaluate() requires HuggingFace Dataset.from_list(samples) — each sample must have question/answer/contexts/ground_truths keys
  - ragas column name is 'answer_relevancy' (not 'answer_relevance') — remap when storing to EvalRunModel which uses 'answer_relevance'
  - GET /monitoring/evals: fetch 50 rows ordered by run_at desc, then filter to 10 per dataset in Python — simpler than SQL windowing with SQLite
  - evals/ is a standalone uv project: uv init --name luminary-evals creates its own pyproject.toml + uv.lock, separate from backend/
---

## 2026-02-25T02:00:00+00:00 - S25a - Langfuse integration and golden dataset JSONL files
- Implemented: backend/app/services/llm.py: added _langfuse module-level singleton, _init_langfuse() (checks LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY, returns Langfuse client or None), get_langfuse() (lazy singleton getter), _log_to_langfuse() (calls client.start_generation() with model/input[:2000]/output/usage, calls gen.end(), swallows errors); generate() updated to call _log_to_langfuse after non-streaming completion. backend/tests/test_langfuse.py (new): 10 tests — init returns None when keys missing, creates client when keys set, handles init error gracefully, log noop when no client, log calls start_generation, log truncates prompt to 2000 chars, log swallows langfuse errors, generate calls langfuse when configured, generate skips langfuse when not configured, get_langfuse returns None by default. docker-compose.langfuse.yml (new): PostgreSQL 15 + Langfuse latest, port 3000. evals/golden/: 5 JSONL files (book, paper, conversation, notes, code), each with 10 synthetic Q&A examples with question/ground_truth_answer/document_id fields.
- Files changed: backend/app/services/llm.py, backend/tests/test_langfuse.py (new), docker-compose.langfuse.yml (new), evals/golden/book.jsonl (new), evals/golden/paper.jsonl (new), evals/golden/conversation.jsonl (new), evals/golden/notes.jsonl (new), evals/golden/code.jsonl (new), scripts/ralph/prd.json (S25a passes=True)
- Learnings:
  - langfuse v3.14 has no langfuse.decorators module — observe() is at langfuse.observe (module-level) but using manual client.start_generation() is cleaner for testing
  - langfuse v3 Langfuse.start_generation() returns a LangfuseGeneration handle; call handle.end() to finalize the span
  - patch.dict("sys.modules", {"langfuse": MagicMock(Langfuse=mock_cls)}) is the cleanest way to mock a library-level import inside a function that does `from langfuse import Langfuse`
  - Singleton reset pattern: set module._langfuse = None before test, restore after to avoid state leakage between tests
---

## 2026-02-25T01:00:00+00:00 - S24b - GET /monitoring/traces endpoint and Monitoring tab traces table
- Implemented: backend/app/routers/monitoring.py (already present from partial work): GET /monitoring/traces (returns [] when PHOENIX_ENABLED=False or Phoenix unreachable, proxies Phoenix /v1/spans when running); GET /monitoring/overview (total_documents, total_chunks, qa_calls_today, phoenix_running, langfuse_configured, llm_status). frontend/src/pages/Monitoring.tsx (already present): overview StatCards grid; Recent Traces table (clickable rows); TraceDetailPanel slide-in drawer with span details + JSON attributes. backend/app/main.py: monitoring_router already registered. backend/tests/test_monitoring.py (new): 8 tests — traces_phoenix_disabled, traces_phoenix_not_running, traces_phoenix_running_returns_spans, overview_document_and_chunk_counts, overview_qa_calls_today, overview_langfuse_configured_when_key_set, overview_phoenix_running_reflects_health_check, overview_empty_db_returns_zeros.
- Files changed: backend/tests/test_monitoring.py (new), scripts/ralph/prd.json (S24b passes=True)
- Learnings:
  - DocumentModel fields: title, format, content_type, stage, file_path (not filename/status/summary) — always check models.py before writing test helpers
  - ChunkModel has no embedding_model column — minimal required fields are id, document_id, text, chunk_index
  - ruff UP017 fires on timezone.utc — auto-fixed with `uv run ruff check . --fix`; use `datetime.UTC` in Python 3.11+
  - Monkeypatching httpx inside a router: patch the module-level function directly via monkeypatch.setattr(mon_module, "_check_phoenix_running", _async_fn)
---

## 2026-02-25T00:00:00+00:00 - S24a - Arize Phoenix — OTel instrumentation for Q&A, summarization, and retrieval
- Implemented: backend/app/telemetry.py: added trace_llm_call(operation, model, prompt_tokens, completion_tokens) → span "llm.{operation}", records model/tokens/latency_ms/error; trace_retrieval(operation, query) → "retrieval.{operation}", records query[:500]/latency_ms; trace_ingestion_node(node_name, state) → "ingestion.{node_name}", records document_id/content_type/latency_ms. backend/app/services/llm.py: wrapped non-streaming litellm.acompletion() in trace_llm_call; reads usage.prompt_tokens/completion_tokens from response. backend/app/services/retriever.py: wrapped HybridRetriever.retrieve() in trace_retrieval; sets chunk_count and top_score. backend/app/workflows/ingestion.py: wrapped all nodes (parse, classify, chunk, embed, keyword_index, entity_extract) with trace_ingestion_node. backend/app/main.py: FastAPIInstrumentor.instrument_app(app) and SQLAlchemyInstrumentor().instrument(engine=engine.sync_engine) in lifespan after setup_tracing. backend/tests/test_telemetry.py: 10 tests using InMemorySpanExporter via monkeypatching get_tracer().
- Files changed: backend/app/telemetry.py, backend/app/services/llm.py, backend/app/services/retriever.py, backend/app/workflows/ingestion.py, backend/app/main.py, backend/tests/test_telemetry.py (new), backend/pyproject.toml, scripts/ralph/prd.json (S24a passes=True)
- Learnings:
  - OTel global TracerProvider can only be set once per process — monkeypatch app.telemetry.get_tracer to return a tracer from InMemorySpanExporter-backed provider
  - ruff UP043: Generator[Span, None, None] → Generator[Span] in Python 3.13
  - SQLAlchemyInstrumentor requires engine.sync_engine (the underlying sync engine from async engine)
  - FastAPIInstrumentor.instrument_app() must be called AFTER setup_tracing() so global provider is set
---

## 2026-02-24T23:30:00+00:00 - S23b - Study tab progress dashboard — retention curve, heatmap, streak calendar
- Implemented: frontend/src/components/ProgressDashboard.tsx (new): fetchStats + fetchHistory API calls; SummaryCard sub-component (4 tiles: Cards Mastered, Avg Retention %, Current Streak, Study Time); buildRetentionCurveData (average e^(-day/stability)*100 over all_card_stabilities array, 0-30 days); Recharts LineChart with CartesianGrid, XAxis, YAxis, ReferenceLine at y=80 (dashed, "Target 80%"), Line with dot=false; mastery heatmap — flex-wrap grid of per_section_stability items with stabilityColor helper (red/amber/yellow/green CSS bg classes), hover tooltip via state; 90-day streak calendar — build calendarDays array, pad to Sunday alignment, render weeks as columns with streakCellColor helper (bg-secondary / bg-green-200/400/600). Study.tsx: import ProgressDashboard, replace placeholder section with <ProgressDashboard documentId={activeDocumentId} />
- Files changed: frontend/src/components/ProgressDashboard.tsx (new), frontend/src/pages/Study.tsx, scripts/ralph/prd.json (S23b passes=True)
- Learnings:
  - Recharts Tooltip formatter type requires `value: string | number | undefined` not just `number`; labelFormatter receives unknown so cast with `as number`
  - 90-day calendar: pad leading days by `firstDayOfWeek` null slots, then chunk into weeks of 7 — renders left-to-right week columns
  - npm install recharts adds ~350KB gzipped; chunk size warning is expected and acceptable
  - avg_retention from API is 0.0-1.0, multiply by 100 for display
---

## 2026-02-24T23:00:00+00:00 - S23a - Study stats backend — retention history and progress aggregation
- Implemented: backend/app/routers/study.py: added GET /study/stats/{document_id} (total_cards; cards_mastered = fsrs_state='review' AND fsrs_stability>10; avg_retention = mean(e^(-days_since_review/stability)) over reviewed cards; current_streak = consecutive days with completed session going back from today/yesterday; total_study_time_minutes = sum of session durations; per_section_stability via ChunkModel outerjoin SectionModel, grouped in Python; all_card_stabilities list); added GET /study/history (groups sessions by date, respects ?days= cutoff); added Pydantic schemas: SectionStabilityItem, CardStabilityItem, StudyStatsResponse, DailyHistoryItem; added math + date + timedelta imports. backend/tests/test_study_stats.py: 10 tests — test_stats_total_cards, test_stats_cards_mastered, test_stats_avg_retention, test_stats_current_streak, test_stats_total_study_time, test_stats_empty_document, test_stats_all_card_stabilities, test_history_daily_aggregation, test_history_respects_days_filter, test_history_empty
- Files changed: backend/app/routers/study.py (stats + history endpoints), backend/tests/test_study_stats.py (new), scripts/ralph/prd.json (S23a passes=True)
- Learnings:
  - avg_retention must skip cards with last_review=None or stability=0 to avoid math domain errors and division by zero
  - current_streak: start check from today, fall back to yesterday if no session today (allows sessions earlier today to count)
  - GET /study/history: filter by ended_at IS NOT NULL in the query to exclude in-progress sessions from time calculation
  - ruff E501 in test_learning.py (string literal) fixed by splitting into concatenated multi-line string
---

## 2026-02-24T22:00:00+00:00 - S22b - Study tab — Weak Areas panel and teach-back mode UI
- Implemented: frontend/src/pages/Study.tsx: added GapResult interface, fetchGaps(documentId) calling GET /study/gaps/{id}; WeakAreasPanel component renders sections sorted by fragility — fragility bar colored red (avg_stability<2), amber (2-5), green (>5), clicking a gap item calls setSelectedGapSection to pre-scope GeneratePanel; GeneratePanel updated with preselectedSection prop + useEffect to sync into internal state; Study page wires selectedGapSection and renders WeakAreasPanel below flashcard list. frontend/src/components/StudySession.tsx: TeachbackResult interface + submitTeachback() API; TeachbackPanel component (textarea → submit → score badge green≥80/amber60-79/red<60 + correct_points/missing_points/misconceptions sections + Next card button); mode toggle row (Flashcard | Teach-back); handleTeachbackNext (advance index, reset teachbackMode); studying view conditionally renders TeachbackPanel or flip card + rating buttons based on teachbackMode state
- Files changed: frontend/src/pages/Study.tsx, frontend/src/components/StudySession.tsx, scripts/ralph/prd.json (S22b passes=True)
- Learnings:
  - Framer Motion AnimatePresence inside a fragment ({...}) inside ternary works fine — just ensure the key prop is on the motion.div
  - Mode toggle (two adjacent buttons) can be done with border-border rounded-l / rounded-r divs without a separate library component
  - handleTeachbackNext: advance index + reset teachbackMode so next card starts in flashcard mode by default
  - GeneratePanel preselectedSection: sync via useEffect (not just initial state) so clicking different gap items updates the dropdown
---

## 2026-02-24T21:00:00+00:00 - S22a - Learning engine — gap detection, teach-back API, misconception tracking
- Implemented: backend/app/models.py: TeachbackResultModel (id, flashcard_id, user_explanation, score, correct_points, missing_points, misconceptions, created_at); backend/app/routers/study.py extended with: GET /study/gaps/{document_id} (loads weak cards: fsrs_stability<2.0 AND reps>1; joins FlashcardModel → ChunkModel outerjoin SectionModel to resolve section heading; groups in Python by heading; returns GapResult list sorted by avg_stability asc with sample_questions); POST /study/teachback (loads card, builds TEACHBACK_USER_TMPL prompt, calls LLM, parses JSON via _parse_teachback_response, stores TeachbackResultModel, if score<60 creates MisconceptionModel rows + calls _generate_correction_flashcard → correction FlashcardModel stored); tests/test_learning.py: 11 tests (_parse_teachback_response: valid JSON, strips fences, bad JSON fallback; GET /study/gaps: grouped weak cards, empty when none, sorted by stability; POST /study/teachback: returns score+feedback, score<60 creates misconception rows, score<60 creates correction flashcard, stores TeachbackResultModel, 404 missing card)
- Files changed: backend/app/models.py (TeachbackResultModel), backend/app/routers/study.py (gaps + teachback endpoints), backend/tests/test_learning.py (new), scripts/ralph/prd.json (S22a passes=True)
- Learnings:
  - Gap grouping in Python (not SQL GROUP BY) is cleaner when you also need to collect sample_questions per group — avoids SQLite GROUP_CONCAT limitations
  - ChunkModel outerjoin SectionModel via .outerjoin(SectionModel, ChunkModel.section_id == SectionModel.id) — returns (chunk, heading) tuples including None heading for unsectioned chunks
  - TeachbackResultModel (JSON columns for lists) created at startup via create_all_tables; no migration needed since SQLAlchemy Base.metadata.create_all is idempotent
  - reps > 1 (not > 0) for gap detection: cards seen at least twice — avoids gaps for first-review new cards
---

## 2026-02-24T20:00:00+00:00 - S21b - Study card UI — flashcard flip, rating buttons, progress bar, session complete
- Implemented: frontend/src/components/StudySession.tsx (StudySessionProps: documentId + onExit; useEffect init: Promise.all([startSession, fetchDueCards]) → sessionId + queue; FlashCard with rotateY animation via motion.div + perspective 1000px style on container + backfaceVisibility hidden on face divs; AnimatePresence mode="wait" + motion.div key={card.id} for x slide transitions; 4 rating buttons Again/Hard/Good/Easy; handleRate: submitReview → track reviewed/correct → advance or endSession → 'complete'; ProgressBar; SessionComplete screen with reviewed count, % correct, min due_date); frontend/src/pages/Study.tsx: studying state, StudySession import, StudySession replaces tab when studying=true, Start Studying button enabled
- Files changed: frontend/src/components/StudySession.tsx (new), frontend/src/pages/Study.tsx (studying state + wiring), scripts/ralph/prd.json (S21b passes=True)
- Learnings:
  - Framer Motion v12 AnimatePresence mode="wait": key on motion.div triggers exit+enter; exit plays before next child mounts
  - 3D flip: perspective on container div (style prop), transformStyle: "preserve-3d" on motion.div, backfaceVisibility: "hidden" on each face div, back face pre-rotated with transform: "rotateY(180deg)"
  - rotateY animation: animate={{ rotateY: showAnswer ? 180 : 0 }} on the wrapper motion.div
---

## 2026-02-24T19:00:00+00:00 - S21a - FSRS spaced repetition — service and study session endpoints
- Implemented: uv add fsrs (v6.3.0); app/models.py: ReviewEventModel (id, session_id, flashcard_id, rating, is_correct, reviewed_at); app/services/fsrs_service.py (FSRSService — _build_fsrs_card() reconstructs fsrs v6 Card via Card.from_dict using STATE_STR_TO_INT map; schedule() loads FlashcardModel, calls Scheduler().review_card(card, Rating.X, now), persists stability/difficulty/due_date/state manually since fsrs v6 Card has no reps/lapses; increments reps on every review, lapses only on 'again'; get_fsrs_service() singleton); app/routers/flashcards.py: ReviewRequest Pydantic model, POST /{card_id}/review endpoint (calls FSRSService.schedule, optionally stores ReviewEventModel with session_id, is_correct=rating!='again'); app/routers/study.py (GET /study/due: due_date<=now with optional document_id filter; POST /study/sessions/start: creates StudySessionModel row returns 201 SessionResponse; POST /study/sessions/{id}/end: counts ReviewEventModel rows for session, sets ended_at, returns SessionSummary); study_router registered in main.py; tests/test_fsrs.py: 15 tests (service: good sets stability>0+future due, again increments lapses, 3x again=3 lapses, ValueError for missing card, easy transitions state; GET /study/due: past-due vs future, document_id filter; POST /flashcards/{id}/review: 200 good, 200 again+lapses, 404 missing; session: start creates row, start with doc_id, end sets ended_at, end tallies 2 events, 404 missing session)
- Files changed: backend/app/models.py (ReviewEventModel), backend/app/services/fsrs_service.py (new), backend/app/routers/flashcards.py (ReviewRequest + review endpoint), backend/app/routers/study.py (new), backend/app/main.py (study_router), backend/tests/test_fsrs.py (new), scripts/ralph/prd.json (S21a passes=True)
- Learnings:
  - fsrs v6 uses Scheduler (not FSRS), sched.review_card(card, rating, now) returns (card, review_log) — not a dict keyed by rating
  - fsrs v6 Card has no .reps or .lapses — track manually in FlashcardModel; Card attrs: stability, difficulty, due, state, step, last_review, card_id
  - State enum: Learning=1, Review=2, Relearning=3 (no "New" state — new cards start at Learning)
  - Card.from_dict() requires card_id key — derive from abs(hash(uuid)) % 10^12 for deterministic reconstruction
  - ruff UP017: timezone.utc → datetime.UTC in Python 3.11+ (auto-fixed by ruff --fix)
---

## 2026-02-24T18:00:00+00:00 - S20b - Flashcard UI — Study tab generation, list, edit, delete, export
- Implemented: frontend/src/pages/Study.tsx full replacement — GeneratePanel (count select: 5/10/20/50; scope select: Full/By section; section heading select from GET /documents/{id}.sections; Generate button calls POST /flashcards/generate via useMutation, invalidates flashcard query, shows sonner toast); FlashcardCard (question text visible; Show/Hide answer chevron toggle; Pencil icon enters editing mode with textarea for question+answer + Save/Cancel; Trash icon shows inline confirmation with Delete/Cancel; FSRS state badge + edited marker + reps count in footer); Study page (useAppStore activeDocumentId gating: shows empty state when null; useQuery for GET /flashcards/{documentId}; useQuery for document sections; grid layout 1/2/3 cols; cardsLoading spinner; zero-state message; bottom bar with card count + Export CSV + Start Studying disabled button with S21b tooltip); Progress section placeholder for S23b
- Files changed: frontend/src/pages/Study.tsx (full replacement), scripts/ralph/prd.json (S20b passes=True)
- Learnings:
  - No shadcn Select/AlertDialog available — use plain <select> with Tailwind styling + inline confirmDelete state boolean per card (no modal library needed)
  - window.open(`/flashcards/${id}/export/csv`, "_blank") triggers CSV download without needing a hidden anchor trick
  - Disabled Start Studying button with title tooltip satisfies acceptance criteria while keeping placeholder for S21b
  - useMutation onSuccess: void queryClient.invalidateQueries(...) — must use void to satisfy noFloatingPromises-style TSC checks
---

## 2026-02-24T17:00:00+00:00 - S20a - Flashcard generation backend — CRUD, LLM generation, CSV export
- Implemented: app/services/flashcard.py (FlashcardService.generate(): fetches chunks via SQLAlchemy — all or filtered by section heading; builds combined text up to 32KB char limit; calls LiteLLM with JSON output prompt; strips markdown fences; parses JSON array; creates FlashcardModel rows with fsrs_state='new', due_date=now(), reps/lapses=0; commits and refreshes; extracted helpers _fetch_chunks, _build_text, _parse_llm_response to stay under PLR0915 limit); app/routers/flashcards.py (POST /flashcards/generate returns 201 list; GET /flashcards/{document_id}/export/csv StreamingResponse with csv module; GET /flashcards/{document_id} list ordered created_at desc; PUT /flashcards/{card_id} sets is_user_edited=True; DELETE /flashcards/{card_id} 204); registered flashcards_router in main.py; tests/test_flashcards.py: 11 tests (service: JSON parse, markdown fence strip, empty-on-no-chunks, prompt includes count; endpoints: generate 201, list, update sets user_edited, 404 on missing, delete 204, 404 on missing, CSV export)
- Files changed: backend/app/services/flashcard.py (new), backend/app/routers/flashcards.py (new), backend/tests/test_flashcards.py (new), backend/app/main.py (flashcards_router), scripts/ralph/prd.json (S20a passes=True)
- Learnings:
  - PLR0915 (too many statements >50) fires on long async methods — extract private helper functions (_fetch_chunks, _build_text, _parse_llm_response) to reduce statement count below limit
  - GET /{document_id}/export/csv must be declared BEFORE GET /{document_id} in FastAPI router to avoid path-matching ambiguity (FastAPI matches in declaration order)
  - test_db fixture from test_documents.py: monkeypatch DATA_DIR + cache_clear + in-memory engine + swap db_module._engine/_session_factory — cleanest pattern for isolated DB tests
  - async_sessionmaker factory() as session context manager used directly in tests — no need for dependency_overrides when using AsyncClient + ASGITransport
---

## 2026-02-25T03:00:00+10:00 - S19b - Notes UI — inline editor in Learning tab and standalone /notes route
- Implemented: (1) DocumentReader.tsx updated — per-section note icon (StickyNote from lucide-react) button toggles inline NoteEditor; notedSections Set<string> state tracks which sections have had notes saved (indicator appears immediately after save); NoteEditor: textarea + tag chips (Enter/comma to add, X to remove) + Save/Cancel; calls POST /notes; removed unused fetchNotesForDocument + Note interface + docNotes query since notes data model has no section_id linkback; (2) Notes.tsx standalone page — two-column layout, left sidebar 280px with All Notes/groups/tags filter tree (GET /notes/groups), right panel NoteCard grid (react-markdown preview, inline edit mode via useMutation, delete button); (3) App.tsx — added StickyNote icon import, /notes entry in NAV_ITEMS, Notes import, <Route path="/notes" element={<Notes />} /> in AppShell Routes
- Files changed: frontend/src/components/reader/DocumentReader.tsx, frontend/src/pages/Notes.tsx (already existed), frontend/src/App.tsx
- Learnings:
  - Notes data model has no section_id — indicator cannot persist across reloads without backend change; local notedSections Set state satisfies acceptance criteria for session lifetime
  - TS6133 noUnusedLocals fires on module-level async functions too when TypeScript build (tsc -b) is used — must remove unused module-level declarations, not just local vars
  - createNote return value not needed in NoteEditor — simplify to Promise<void> to avoid keeping unused Note interface
---

## 2026-02-24T16:00:00+00:00 - S19a - Notes backend — CRUD endpoints for notes with tags and groups
- Implemented: app/routers/notes.py with NoteCreateRequest/NoteUpdateRequest/NoteResponse Pydantic models; POST /notes (201); GET /notes/groups (SQLite JSON_EACH via text() for tag counts, func.count for group counts); GET /notes (optional document_id/group/tag filters, tag filter via EXISTS subquery with JSON_EACH bindparams); PUT /notes/{id} (partial update, updated_at refresh); DELETE /notes/{id} (204). Registered notes_router in main.py. Tests: 10 tests — create, filter, tag filter, update, delete, groups counts with UUID-suffixed unique group/tag names to avoid cross-test contamination.
- Files changed: backend/app/routers/notes.py (new), backend/tests/test_notes.py (new), backend/app/main.py (notes_router)
- Learnings:
  - SQLAlchemy text() can't be used as ORM join right side (TextClause has no .selectable) — use text("EXISTS (SELECT 1 FROM json_each...)").bindparams() directly in .where()
  - Shared test SQLite DB causes count test pollution — use UUID-suffixed group/tag names to isolate per-test assertions
  - json_each(notes.tags) in SQLite works when tags column stores a valid JSON array string
---
## 2026-02-24T15:00:00+00:00 - S18b - Learning tab — text selection toolbar, explanation sheet, glossary tab
- Implemented: (1) FloatingToolbar.tsx — mouseup listener on containerRef, window.getSelection() check inside setTimeout(10ms), getBoundingClientRect() for positioning (relative to container), 3 buttons (Explain/ELI5/Analogy), mousedown listener to clear toolbar, onMouseDown + preventDefault on buttons to prevent selection loss; (2) ExplanationSheet.tsx — fixed right panel (400px), dark backdrop overlay, SSE streaming via fetch POST /explain with AbortController cleanup, pre element with cursor blink; (3) GlossaryPanel inside DocumentReader — POST /glossary/{id}, loading state, searchable table with term/definition/page; SummaryPanel extended with allTabs array including Glossary tab; FloatingToolbar mounted inside section list div with relative positioning, handleExplain callback sets sheetText/sheetMode/sheetOpen.
- Files changed: frontend/src/components/FloatingToolbar.tsx (new), frontend/src/components/ExplanationSheet.tsx (new), frontend/src/components/reader/DocumentReader.tsx (Glossary tab + FloatingToolbar + ExplanationSheet integration)
- Learnings:
  - TypeScript closure narrowing: `const container = ref.current; if (!container) return` does NOT narrow inside inner functions/setTimeout callbacks — must capture as `nonNullContainer = container!` outside
  - useRef<HTMLDivElement>(null) paired with containerRef: React.RefObject<HTMLElement | null> — both typed correctly
  - onMouseDown + e.preventDefault() on toolbar buttons prevents mousedown from clearing the selection before the click handler fires
---
## 2026-02-24T14:00:00+00:00 - S18a - Backend /explain and /glossary endpoints
- Implemented: app/routers/explain.py (ExplainRequest model with mode: Literal["plain","eli5","analogy","formal"]; POST /explain SSE stream; POST /glossary/{document_id} JSON list); app/services/explain.py (ExplainService.stream_explain: retrieves 3 context chunks via HybridRetriever, builds context string, uses MODE_INSTRUCTIONS dict for per-mode system prompt suffix, streams LLM tokens as SSE data events, ends with done:true; ExplainService.extract_glossary: retrieves up to 50 chunks, truncates to 32KB, calls LLM with GLOSSARY_USER_TMPL, strips markdown fences, parses JSON array). Registered explain_router in main.py. Tests: 7 tests — all 4 mode instructions tested as distinct system prompts, token+done SSE events, glossary list return, markdown fence stripping, empty-on-no-chunks.
- Files changed: backend/app/routers/explain.py (new), backend/app/services/explain.py (new), backend/tests/test_explain.py (new), backend/app/main.py (explain_router)
- Learnings:
  - GLOSSARY_USER_TMPL uses str.format(text=...) — must escape literal JSON braces as {{ and }} in the template string
  - patch("app.routers.explain.get_explain_service") not dependency_overrides — router calls service directly, not via Depends
  - async generator tests: patch context managers + capturing_generate closure to intercept system prompt arg
---
## 2026-02-24T13:00:00+00:00 - S17b - Search UI — Cmd+K command dialog and persistent /search sidebar
- Implemented: (1) SearchDialog.tsx — full-screen overlay modal, Cmd+K/Ctrl+K shortcut via useEffect in AppShell (App.tsx refactored: Sidebar + Routes + SearchDialog wrapped in AppShell component with searchOpen state), 300ms debounce via useDebounce hook, results grouped by document, clicking result sets activeDocumentId + navigates to /; (2) SearchPanel in Learning.tsx — replaces catalog when search.trim() is non-empty, calls GET /search with 300ms debounce, content_type filter chips (book/paper/conversation/notes/code), grouped results with section/page/relevance%, no-results empty state; (3) hooks/useDebounce.ts — generic T debounce hook.
- Files changed: frontend/src/components/SearchDialog.tsx (new), frontend/src/hooks/useDebounce.ts (new), frontend/src/App.tsx (AppShell with Cmd+K), frontend/src/pages/Learning.tsx (SearchPanel + search mode)
- Learnings:
  - ContentType union must match types.ts exactly: "book" | "paper" | "conversation" | "notes" | "code"
  - AppShell pattern: wrap Routes + global dialogs in a child of BrowserRouter (not inside App) to use useNavigate
  - fetchSearch returns Promise<DocumentGroup[]> from { results: [] } shape
---
## 2026-02-24T12:00:00+00:00 - S17a - GET /search backend — hybrid cross-document search endpoint
- Implemented: backend/app/routers/search.py with SearchResult/DocumentGroup/SearchResponse Pydantic models; GET /search endpoint (q, content_types, limit params); resolves content_types to document_ids via SQLite; calls HybridRetriever.retrieve(); groups ScoredChunk results by document_id; returns grouped SearchResponse. Registered search_router in main.py. Tests: tests/test_search.py with 6 tests using dependency_overrides for get_db (async generator override) and get_retriever (lambda override); covers grouped results, empty results, content_type filter, no-match filter early-return, missing query 422, result field shapes.
- Files changed: backend/app/routers/search.py (new), backend/tests/test_search.py (new), backend/app/main.py (search_router import + include)
- Learnings:
  - FastAPI dependency_overrides for yield deps: provide async def _override(): yield value — not a lambda returning an async generator
  - dependency_overrides[get_retriever] = lambda: retriever works for simple (non-yield) deps
  - ruff --fix handles F401 unused imports and I001 import sorting; E501 long docstrings need manual split
---
## 2026-02-25T02:00:00+10:00 - S16b - Viz tab — entity type filter, node search, click popover, edge tooltip
- Implemented: Enhanced src/pages/Viz.tsx with full interactive controls — controls sidebar (240px): scope toggle (This doc/All docs, invalidates query), search input (uses setSettings nodeReducer to dim non-matches + camera.animate to first match), entity type checkboxes (filter rebuilds filteredGraph via useMemo); GraphController inner component (useRegisterEvents returns a fn to call with handlers: clickNode computes graphToViewport coords from graph attrs + container getBoundingClientRect, enterEdge reads edge "type" attr for tooltip, leaveEdge/clickStage clear selection); CameraControls inner component (useCamera hook: zoomIn/zoomOut/reset); node click popover (fixed position from screenX/Y); edge hover tooltip; all type-safe with SigmaNodeEventPayload/SigmaEdgeEventPayload imports
- Files changed: frontend/src/pages/Viz.tsx
- Learnings:
  - useRegisterEvents() returns a registerEvents function to call inside useEffect — not used directly as JSX event prop
  - graphToViewport() accepts {x, y} Coordinates and returns viewport pixel coordinates — must read node attrs as numbers
  - nodeReducer prefix unused param with _ to satisfy noUnusedLocals: (_nodeKey, data) => ...
  - SigmaNodeEventPayload / SigmaEdgeEventPayload imported from "sigma/types" — both have event.preventSigmaDefault()
---

## 2026-02-25T01:00:00+10:00 - S16a - Viz tab — basic Sigma.js knowledge graph rendering
- Implemented: npm install sigma graphology @react-sigma/core graphology-layout-forceatlas2; src/pages/Viz.tsx (TanStack Query fetches GET /graph/{documentId} or GET /graph when activeDocumentId is null; buildGraphologyGraph() converts API nodes to Graphology nodes with random x/y, linear size scaling 4-20, TYPE_COLORS palette, adds edges with deduplication check; forceAtlas2.assign() 100 iterations with inferSettings(); SigmaContainer renders the result; loading/error/empty states)
- Files changed: frontend/src/pages/Viz.tsx, frontend/package.json (sigma, graphology, @react-sigma/core, graphology-layout-forceatlas2)
- Learnings:
  - forceAtlas2.assign(g, {iterations: 100, settings: forceAtlas2.inferSettings(g)}) works for synchronous layout (not syncLayout())
  - Graphology deduplication: check g.hasEdge(source, target) before addEdge to avoid MultiGraph issues
  - SigmaContainer from @react-sigma/core accepts graph prop directly; no need for custom sigma instance hook for basic rendering
  - Bundle size warning: Sigma.js + Graphology adds ~175KB gzipped — acceptable for a WebGL graph component
---

## 2026-02-25T00:00:00+10:00 - S15b - GLiNER entity extraction — NER pipeline wired into ingestion workflow
- Implemented: uv add gliner; app/services/ner.py (EntityExtractor — lazy-loads GLiNER 'urchade/gliner_multi_pii-v1' to DATA_DIR/models/gliner/; extract() batches over chunks, calls model.predict_entities() with threshold=0.5, normalizes entity name to lowercase, generates deterministic UUID5 per doc+name, skips empty chunks); entity_extract_node in ingestion.py (replaces stub: calls EntityExtractor.extract(), upserts Document node to Kuzu, upserts entities + MENTIONED_IN edges, calculates CO_OCCURS edges via itertools.combinations for chunk-level co-occurrence; non-fatal exception handling); tests/test_ner.py (6 unit tests with mocked GLiNER model + 1 skipped integration test)
- Files changed: backend/app/services/ner.py (new), backend/app/workflows/ingestion.py, backend/tests/test_ner.py (new), backend/pyproject.toml (gliner dependency)
- Learnings:
  - GLiNER predict_entities() returns list of {text, label, score} dicts — use threshold=0.5 to filter low-confidence predictions
  - uuid.uuid5(NAMESPACE_DNS, f"{doc_id}:{name}") creates deterministic entity IDs for deduplication across chunks
  - Lazy imports inside entity_extract_node with # noqa: PLC0415 comments avoid circular imports and defer heavy imports to runtime
  - itertools.combinations(chunk_ent_ids, 2) generates all entity pairs for co-occurrence edges
---

## 2026-02-24T23:00:00+10:00 - S15a - Kuzu graph database — schema setup and GET /graph endpoints
- Implemented: uv add kuzu; app/services/graph.py (KuzuService — _create_schema creates Entity/Document node tables and MENTIONED_IN/CO_OCCURS/RELATED_TO edge tables; upsert_entity checks existence and increments frequency; upsert_document; add_mention with increment; add_co_occurrence with increment; delete_document deletes MENTIONED_IN edges then Document node; get_graph_for_document returns nodes+edges; get_graph_for_documents with IN clause for multi-doc; _get_co_occurrence_edges helper); app/routers/graph.py (GET /graph/{document_id}, GET /graph?doc_ids=); app/main.py (import graph_router + get_graph_service, initialize in lifespan, include router); tests/test_graph.py (13 tests: 8 unit tests for KuzuService, 5 endpoint integration tests)
- Files changed: backend/app/services/graph.py (new), backend/app/routers/graph.py (new), backend/app/main.py, backend/tests/test_graph.py (new), backend/pyproject.toml (kuzu dependency)
- Learnings:
  - Kuzu has_next()/get_next() pattern for iterating query results — no pandas needed for simple queries
  - Kuzu CREATE OR REPLACE not available — use MATCH then SET for upsert pattern, or CREATE if not exists
  - CO_OCCURS edge parameters: Kuzu requires params dict in execute(); use WHERE r.document_id = $did to filter by edge property
  - monkeypatch.setattr(graph_module, "_graph_service", svc) correctly patches the singleton for TestClient tests
---

## 2026-02-24T22:00:00+10:00 - S_ARCH_b - Doc-gardener script and QUALITY_SCORE domain grading
- Implemented: scripts/ralph/doc_gardener.py (checks QUALITY_SCORE.md, tech-debt-tracker.md, ARCHITECTURE.md using git log HEAD~10..HEAD -- <file>; prints STALE DOC warning if file not in last 10 commits; exits 0 always); docs/QUALITY_SCORE.md (updated with Last Updated header 2026-02-24, Phase 1 domains marked: Ingestion/Summarization/Q&A = implemented/yes/partial/B; remaining domains = F)
- Files changed: scripts/ralph/doc_gardener.py (new), docs/QUALITY_SCORE.md
- Learnings:
  - git log --oneline -N -- <file> returns last N commits that TOUCHED the file, NOT last N overall commits — wrong for staleness check
  - Correct staleness check: git log --oneline HEAD~10..HEAD -- <file> returns empty if file not changed in last 10 commits
  - Fallback to full log if HEAD~10 doesn't exist (shallow repo with fewer than 10 commits)
---

## 2026-02-24T21:00:00+10:00 - S_ARCH_a - Layer linter, boundary validation, and CI Makefile
- Implemented: backend/tools/layer_linter.py (AST-based layer enforcement — LAYER_ORDER dict maps types/config/database/models=0-1, repo=2, service/workflow=3, router=4, main=5; get_layer() maps filepath to layer via path segment; check_file() emits violations when source_layer < target_layer; exits 1 on violations, 0 on clean); backend/tools/boundary_checker.py (FastAPI route boundary checker — detects route decorator, builds defaults map to identify Depends() args, flags raw dict/Any annotations; exits 0 advisory-only); backend/pyproject.toml (added PL to ruff select with ignore list for PLC0415/PLR0911/PLR0912/PLR0913/PLR2004/PLW0603; per-file-ignores for tests/** and tools/**); Makefile updated (ci target adds uv sync + layer_linter + boundary_checker); .github/workflows/ci.yml (runs make ci on push/PR)
- Files changed: backend/tools/layer_linter.py (new), backend/tools/boundary_checker.py (new), backend/tools/__init__.py (new), backend/pyproject.toml, Makefile, .github/workflows/ci.yml (new)
- Learnings:
  - Layer linter maps subdirs via SUBDIR_MAP ('services'->'service') so get_layer works for both root files (stem) and subdir files (first dir part)
  - Violation check: source_layer < target_layer means a lower layer importing from a higher layer (types importing service)
  - boundary_checker: build_defaults_map aligns args.defaults to last N of args.args using offset i - (n_args - n_defaults)
  - PL rules that fire on legitimate FastAPI code: PLR0913 (many args), PLR2004 (HTTP codes), PLW0603 (lazy singleton), PLC0415 (lazy import in embedder)
---

## 2026-02-24T20:00:00+10:00 - S14 - LLM mode indicator badge and Settings drawer
- Implemented: frontend/src/components/SettingsDrawer.tsx (LLMModeBadge component with colored dot indicator — green=local, blue=cloud, red=unavailable — refetches every 30s via TanStack Query, shown in sidebar bottom; SettingsDrawer right-side panel 400px — Section 1: LLM Mode with Local/Cloud radio, local sub-section lists available Ollama models + Pull model input that streams SSE output, cloud sub-section tabs OpenAI/Anthropic/Google each with masked password input + Save button that calls PATCH /settings; Section 2: Storage shows DATA_DIR + raw/vectors/models MB from GET /settings/storage; Section 3: Privacy notice); App.tsx updated to import LLMModeBadge + SettingsDrawer; backend main.py: PATCH /settings (upserts to SQLite settings table with TODO keychain comment), POST /settings/ollama/pull (asyncio subprocess streams ollama pull output as SSE), GET /settings/storage (returns data_dir + dir sizes in MB)
- Files changed: frontend/src/components/SettingsDrawer.tsx (new), frontend/src/App.tsx, backend/app/main.py
- Learnings:
  - Sidebar bottom slot: use mt-auto inside flex-col to push badge to bottom of nav
  - SSE streaming from asyncio subprocess: async for line in proc.stdout yields bytes; decode + rstrip + yield as "data: ...\n\n"
  - PATCH /settings with dict[str, str] body: Pydantic validates the body; session.merge(SettingsModel) upserts by primary key
  - pullScrollRef.current?.scrollTo({ top: scrollHeight }) in setTimeout(0) ensures DOM update completes before scrolling
---

## 2026-02-24T19:00:00+10:00 - S13 - Chat tab — streaming Q&A with citation chips and confidence badges
- Implemented: src/pages/Chat.tsx full replacement — scope selector (This document / All my content radio); model selector (fetches GET /settings/llm, populates from available_local_models + available cloud_providers); message list (user right-aligned slate-100, assistant left-aligned white card); SSE stream consumed via fetch+ReadableStream, token appended in real-time, done event parses citations+confidence; citation chips rendered as muted badge spans with document_title > section_heading (p.N); confidence Badge using green/blue/gray variants; NOT_FOUND shows blue info text; empty state with 3 example question chips; auto-resize textarea; Enter to send, Shift+Enter for newline; error message on fetch failure
- Files changed: frontend/src/pages/Chat.tsx
- Learnings:
  - crypto.randomUUID() available in modern browsers for generating message IDs without a library
  - assistantId must be captured before async function to close over the correct ID in setMessages callbacks
  - useEffect for scrollIntoView on messages array change handles auto-scroll to bottom
  - sendMessage is an async function called with void operator from JSX: void sendMessage(input)
---

## 2026-02-24T18:00:00+10:00 - S12 - Document reader UI with streaming summary panel in Learning tab
- Implemented: added preview: Mapped[str] column to SectionModel; updated chunk_node in ingestion.py to create SectionModel rows with preview (first 300 chars of section text); added GET /documents/{id} endpoint returning DocumentDetail with SectionItem list; frontend src/components/reader/types.ts (DocumentDetail, SectionItem, SummaryMode, SUMMARY_TABS, CONVERSATION_TAB); src/components/reader/DocumentReader.tsx (two-panel layout: left 60% with section list using line-clamp-2 for previews, right 40% with SummaryPanel; SummaryPanel with tab set, Generate/Regenerate buttons, SSE stream consumed via fetch+ReadableStream, animated cursor during streaming, 4th Meeting Notes tab for conversation content type); updated Learning.tsx to check activeDocumentId and show DocumentReader instead of catalog
- Files changed: backend/app/models.py (SectionModel.preview), backend/app/workflows/ingestion.py (SectionModel storage), backend/app/routers/documents.py (DocumentDetail, SectionItem, GET /documents/{id}), frontend/src/components/reader/types.ts, DocumentReader.tsx, frontend/src/pages/Learning.tsx
- Learnings:
  - SSE stream parsing: split buffer on '\n', buffer = lines.pop() to handle partial lines correctly
  - fetchDocument queryFn type: need to return Promise<DocumentDetail> explicitly (res.json() returns Promise<unknown>)
  - SummaryMode as key in Partial<Record<SummaryMode, string>> needs safe access with ?? '' to avoid undefined spread issues
  - Back button clears activeDocumentId with setActiveDocument(null) — the catalog renders when activeDocumentId is null
---

## 2026-02-24T17:00:00+10:00 - S11 - Upload flow — file drag-and-drop and ingestion progress toast
- Implemented: src/components/library/UploadDialog.tsx (Dialog with Upload File + Paste Text tabs; drag-and-drop with dragover/dragleave/drop events; file picker; Paste text with label + content_type selector; calls POST /ingest as multipart FormData for both paths; pollStatus() uses setInterval to poll GET /documents/{id}/status every 2s, updates sonner toast loading message per stage, dismisses with success or error); installed sonner, added <Toaster /> to App.tsx; backend status endpoint updated to include error_message: str|None
- Files changed: frontend/src/components/library/UploadDialog.tsx, frontend/src/pages/Learning.tsx (Add Content button), frontend/src/App.tsx (Toaster), backend/app/routers/documents.py (error_message in status response), frontend/package.json (sonner)
- Learnings:
  - sonner@2 peer-dep issue with React 19 — use --legacy-peer-deps; works fine at runtime
  - Paste text: create a File from a Blob and post via FormData — same POST /ingest endpoint handles both
  - pollStatus cleanup: clearInterval must be called before toast.success/toast.error to prevent racing
  - void operator needed in JSX onClick for async handlers: onClick={() => void handleSubmit()} to satisfy noFloatingPromises-style TSC rules
---

## 2026-02-24T16:00:00+10:00 - S10b - Library catalog UI — document grid, search, filter, sort in Learning tab
- Implemented: src/components/ui/card.tsx (Tailwind-only Card primitive), src/components/ui/badge.tsx (Badge with gray/blue/indigo/green variants), src/components/library/types.ts (DocumentListItem, ContentType, LearningStatus, SortOption, ViewMode types), src/components/library/utils.ts (CONTENT_TYPE_ICONS, STATUS_LABELS, STATUS_VARIANTS, formatWordCount, relativeDate), src/components/library/SearchBar.tsx, ViewToggle.tsx, FilterBar.tsx, SortSelect.tsx, DocumentCard.tsx, DocumentRow.tsx; updated src/pages/Learning.tsx (TanStack Query fetch, client-side filter+sort, recently accessed section, loading skeleton with animate-pulse, empty state with CTA)
- Files changed: frontend/src/components/ui/card.tsx, badge.tsx, frontend/src/components/library/types.ts, utils.ts, SearchBar.tsx, ViewToggle.tsx, FilterBar.tsx, SortSelect.tsx, DocumentCard.tsx, DocumentRow.tsx, frontend/src/pages/Learning.tsx
- Learnings:
  - No Radix UI in this project (shadcn init ran but no components added) — build Card/Badge as plain Tailwind components
  - TypeScript strict mode: noUnusedLocals + noUnusedParameters enforced — every import and param must be used
  - TanStack Query v5: queryFn must return Promise<T>; cast res.json() explicitly to avoid unknown type
  - LEARNING_STATUS_ORDER map uses Record<string, number> to avoid indexing issues with learning_status in sort comparator
  - applyFiltersAndSort sorts a spread copy ([...result]) to avoid mutating the original array
---

## 2026-02-24T15:00:00+10:00 - S10a - GET /documents, PATCH, and DELETE endpoints with derived fields
- Implemented: GET /documents with DocumentListItem Pydantic model (correlated scalar subqueries for summary_one_sentence, flashcard_count, summary_count, study_session_count); _derive_learning_status() helper (studied > flashcards_generated > summarized > not_started); PATCH /documents/{id} with PatchDocumentRequest(title, tags); DELETE /documents/{id} manually deleting child rows from all related tables + FTS5 virtual table + LanceDB; delete_document() method on LanceDBService; tags column added to DocumentModel; PRAGMA foreign_keys = ON in db_init; tests/test_documents.py (18 tests: 4 unit tests for _derive_learning_status, 7 GET endpoint tests, 3 PATCH tests, 4 DELETE tests)
- Files changed: backend/app/routers/documents.py, backend/app/services/vector_store.py, backend/app/models.py, backend/app/db_init.py, backend/tests/test_documents.py
- Learnings:
  - SQLAlchemy correlated scalar subqueries: use .correlate(ParentModel).scalar_subquery() for efficient single-query derived field computation
  - LanceDB table.delete() takes a SQL WHERE clause string like "document_id = 'abc'" (no parameterization API available)
  - FTS5 virtual table deletion requires raw SQL text() since it's not an ORM model: DELETE FROM chunks_fts WHERE document_id = :id
  - SQLAlchemy delete() import: from sqlalchemy import delete (not session.delete which is for ORM objects); use delete(Model).where(...) for bulk deletes
  - GET "" route (empty string path) on router with prefix="/documents" correctly maps to GET /documents
---

## 2026-02-24T14:00:00+10:00 - S09 - Grounded Q&A with citations, confidence, and Phoenix tracing
- Implemented: app/telemetry.py (setup_tracing — launches Phoenix in background thread, registers OTLPSpanExporter, get_tracer returns opentelemetry tracer), app/services/qa.py (QAService — stream_answer async generator, _build_context, _split_response, _store_qa, _fetch_doc_titles; NOT_FOUND_IN_CONTENT handling; traces retrieve+generate spans with chunk_count/model_name attributes), app/routers/qa.py (POST /qa, QARequest Pydantic model with question/document_ids/scope/model), registered qa_router + called setup_tracing in lifespan in main.py, tests/test_qa.py (16 tests: 4 _split_response unit, 2 _build_context unit, 2 prompt unit, 3 stream_answer service tests, 2 NOT_FOUND tests, 3 endpoint integration tests)
- Files changed: backend/app/telemetry.py, backend/app/services/qa.py, backend/app/routers/qa.py, backend/app/main.py, backend/tests/test_qa.py, backend/pyproject.toml (added arize-phoenix, opentelemetry-sdk, opentelemetry-exporter-otlp-proto-grpc)
- Learnings:
  - For RAG responses with embedded JSON (citations at end), collect full LLM response first, then parse citations with rfind('{"citations":'), then stream answer words as fake tokens — cleaner than detecting JSON boundary mid-stream
  - opentelemetry tracer.start_as_current_span() is a sync context manager, works fine with await inside the with block
  - arize-phoenix can be imported with try/except ImportError for graceful degradation when not installed
  - OTLPSpanExporter(endpoint=..., insecure=True) avoids TLS handshake issues when Phoenix runs locally
---

## 2026-02-24T13:00:00+10:00 - S08 - Multi-granularity summarization with SSE streaming
- Implemented: app/services/summarizer.py (SummarizationService — stream_summary async generator, _fetch_chunks, _store_summary, _map_sections map-reduce for docs >8000 tokens), app/routers/summarize.py (POST /summarize/{document_id}, SummarizeRequest Pydantic model, StreamingResponse with text/event-stream), registered summarize_router in app/main.py, tests/test_summarizer.py (15 tests: 6 prompt construction unit tests, 5 service unit tests, 4 endpoint integration tests)
- Files changed: backend/app/services/summarizer.py, backend/app/routers/summarize.py, backend/app/main.py, backend/tests/test_summarizer.py, backend/pyproject.toml (added sse-starlette)
- Learnings:
  - UP043 ruff rule: AsyncGenerator[str, None] → AsyncGenerator[str] (None is default send type, same pattern as UP043 on AsyncGenerator)
  - sse-starlette not needed for raw SSE — StreamingResponse with media_type="text/event-stream" and manually formatted "data: ...\n\n" strings works cleanly
  - Async generator methods: call without await in the router (svc.stream_summary(...) returns AsyncGenerator directly)
  - map-reduce pattern: non-streaming calls for map phase (section summaries), streaming for reduce phase (final output)
  - Import sort order: constants from same module must be alphabetically ordered in the import block (GROUNDING_PREFIX before MAP_TOKEN_THRESHOLD)
---

## 2026-02-24T12:00:00+10:00 - S07 - LiteLLM integration — Ollama, OpenAI, Anthropic, Gemini with streaming
- Implemented: app/services/llm.py (LLMService — generate non-stream/stream, _build_kwargs routing, _token_stream async generator, get_llm_service singleton), enhanced classify_node in ingestion.py (LLM reclassification when heuristic='notes' and word_count>5000), GET /settings/llm endpoint in main.py (Ollama ping, processing_mode logic, cloud_providers list), tests/test_llm.py (13 tests: 3 non-stream, 2 stream, 4 routing, 3 endpoint, 1 singleton)
- Files changed: backend/app/services/llm.py, backend/app/workflows/ingestion.py, backend/app/main.py, backend/tests/test_llm.py, backend/pyproject.toml (added litellm, httpx)
- Learnings:
  - AsyncGenerator patches must stay active during iteration — the generator body executes lazily; if patch exits before iteration, real network calls fire
  - litellm.acompletion patched at "litellm.acompletion" works for both stream and non-stream when called from app/services/llm.py (import litellm; litellm.acompletion)
  - UP043 ruff rule: AsyncGenerator[str, None] → AsyncGenerator[str] (None is default send type)
  - classify_node changed from sync def to async def to support await get_llm_service().generate()
---

## 2026-02-24T11:00:00+10:00 - S06 - SQLite FTS5 keyword index and hybrid retrieval with RRF
- Implemented: app/services/retriever.py (HybridRetriever — vector_search, keyword_search, rrf_merge, retrieve), keyword_index_node live in ingestion.py (replaces stub), ScoredChunk dataclass added to app/types.py, tests/test_retriever.py (12 tests: 6 rrf_merge unit tests, 3 keyword_search integration tests, 1 keyword_index_node integration test, 2 vector_search mocked unit tests)
- Files changed: backend/app/services/retriever.py, backend/app/workflows/ingestion.py, backend/app/types.py, backend/app/db_init.py, backend/app/routers/documents.py, backend/app/workflows/__init__.py, backend/tests/test_retriever.py
- Learnings:
  - FTS5 keyword_search: ORDER BY bm25(chunks_fts) returns NEGATIVE scores (more negative = better match) — correct, do not flip sign
  - LanceDB vector_search: score = 1.0 - _distance (cosine distance → cosine similarity)
  - RRF k=60 produces all-positive scores regardless of BM25 sign — safe to compare across sources
  - vector_search is synchronous (LanceDB to_list() is blocking); keyword_search is async (SQLAlchemy async session)
  - db_init.py FTS5 DDL: content/content_rowid columns removed — use simple fts5 schema (text, chunk_id UNINDEXED, document_id UNINDEXED) with manual INSERT SELECT
---

# Ralph Progress Log
Started: Tue 24 Feb 2026 08:01:32 AEST
---

## 2026-02-24T10:00:00+10:00 - S05 - BGE-M3 embedding pipeline with LanceDB vector storage
- Implemented: app/services/embedder.py (EmbeddingService — ORT first, ST fallback, lazy load, BATCH_SIZE=32), app/services/vector_store.py (LanceDBService — chunk_vectors table, upsert via merge_insert), updated embed_node in ingestion.py (replaces stub), tests/test_embedder.py (3 unit tests + 1 skipped integration test)
- Files changed: backend/app/services/embedder.py, backend/app/services/vector_store.py, backend/app/workflows/ingestion.py, backend/tests/test_embedder.py, backend/pyproject.toml (added sentence-transformers, lancedb, optimum[onnxruntime], numpy)
- Learnings:
  - LanceDB 0.29.2: use list_tables().tables (not table_names()) — table_names() is deprecated
  - pa.list_(pa.float32(), 1024) creates a fixed_size_list PyArrow type, which LanceDB accepts for vector columns
  - merge_insert("chunk_id").when_matched_update_all().when_not_matched_insert_all().execute(rows) works for upsert in LanceDB 0.29
  - ORTModelForFeatureExtraction.from_pretrained(export=True) + return_tensors="np" → last_hidden_state is numpy; mean pool + normalize manually
  - Integration test skipped with pytest.mark.skipif when model not cached; unit tests (_mean_pool, _normalize, instantiation) always run
---

## Codebase Patterns
- uv commands must be run from /backend directory (cd backend && uv ...)
- pytest exit code 5 = no tests collected (acceptable at scaffold stage, real tests added per story)
- Settings singleton via @functools.lru_cache on get_settings()
- FastAPI app uses lifespan context manager for startup/shutdown (not deprecated on_event)
- All route handlers use Depends(get_settings) — never import settings directly in handlers
- uv init creates a stub backend/main.py — delete it, app entrypoint lives at backend/app/main.py

---

## 2026-02-24T09:10:00+10:00 - S04 - LangGraph ingestion workflow — parse, classify, chunk nodes + POST /ingest
- Implemented: tests/test_ingest.py — 12 tests covering classification heuristics, chunk_node DB writes, POST /ingest response, GET status endpoint, 404 handling
- Files changed: backend/tests/test_ingest.py
- Learnings:
  - Workflow nodes (ingestion.py) and router (documents.py) were already implemented; only the test file was missing
  - To isolate DB in async tests: patch db_module._engine and db_module._session_factory with in-memory engine/factory; also monkeypatch DATA_DIR env and call get_settings.cache_clear()
  - Background tasks created by asyncio.create_task() inside request handlers use the patched singletons correctly
  - All async tests in the test file that use the app via ASGITransport need the test_db fixture even if they only test 4xx responses
---

## 2026-02-24T08:30:00+10:00 - S00a - Backend monorepo scaffold — uv, Python 3.13, FastAPI app skeleton
- Implemented: .gitignore, README.md, Makefile, evals/README.md, backend/ with uv project, app/config.py (Settings + get_settings), app/main.py (FastAPI with /health + /settings), tests/test_config.py
- Files changed: .gitignore, README.md, Makefile, evals/README.md, backend/pyproject.toml, backend/app/__init__.py, backend/app/config.py, backend/app/main.py, backend/tests/__init__.py, backend/tests/test_config.py
- Learnings:
  - uv init generates backend/main.py stub — remove it; real entrypoint is backend/app/main.py
  - pytest returns exit code 5 (not 0) when no tests collected — added test_config.py to satisfy CI
  - ruff check passes cleanly with E/F/I/UP rules on the scaffold
---

---

## 2026-02-25T00:00:00+00:00 - S38 - Retrieval smoke test and latency baseline assertions
- Implemented: backend/tests/test_performance.py — 3 slow tests covering p50/p95 search latency, ingestion throughput, and memory growth; Makefile test-perf target; README Performance Baselines section
- Files changed: backend/tests/test_performance.py (new), backend/pyproject.toml (psutil dev dep), Makefile (test-perf target), README.md (Performance Baselines section)
- Learnings:
  - Reused perf_db fixture pattern from test_concurrent.py: file-based SQLite + mock ML services (module-level singleton injection)
  - psutil.Process().memory_info().rss measures current process RSS in bytes; 500MB growth budget is conservative for 10 mocked-ingestion docs
  - p50/p95 assertions over 20 queries with mocked retriever run well under baselines (p50 ~5ms, p95 ~20ms on dev machine)
  - shutil import was unused — ruff F401 caught it, fixed with --fix
  - All 3 perf tests pass in 53s wall clock (asyncio.gather for concurrent ingestion throughput test)
---

---

## 2026-02-26T00:00:00+00:00 - S41 - App startup pre-fetch and perceived performance
- Implemented: GlobalLoadingBar component (useIsFetching + CSS @keyframes loading-bar animation), startup prefetch in AppShell useEffect for documents + LLM settings, hover prefetch on NavLink onMouseEnter, NavItemDef interface with prefetchKey/prefetchFn, NAV_ITEMS array with per-tab prefetch config, four prefetchDocuments/prefetchLLMSettings/prefetchDueCards/prefetchMonitoringOverview helper functions
- Files changed: frontend/src/App.tsx (major rewrite), frontend/src/index.css (@keyframes loading-bar)
- Learnings:
  - GlobalLoadingBar must live inside QueryClientProvider to call useIsFetching(); placed before BrowserRouter so it covers all routes
  - Hover prefetch skips the network call if data already cached (qc.getQueryData check); staleTime: 60_000 consistent with QueryClient defaultOptions
  - documents prefetchKey must exactly match the key used in Learning page: ["documents", undefined, null, "newest", 1, 20]
  - useIsFetching() returns a number (count of in-flight queries), not a boolean — guard with === 0 for hiding the bar
  - CSS loading-bar animation: translateX(-100%)→(0%)→(100%) with scaleX gives a realistic indeterminate progress feel
  - CI: 280 passed, 0 failed; tsc --noEmit clean; ruff clean
---

---

## 2026-02-26T01:00:00+00:00 - S42 - E2E upload test — real HTTP endpoint, real Ollama, real pipeline
- Implemented: backend/tests/test_e2e_upload.py (3 @pytest.mark.e2e tests for live backend + 3 @pytest.mark.integration_http tests for CI), pyproject.toml markers, Makefile test-e2e target, README E2E Upload Tests section
- Files changed: backend/tests/test_e2e_upload.py (new), backend/pyproject.toml (markers + addopts), Makefile (test-e2e target), README.md (E2E section)
- Learnings:
  - asyncio.create_task background ingestion runs in same event loop as ASGITransport tests; polling with asyncio.sleep(0.05) yields control and lets the task advance
  - The ingestion workflow does NOT short-circuit on parse_node failure — corrupt PDFs flow through all nodes and reach 'complete' with 0 chunks; test asserts done/terminated rather than stage='error'
  - ruff F541: f-string without placeholders — "prefix\n" + "...".join() should not have f"" prefix
  - addopts updated to "-m 'not slow and not e2e'" to exclude both slow and e2e from make ci
  - integration_http marker tests selected by default pytest run (not excluded); e2e tests excluded
  - CI: 283 passed (+3 new integration_http tests), 9 deselected, ruff clean
---

---

## 2026-02-26T02:00:00+00:00 - S43 - Frontend compliance audit — loading, error, empty states across all pages
- Implemented targeted additions to Chat, Study, Viz, Notes pages per S43 spec
- Chat.tsx: added logger + useQueryClient, qaError dismissable inline banner on /qa failure, skeleton for message area while llmLoading, "Upload a document" empty state when hasDocuments=false (reads from TanStack Query cache), logger.error on /qa errors, logger.info mount/load
- Study.tsx: added logger + Skeleton import + useRef, replaced Loader2 spinner with skeleton grid (6 cards), isError: cardsError from flashcard query → inline red error banner, logger.info mount/load
- Viz.tsx: added logger + Skeleton + useRef, noDocSelected guard → "Select a document" empty state, replaced text loading with skeleton canvas, styled error as red border/bg card with "Graph unavailable for this document", query disabled when no doc selected, logger.info mount/load/error
- Notes.tsx: added logger + Skeleton + useRef + useEffect, isLoading/isError from notes query, skeleton grid loading state, inline error banner, updated empty state text to "No notes yet — highlights and annotations will appear here"
- Files changed: frontend/src/pages/Chat.tsx, Study.tsx, Viz.tsx, Notes.tsx
- Learnings:
  - useQueryClient().getQueryData() reads from TanStack Query cache synchronously — safe to use in render without a query call
  - Cache key ["documents", undefined, null, "newest", 1, 20] must match exactly what App.tsx prefetches
  - Viz query: added enabled: !noDocSelected to skip graph fetch when no doc selected and scope="document"
  - CI: 283 passed, tsc --noEmit clean, ruff clean
---

---

## 2026-02-26T10:00:00+00:00 - S44 - Backend structured logging audit — consistent log coverage across all services
- Added INFO-level structured logging to all mutating endpoints across routers: flashcards, documents, notes, study, explain, monitoring
- flashcards.py: added import logging + logger, INFO logs for generate, update, delete, review endpoints
- documents.py: fixed LogRecord attribute collision bug (extra={"filename":...} → extra={"upload_filename":...}) which caused KeyError in Python 3.13; added INFO logs for bulk_delete, patch, patch_tags
- notes.py: added import logging + logger, INFO logs for create, update, delete note endpoints
- study.py: added INFO logs for start_session (with session_id, document_id) and end_session (with cards_reviewed, cards_correct)
- explain.py: added INFO log for explain_text (before streaming), restructured extract_glossary to capture result then log count
- monitoring.py: added INFO log for store_eval_run
- embedder.py: added INFO init log "EmbeddingService created"
- ner.py: added INFO init log "EntityExtractor created" with model_dir in extra
- ingestion.py: added task_start/complete/failed logs to run_ingestion; wraps ainvoke in try/except with logger.error on failure
- Created tests/test_logging.py: 9 caplog-based tests using module-scoped TestClient fixture (avoids Phoenix lifecycle failures); uses caplog.at_level + caplog.set_level to capture INFO records
- Learnings:
  - Python 3.13 raises KeyError when extra={} dict uses a reserved LogRecord attribute name (filename, levelname, pathname, etc.) — always use unique keys
  - asyncio.create_task background tasks run after request returns; caplog cannot capture their logs in the same context manager block — test synchronous log points instead
  - Module-scoped TestClient fixture prevents Phoenix thread pool shutdown errors when running multiple logging tests sequentially
  - caplog.set_level applies for entire test function; caplog.at_level is a context manager for a block
  - CI: 291 passed, 1 pre-existing flaky test (test_http_corrupt_upload_terminates passes in isolation), 9 deselected, ruff clean
---

## S48 — Fix Chat Q&A — 2026-02-26
- Added no-context guard in qa.py stream_answer(): if retriever returns 0 chunks, yields {error: "no_context", done: true} and returns before calling LLM
- Wrapped LLM generate call in try/except: on any exception yields {error: "llm_unavailable", done: true} and returns
- Fixed vector_search() in retriever.py: wraps table.search() in try/except, returns [] on any LanceDB error; checks total rows first
- Chat.tsx: added SSE error event handling — setIsStreaming(false), remove placeholder message, show qaError banner
- Chat.tsx: error banner changed from red to amber styling (consistent with LLM settings warning)
- Added 3 new tests: test_qa_no_context, test_qa_ollama_offline, test_qa_with_mock_llm
- Fixed test_endpoint_not_found_response: updated assertion from not_found=True to error='no_context' (no-context guard fires before LLM when retriever returns [])
- CI: 302 passed, 13 deselected (slow), ruff clean, tsc clean
---

## 2026-03-05T00:00:00Z - S54 - Per-book content verification — known entities, keywords, co-occurrences, and semantic search for all 3 books
- Implemented: backend/tests/test_book_content.py with 6 test methods, all @pytest.mark.slow, parametrized over manifest.json. TestBookContentVerification class: test_known_entities_in_kuzu (Kuzu graph case-insensitive substring match), test_known_keywords_in_fts (FTS5 keyword hits), test_co_occurrence_edges (bidirectional edge lookup), test_semantic_search_relevance (vector search), test_fts_raw_term_coverage (raw SQL COUNT), test_ingest_timing_all_books (prints timings for all 3 books). Also marked S53 (DEMO REVIEW GATE 1) as passing since approval file existed.
- Files changed: backend/tests/test_book_content.py (created), scripts/ralph/prd.json (S53+S54 passes=true)
- Learnings:
  - get_graph_for_document returns {nodes: [{id, label, type, size, mention_count}], edges: [{source, target, weight}]}
  - Edge lookup must check both directions (source==a,target==b OR source==b,target==a)
  - asyncio_mode=auto means async test methods in classes work without @pytest.mark.asyncio
  - db_module.get_session_factory() gives the async session factory for raw SQL in tests
  - pytest_plugins = ["tests.conftest_books"] loads the shared session fixture (note "tests." prefix for rootdir=backend)
---

## S66 — Book ingestion: section-boundary chunking + chunk-to-section linking (2026-03-05)
Status: DONE
Commit: 7735569

### What was built
- `_chunk_book()` helper in `ingestion.py`: processes each section independently so no chunk crosses a chapter boundary
- `ChunkModel.section_id` set on every book chunk linking to its `SectionModel`
- `DocumentModel.chapter_count` (new nullable int column + migration) set after book ingestion
- Flat books (no sections detected) get a synthetic "Full Text" SectionModel; all chunks link to it
- `GET /sections/{document_id}` endpoint returns sections with accurate `chunk_count` per section
- 4 tests in `tests/test_book_ingestion.py` covering all acceptance criteria

### Key learnings
- Use `session.flush()` before creating chunks so section PKs are available for FK references in the same transaction
- `DocumentModel.chapter_count` exposed via `DocumentListItem` for use by chapter sidebar (S60)
- `has_summary` is always False (placeholder until section-level summaries implemented)
---

## S56 — Conversation ingestion: speaker-turn chunking, roster, timeline (2026-03-05)
Status: DONE
Commit: 6a52c55

### What was built
- `app/services/conversation_chunker.py` — ConversationChunker: detect(), chunk(), extract_roster(), extract_timeline()
- detect() matches >= 5 lines in first 60 using timestamp, name-colon, or name-arrow patterns
- chunk() groups consecutive same-speaker lines into turns, accumulates to 450 tokens, carries 1 overlap turn
- extract_roster() parses chunk texts for speaker lines to count per-speaker turns
- extract_timeline() regex-scans for timestamps and returns first/last
- `_chunk_conversation()` in ingestion.py: uses chunker if detect() → True, else fallback to RecursiveCharacterTextSplitter; sets chunk.speaker; writes conversation_metadata to DocumentModel
- DocumentModel.conversation_metadata (JSON nullable) + migration DDL
- GET /documents/{id}/conversation endpoint in documents router
- 12 tests in test_conversation_chunker.py

### Key learnings
- extract_roster must parse speaker lines from chunk.text, not just count chunk.speaker — short convos fit in one chunk so chunk.speaker alone misses secondary speakers
- Pattern order matters: try timestamp pattern before name-colon to avoid misparse
---

## S57 — Context expansion: adjacent chunks for books/conversations after RRF (2026-03-05)
Status: DONE
Commit: 288a678

### What was built
- ScoredChunk extended with `chunk_index: int = 0` and `speaker: str | None = None`
- source Literal extended with 'context_expansion'
- vector_search: reads speaker from LanceDB row (stored as "" → converted to None)
- keyword_search: batch SELECT from chunks table after FTS to get speaker + chunk_index
- _expand_context(): queries SQLite for chunk_index by chunk_id, then fetches neighbors at ±window; skips code/paper; dedup keeping higher score; caps at k*2; sorts desc
- retrieve() now calls _expand_context after rrf_merge
- 4 tests

### Key learnings
- chunk_index is not stored in LanceDB, so _expand_context does a SQLite lookup by chunk_id to get it
- SQL batch queries use string interpolation for IN clauses (SQLite doesn't support array binding)
- Dedup: when a neighbor is already in result_map from a different source, compare scores and keep higher
---

## S59 — Speaker-balanced retrieval (2026-03-05)
Status: DONE
Commit: a614a26

### What was built
- _round_robin() generic helper in retriever.py extracted from _diversify()
- _diversify() now checks speakers FIRST: if any chunk.speaker != None and concentration > 60%, round-robin across speakers
- Falls through to section-diversity when no speaker data present
- 3 new tests in test_retriever.py

### Key learnings
- ScoredChunk.speaker was already added in S57 — S59 is purely a change to _diversify()
- Generic _round_robin(group_key) works for both "speaker" and "section_heading" fields
---

## 2026-03-06T16:00:00+00:00 - S67 - Library and Notes list view — tabular layout with metadata columns
- Implemented: (1) backend/app/routers/documents.py — added `chunk_count_sq` correlated scalar subquery (COUNT chunks per document); added `chunk_count: int` to `DocumentListItem` Pydantic model; fetches chunk_count from row[5] in list_documents. (2) backend/tests/test_documents.py — added `test_documents_list_includes_chunk_count` (ingest 1 doc + 3 chunks, GET /documents, assert chunk_count>=1) and `test_documents_chunk_count_is_zero_for_fresh_doc` (doc with no chunks, assert chunk_count=0). (3) frontend/src/components/library/types.ts — added `chunk_count: number` to DocumentListItem interface. (4) frontend/src/store.ts — added `libraryView: 'grid'|'list'` and `notesView: 'grid'|'list'` state + setters to Zustand store. (5) frontend/src/components/ui/table.tsx (new) — shadcn Table component: Table, TableHeader, TableBody, TableRow, TableHead, TableCell forwardRef wrappers. (6) frontend/src/components/library/utils.ts — added `formatDate(isoStr)` helper (returns e.g. 'Mar 6, 2026'). (7) frontend/src/pages/Learning.tsx — removed local `useState<ViewMode>`, uses `libraryView`/`setLibraryView` from Zustand; added `isError`+`refetch` to useQuery; added `LibraryTable` component (sortable by Title and Ingested At with ChevronsUpDown/ChevronUp/ChevronDown icons, 5-skeleton-row loading, empty state, error state with retry); updated list view in "All documents" section to use LibraryTable; grid view unchanged. (8) frontend/src/pages/Notes.tsx — imports ViewToggle + useAppStore; uses `notesView`/`setNotesView` from store; added list view (Table with columns: Title 60-char truncated, Tags comma-joined, Group, Created At, Document Link or Standalone); loading state shows 3 skeleton table rows; panel header updated to include ViewToggle. (9) scripts/smoke/S67.sh (new) — GET /documents asserts 200 + 'chunk_count' in response. CI: 368 tests pass, tsc noEmit pass, vite build pass.
- Files changed: backend/app/routers/documents.py, backend/tests/test_documents.py, frontend/src/components/library/types.ts, frontend/src/store.ts, frontend/src/components/ui/table.tsx (new), frontend/src/components/library/utils.ts, frontend/src/pages/Learning.tsx, frontend/src/pages/Notes.tsx, scripts/smoke/S67.sh (new), scripts/ralph/prd.json (S67 passes=True)
- Learnings:
  - chunk_count correlated subquery follows exact same pattern as flashcard_count_sq and summary_count_sq already in list_documents — consistent and no N+1
  - shadcn Table component was missing from ui/ — created it as forwardRef wrappers with standard Tailwind classes; must be created before using in Table-based list view
  - LibraryTable has its own sortCol/sortDir local state — sorts the already-fetched items array client-side; server-side sort (newest/oldest/etc.) still controls initial order
  - ViewMode import removed from types.ts import in Learning.tsx (not needed since viewMode is now from store as string literal) — use string literals directly
  - Notes list view document link lookup: build `Object.fromEntries(documents.map(d => [d.id, d.title]))` from already-fetched documents list — no extra API call
---

## 2026-03-06T17:00:00+00:00 - S68 - App startup readiness — skeleton loading prevents false empty-state flash on cold start
- Implemented: (1) frontend/src/App.tsx — updated QueryClient defaultOptions: `retry: 1 → 2`, added `retryDelay: 1000`, added `gcTime: 60_000` alongside existing `staleTime: 60_000`. (2) frontend/src/pages/Learning.tsx — destructured `isSuccess` from useQuery; changed EmptyState guard from `!isLoading && !isError && total === 0` to `isSuccess && total === 0` (prevents false empty-state flash during loading); added `staleTime: 10_000, gcTime: 60_000` to the documents query override. (3) frontend/src/pages/Notes.tsx — added `staleTime: 10_000, gcTime: 60_000` to the notes query; changed grid loading state from 3 h-12 skeleton rows to 2 h-28 skeleton card placeholders (matches NoteCard dimensions). (4) frontend/src/pages/Study.tsx — changed flashcard loading state from 6 skeleton grid cards to centered spinner (Loader2 animate-spin) with "Loading your cards..." label; removed unused Skeleton import. (5) scripts/smoke/S68.sh (new) — sleep 2 then GET /documents assert 200.
- Files changed: frontend/src/App.tsx, frontend/src/pages/Learning.tsx, frontend/src/pages/Notes.tsx, frontend/src/pages/Study.tsx, scripts/smoke/S68.sh (new), scripts/ralph/prd.json (S68 passes=True)
- Learnings:
  - TanStack Query isLoading = true only when query has no data AND is fetching; use isSuccess for EmptyState guard to strictly distinguish 'not yet loaded' from 'truly empty'
  - QueryClient global staleTime was already 60_000; adding staleTime: 10_000 on specific query overrides the global to a shorter window — added per acceptance criteria requirement
  - Removing Skeleton import after replacing skeleton loading with spinner triggers TS6133 error — always remove unused imports when changing loading UI
  - retryDelay: 1000 in QueryClient defaultOptions applies a fixed 1s delay between retries (TanStack Query supports function for exponential backoff, but story specifies fixed 1000ms)
---

2026-03-06 S69 PASS — Markdown rendering throughout. MarkdownRenderer.tsx created (react-markdown + remark-gfm + rehype-highlight). Applied to Chat, SummaryPanel, ExplanationSheet, FlashcardCard, LibraryOverview. Notes Write/Preview tab editor. Backend prompts updated in summarizer (MODE_INSTRUCTIONS + LIBRARY_SYSTEM_PROMPTS), qa, flashcard. 5 new prompt tests. 373 tests pass, tsc clean, build clean.

2026-03-06 S70 PASS — Ingestion performance. SQLite chunk writes batched with session.add_all() in all 4 chunk paths. generate_all_summaries() added to SummarizationService (embedding+NER+summarization were already batched/fire-and-forget). 3 new tests. 376 tests pass, ruff clean, tsc clean.
