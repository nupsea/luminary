{
  "product": "Luminary v2",
  "branchName": "ralph/luminary-v2",
  "baseVersion": "v1 complete as of S74 on branch ralph/luminary-v1",
  "v2Philosophy": {
    "theme": "The system becomes an agent that reasons about HOW to answer, not a pipeline that always retrieves and generates.",
    "principles": [
      "Hierarchical knowledge: ingestion builds a three-level summary pyramid \u2014 section \u2192 document \u2192 library. Every level is pre-computed and stored. Nothing expensive is ever recomputed when inputs have not changed (Core Belief #18).",
      "Agentic chat: the QA pipeline is a LangGraph graph, not a linear function. A classifier node decides the retrieval strategy before any retrieval happens. The graph is the explicit, versioned contract for how questions are answered.",
      "Performance budget: ingestion of a large book (Iliad, ~3300 chunks, ~2100 sections) must complete \u2014 including section summaries \u2014 in under 15 minutes on commodity hardware. This is achieved through bounded parallelism, not sequential LLM calls.",
      "Boring technology wins (Core Belief #12): intent classification uses keyword heuristics first, LLM only as fallback. The heuristic is a pure function with a unit test. Reliability over cleverness.",
      "Pure functions for core logic (Core Belief #19): context packing, RRF scoring, intent heuristics are pure functions with no I/O. They live in app/services/ and are tested with plain assert statements.",
      "Every new service method and every new API endpoint must have at least one pytest test. No exceptions."
    ]
  },
  "architecture": {
    "newFiles": [
      "backend/app/models.py \u2014 add SectionSummaryModel (new table section_summaries)",
      "backend/app/services/section_summarizer.py \u2014 SectionSummarizerService: parallel section summary generation",
      "backend/app/services/context_packer.py \u2014 pure function pack_context(): smart context window assembly",
      "backend/app/runtime/chat_graph.py \u2014 LangGraph StateGraph replacing linear stream_answer()",
      "backend/app/types/chat.py \u2014 ChatState TypedDict and intent type literals",
      "backend/tests/test_section_summarizer.py",
      "backend/tests/test_context_packer.py",
      "backend/tests/test_chat_graph.py",
      "backend/tests/test_v2_ingestion_perf.py",
      "scripts/smoke/S75.sh",
      "scripts/smoke/S76.sh",
      "scripts/smoke/S77.sh",
      "scripts/smoke/S78.sh",
      "scripts/smoke/S79.sh"
    ],
    "modifiedFiles": [
      "backend/app/workflows/ingestion.py \u2014 add section_summarize node after entity_extract",
      "backend/app/services/summarizer.py \u2014 pregenerate() uses section summaries as fast path",
      "backend/app/services/qa.py \u2014 stream_answer() becomes thin wrapper over chat_graph",
      "backend/app/db_init.py \u2014 create section_summaries table",
      "backend/app/routers/summarize.py \u2014 add GET /summarize/{doc_id}/sections endpoint",
      "docs/ARCHITECTURE.md \u2014 document v2 additions",
      "docs/QUALITY_SCORE.md \u2014 update Q&A grade"
    ],
    "layerRules": "All new code obeys the existing 6-layer rule: Types \u2192 Config \u2192 Repo \u2192 Service \u2192 Runtime \u2192 API. SectionSummarizerService is a Service. ChatState TypedDict is a Type. chat_graph.py is a Runtime. No cross-layer imports."
  },
  "performanceBudget": {
    "ingestion_iliad": "Full ingestion (parse + chunk + embed + NER + section summaries) <= 15 minutes for a ~180k-word TXT book with ~2100 sections. Section summarization caps at 100 summary units. With Semaphore(10) concurrency and ~3 sec/call: 10 batches * 3 sec = 30 sec for section summaries. Document summary adds ~30 sec. Total background work well under 5 minutes after stage=complete.",
    "chat_response": "First token latency <= 3 seconds for summary-intent queries (pure DB lookup, no retrieval). <= 8 seconds for factual queries (hybrid retrieval + LLM). These are not CI-enforced but Ralph must not introduce regressions observable by a human using the app.",
    "intent_classification": "Keyword heuristic completes in microseconds (no LLM call). LLM fallback for ambiguous cases adds <= 2 seconds."
  },
  "stories": [
    {
      "id": "S75",
      "title": "SectionSummaryModel + parallel section summarization node in ingestion pipeline",
      "phase": "V2A \u2014 Hierarchical Knowledge",
      "priority": 75,
      "featureRef": "F-INGEST-V2",
      "description": "CONTEXT: The ingestion pipeline currently generates document-level summaries only (via background task), using map-reduce over all raw chunks. For a large book like the Iliad (3307 chunks), this requires 100+ sequential LLM calls and either times out or takes 15+ minutes. V2 fixes this by generating section-level summaries during ingestion in parallel, bounded to a maximum of 100 summary units per document regardless of section count. These section summaries become the input to document-level summarization (S76), replacing the slow chunk-based map-reduce. DATA MODEL \u2014 Add SectionSummaryModel to app/models.py: class SectionSummaryModel(Base): __tablename__ = 'section_summaries'. Columns: id: Mapped[str] (UUID primary key), document_id: Mapped[str] (non-nullable, indexed), section_id: Mapped[str | None] (nullable \u2014 null when the unit spans multiple merged sections), heading: Mapped[str] (the heading or merged headings for this unit, max 200 chars), content: Mapped[str] (the generated 1-2 sentence summary), unit_index: Mapped[int] (0-based position within the document), created_at: Mapped[datetime]. Add this model to app/db_init.py so the table is created on startup alongside other tables. DB MIGRATION: add 'section_summaries' to the tables created in db_init.py. SERVICE \u2014 Create app/services/section_summarizer.py. Class SectionSummarizerService. Method: async def generate(self, document_id: str, concurrency: int = 10) -> int. Implementation: (1) Fetch all SectionModel rows for document_id ordered by section_order. (2) Filter: keep only sections where len(section.preview) >= 200 characters. Short headings and near-empty sections produce useless summaries. (3) GROUPING for large documents: if the number of qualifying sections exceeds 100, merge adjacent sections into groups so the total units <= 100. Group by consecutive blocks: group_size = ceil(qualifying_count / 100). Each group's heading = first section heading in the group; each group's text = concatenation of section previews within the group. (4) For each unit (single section or merged group): call LiteLLM with model from get_settings().default_model, system='Summarize the following passage in 1 to 2 sentences. Be specific about names, events, and arguments. Output only the summary.', user=unit_text[:4000] (hard cap to stay within context). (5) Concurrency: use asyncio.Semaphore(concurrency) \u2014 NOT asyncio.gather without a semaphore. Wrap each LiteLLM call in the semaphore. This guarantees at most `concurrency` parallel calls at any moment. (6) Store each result as a SectionSummaryModel row as it completes (don't wait for all). (7) Return total count of rows inserted. Error handling: if an individual unit's LLM call fails, log a warning and skip that unit (non-fatal). If Ollama is unreachable (litellm.ServiceUnavailableError), log a warning and return 0 immediately \u2014 the document is still usable without section summaries. INGESTION GRAPH \u2014 In app/workflows/ingestion.py, add a new node function section_summarize_node(state: IngestionState) -> IngestionState AFTER entity_extract_node and BEFORE summarize_node. The node: imports SectionSummarizerService lazily (same pattern as other nodes), calls await svc.generate(state['document_id']), stores the count in state['section_summary_count'] (add this field to IngestionState TypedDict). The existing summarize_node (which fires background pregenerate) remains unchanged \u2014 S76 will modify pregenerate to use section summaries. Wire the new node into the graph: the edge entity_extract_node -> section_summarize_node -> summarize_node. PERFORMANCE REQUIREMENT: For the Iliad fixture (~2100 sections grouped to 100 units, Semaphore(10)): section_summarize_node must complete in under 5 minutes with a running Ollama instance. With Semaphore(10) and ~3 sec/call: 100 units / 10 concurrent = 10 sequential batches * ~3 sec = ~30 seconds. The 5-minute budget is conservative. API \u2014 Add GET /summarize/{document_id}/sections to app/routers/summarize.py: returns list of {unit_index, heading, content} ordered by unit_index for the given document. Returns [] if no section summaries exist (not an error \u2014 document may be pre-v2). TESTS \u2014 tests/test_section_summarizer.py: (a) test_grouping_caps_at_100_units: create a document with 300 qualifying sections, call generate() with a mock LiteLLM that returns instantly, assert SectionSummaryModel count <= 100. (b) test_short_sections_skipped: create sections where half have len(preview) < 200, assert only the qualifying half are summarized. (c) test_concurrency_respects_semaphore: mock LiteLLM call with asyncio.sleep(0.01), measure time for 50 units with concurrency=5, assert fewer concurrent calls than total units. (d) test_ollama_offline_returns_zero: mock litellm.acompletion to raise litellm.ServiceUnavailableError, assert generate() returns 0 and does not raise. (e) test_get_sections_endpoint_returns_list: after generate(), GET /summarize/{id}/sections returns list with correct unit_index ordering. SMOKE \u2014 scripts/smoke/S75.sh: (1) POST /ingest with a small real TXT fixture, wait for stage=complete by polling GET /documents/{id} every 5s up to 120s. (2) GET /summarize/{id}/sections, assert HTTP 200 and response is a JSON array (may be empty for very small docs). (3) Exit 0.",
      "acceptanceCriteria": [
        "SectionSummaryModel added to app/models.py and app/db_init.py \u2014 table section_summaries created on startup",
        "SectionSummarizerService.generate() groups sections to <= 100 units when document has > 100 qualifying sections",
        "Sections with preview length < 200 characters are skipped",
        "Concurrency controlled by asyncio.Semaphore(concurrency), default concurrency=10",
        "Each unit stored immediately on completion \u2014 no wait-for-all",
        "Ollama offline: generate() returns 0, no exception raised, document ingestion continues",
        "section_summarize_node wired into ingestion graph between entity_extract and summarize",
        "GET /summarize/{document_id}/sections returns [{unit_index, heading, content}] ordered by unit_index",
        "For Iliad fixture: section_summarize_node completes in under 5 minutes with live Ollama",
        "test_grouping_caps_at_100_units passes",
        "test_short_sections_skipped passes",
        "test_ollama_offline_returns_zero passes",
        "uv run pytest tests/test_section_summarizer.py exits 0",
        "scripts/smoke/S75.sh exits 0",
        "No datetime.utcnow() calls in section_summarizer.py \u2014 use datetime.now(datetime.UTC) for created_at and any other datetime construction in this file"
      ],
      "passes": true
    },
    {
      "id": "S76",
      "title": "Document summary fast-path: pregenerate() uses section summaries instead of raw chunks",
      "phase": "V2A \u2014 Hierarchical Knowledge",
      "priority": 76,
      "featureRef": "F-INGEST-V2",
      "description": "CONTEXT: After S75, section summaries exist for documents. This story changes pregenerate() in SummarizationService so that when section summaries are available, it uses them as the map-reduce input instead of raw chunks. This reduces the Iliad's document summary generation from 100+ sequential LLM calls to a single LLM call over the concatenated section summaries. This directly fixes the 15-minute timeout problem. FAST PATH LOGIC \u2014 In app/services/summarizer.py, add a private async method _build_section_summary_input(self, document_id: str) -> str | None. Implementation: (1) Query SectionSummaryModel for all rows matching document_id, ordered by unit_index. (2) If fewer than 3 rows found, return None (not enough coverage to trust). (3) Build a markdown string: join all rows as '## {heading}\\n{content}\\n\\n'. Return this string. MODIFY pregenerate() \u2014 The existing pregenerate() fetches chunks and runs map-reduce. Change it to: (1) First call _build_section_summary_input(document_id). (2) If it returns a non-None string (the fast path): use that string directly as the input_text for all three modes (one_sentence, executive, detailed). No map-reduce needed. Each mode is one LLM call on the section summary input. Store the section summary input as mode='_section_reduce' in SummaryModel (following Core Belief #18: cache expensive intermediates \u2014 same pattern as existing '_map_reduce' mode). (3) If it returns None (the slow path \u2014 no section summaries, pre-v2 document or section summarization was offline): fall through to the existing chunk-based map-reduce. This preserves backward compatibility for all existing documents. IMPORTANT: The '_section_reduce' row should be stored once and reused across all modes, exactly like '_map_reduce'. If '_section_reduce' already exists for this document, skip recomputing it. PERFORMANCE REQUIREMENT: For the Iliad with 100 section summary units: _build_section_summary_input() is a DB query (< 100ms). Each of the three summary modes (one_sentence, executive, detailed) is a single LLM call on the concatenated section summaries (~30-60 sec each). Total pregenerate time: < 3 minutes for Iliad. Compare to v1 which timed out at 15+ minutes. CACHE INVALIDATION: When section summaries for a document are regenerated (e.g. force_refresh ingest), the '_section_reduce' summary row must be deleted so pregenerate() recomputes it. Add a method async def invalidate_section_reduce_cache(self, document_id: str) -> None that deletes SummaryModel rows where document_id matches and mode='_section_reduce'. Call this at the start of generate() in SectionSummarizerService when regenerating. TESTS \u2014 tests/test_document_summary_fast_path.py: (a) test_fast_path_used_when_section_summaries_exist: insert 10 SectionSummaryModel rows for a doc, call pregenerate() with mock LiteLLM, assert mock was called exactly once per mode (not dozens of times as in map-reduce). (b) test_slow_path_fallback_when_no_section_summaries: no SectionSummaryModel rows, call pregenerate(), assert existing map-reduce logic runs (verify by checking that LiteLLM was called with chunk batches). (c) test_section_reduce_cached_as_db_row: after fast path runs, assert SummaryModel has a row with mode='_section_reduce'. (d) test_fast_path_skipped_with_fewer_than_3_units: insert 2 rows, assert slow path is taken. SMOKE \u2014 scripts/smoke/S76.sh: (1) Ingest a document that already has section summaries (from S75 smoke). (2) POST /summarize/{id} with mode=executive and force_refresh=true. (3) Assert HTTP 200 and streaming response contains non-empty content. (4) GET /summarize/{id}/cached, assert 'executive' key present in response. (5) Exit 0.",
      "acceptanceCriteria": [
        "_build_section_summary_input() returns None when < 3 section summary units exist",
        "pregenerate() uses section summaries as input when >= 3 units available (fast path)",
        "Fast path: each of one_sentence/executive/detailed is one LLM call, not a map-reduce loop",
        "Fast path input stored as mode='_section_reduce' in SummaryModel for reuse (Core Belief #18)",
        "Slow path (chunk map-reduce) used when no section summaries exist \u2014 backward compatible with all pre-v2 documents",
        "invalidate_section_reduce_cache() deletes '_section_reduce' row on regeneration",
        "For Iliad: pregenerate() completes in under 3 minutes with live Ollama",
        "test_fast_path_used_when_section_summaries_exist: LiteLLM called once per mode, not N times",
        "test_slow_path_fallback_when_no_section_summaries passes",
        "test_section_reduce_cached_as_db_row passes",
        "uv run pytest tests/test_document_summary_fast_path.py exits 0",
        "scripts/smoke/S76.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S77",
      "title": "ChatState TypedDict + LangGraph chat router skeleton with intent classifier",
      "phase": "V2B \u2014 Agentic Chat",
      "priority": 77,
      "featureRef": "F-CHAT-V2",
      "description": "CONTEXT: The current qa.py stream_answer() is a linear async function that always does: rewrite query \u2192 hybrid retrieval \u2192 (maybe prepend summary) \u2192 LLM call. There is no intelligence about what kind of question is being asked. V2 replaces this with a LangGraph StateGraph in app/runtime/chat_graph.py. This story creates the graph skeleton, the ChatState type, and the intent classifier node. Subsequent stories (S78, S79) fill in the strategy nodes and context packer. TYPES \u2014 Create app/types/chat.py. Define: (1) IntentType = Literal['summary', 'factual', 'relational', 'comparative', 'exploratory']. (2) ChatState as a TypedDict with fields: question: str, doc_ids: list[str], scope: Literal['single', 'all'], intent: IntentType | None, rewritten_question: str | None, chunks: list[dict], section_context: str | None, answer: str, citations: list[dict], confidence: Literal['high', 'medium', 'low'], not_found: bool, model: str | None. All fields must have defaults compatible with TypedDict (use total=False for optional fields OR provide all defaults). INTENT CLASSIFIER \u2014 The intent classifier runs as the first node in the graph. It must be fast because it adds latency to every query. Strategy: keyword heuristics first (pure function, microseconds), LLM fallback only when heuristics return low confidence. Pure function classify_intent_heuristic(question: str) -> tuple[IntentType, float]: implements the following rules in order (first match wins): (1) If question contains any of [summarize, overview, what is this about, what are the themes, main points, key ideas, synopsis, brief me, give me a summary]: return ('summary', 0.9). (2) If question contains any of [how are, how does, relation between, connection between, what is the relationship, how do X and Y]: return ('relational', 0.85). (3) If question contains any of [compare, difference between, how is X different, versus, vs., similarities]: return ('comparative', 0.85). (4) If question contains any of [explain, what does, who is, where is, when did, which, list all, how many, what happened]: return ('factual', 0.8). (5) Otherwise: return ('exploratory', 0.5). All string matching is case-insensitive. This function lives in app/services/intent.py as a pure function with no imports from other app layers. LLM FALLBACK: if confidence < 0.7, call LiteLLM with system='Classify the question. Reply with exactly one word: summary, factual, relational, comparative, or exploratory.', user=question. Parse response: strip whitespace and lowercase. If response matches one of the five values, use it with confidence=0.8. Otherwise fall back to 'factual' with confidence=0.6. The LLM fallback must be wrapped in try/except \u2014 if it fails (Ollama offline), default to 'factual'. GRAPH SKELETON \u2014 Create app/runtime/chat_graph.py. Build a LangGraph StateGraph[ChatState] with the following nodes: (1) classify_node: calls classify_intent_heuristic(), if confidence < 0.7 calls LLM fallback, writes intent and (if rewriting enabled) calls existing Kuzu rewriting logic (import from qa.py, do not duplicate). (2) route_node: a conditional edge function, not a node \u2014 reads intent from state and routes: 'summary' -> 'summary_node', 'relational' -> 'graph_node', 'comparative' -> 'comparative_node', 'factual' OR 'exploratory' -> 'search_node'. (3) summary_node: STUB for S78 \u2014 for now, writes answer='[summary_node stub]' to state, sets confidence='high'. (4) search_node: STUB for S78 \u2014 calls the existing _hybrid_retrieval logic from qa.py, writes chunks to state, writes answer='[search_node stub]'. (5) graph_node: STUB for S78 \u2014 writes answer='[graph_node stub]'. (6) comparative_node: STUB for S78 \u2014 writes answer='[comparative_node stub]'. (7) synthesize_node: STUB for S79 \u2014 takes chunks from state, calls LLM, writes answer. For now copies existing LLM call logic from stream_answer(). The graph's entry point is classify_node. All terminal nodes (after their answer is set) route to synthesize_node, then END. QA.PY INTEGRATION \u2014 Modify stream_answer() in qa.py to invoke the LangGraph graph instead of its existing logic. The signature of stream_answer() does NOT change \u2014 the frontend contract is unchanged. stream_answer() creates the initial ChatState from its parameters, invokes the graph with graph.invoke(state), then streams the answer field token by token using the same SSE format as before. The graph must be compiled once at module load (graph = build_chat_graph().compile()) and reused. IMPORTANT: The streaming behavior from the frontend's perspective must be identical to v1 \u2014 the same SSE events with the same fields (token, done, citations, confidence, not_found). The graph's synthesize_node is responsible for calling LLM and accumulating tokens. TESTS \u2014 tests/test_intent_classifier.py: (a) test_summary_keywords_detected: each keyword in the summary list triggers intent='summary'. (b) test_comparative_keywords_detected. (c) test_relational_keywords_detected. (d) test_low_confidence_triggers_llm_fallback: mock LiteLLM to return 'factual', assert intent='factual'. (e) test_llm_fallback_offline_defaults_to_factual: mock LiteLLM to raise exception, assert intent='factual'. tests/test_chat_graph.py: (a) test_summary_question_routes_to_summary_node: run graph with question='summarize this book', assert stub answer contains 'summary_node'. (b) test_factual_question_routes_to_search_node. (c) test_graph_invoke_does_not_raise: run full graph with real state dict, assert no exception. SMOKE \u2014 scripts/smoke/S77.sh: POST /qa with body {question: 'What are the main themes?', document_ids: [], scope: 'all'}, assert HTTP 200 and response body contains 'answer' field with non-empty string. Exit 0.",
      "acceptanceCriteria": [
        "app/types.py defines IntentType literal and ChatState TypedDict (collocated with ScoredChunk and other types \u2014 do NOT create a separate app/types/chat.py)",
        "app/services/intent.py contains classify_intent_heuristic() as a pure function with no I/O",
        "Keyword heuristics cover all five intent types with case-insensitive matching",
        "LLM fallback invoked only when heuristic confidence < 0.7",
        "LLM fallback offline: defaults to 'factual' without raising",
        "app/runtime/chat_graph.py defines LangGraph StateGraph[ChatState] with all six nodes",
        "route_node is a conditional edge dispatching on intent field",
        "Stub nodes for summary/graph/comparative return placeholder answers (replaced in S78)",
        "search_node stub calls existing hybrid retrieval and writes chunks to state",
        "test_summary_keywords_detected passes for all keywords in the summary list",
        "test_llm_fallback_offline_defaults_to_factual passes",
        "test_summary_question_routes_to_summary_node passes",
        "test_graph_invoke_does_not_raise passes",
        "uv run pytest tests/test_intent_classifier.py tests/test_chat_graph.py exits 0",
        "scripts/smoke/S77.sh exits 0 \u2014 POST /qa returns HTTP 200 with non-empty answer",
        "True streaming: stream_answer() does NOT buffer the full LLM response before yielding tokens. Implement one of: (a) graph.astream_events(version='v1') intercepting on_chat_model_stream events from synthesize_node, OR (b) synthesize_node returns the prepared prompt+context in state without calling the LLM, and stream_answer() calls the LLM directly after ainvoke(). In either case, the SSE client receives the first 'token' event as the LLM generates it, not after all tokens are collected.",
        "Graph singleton compiled once at module load (not per request) and reused across all calls to stream_answer()",
        "'summary' (the word alone) is in _SUMMARY_KWS \u2014 'what is the summary?', 'provide a summary', 'give me a summary', 'the summary of this' all classify as summary intent",
        "'how does' and 'how do' are removed from _RELATIONAL_KWS \u2014 factual questions like 'how does the main character escape?' and 'how does FSRS scheduling work?' must NOT misclassify as relational intent",
        "test_streaming_is_progressive: with a mock LLM async-generator that yields 3 tokens one at a time (with asyncio.sleep(0) between each), assert that the first SSE 'token' event is yielded before all 3 tokens are generated \u2014 verifies tokens are not fully buffered before streaming begins",
        "test_summary_keyword_alone: classify_intent_heuristic('what is the summary of this book?') returns ('summary', 0.9)",
        "test_factual_question_not_relational: classify_intent_heuristic('how does the hero escape?') returns intent != 'relational'"
      ],
      "passes": true
    },
    {
      "id": "S78",
      "title": "Strategy nodes: summary_lookup, graph_traversal, comparative, and search with section augmentation",
      "phase": "V2B \u2014 Agentic Chat",
      "priority": 78,
      "featureRef": "F-CHAT-V2",
      "description": "CONTEXT: S77 created the LangGraph skeleton with stub nodes. This story implements the real logic for each strategy node. Each node is a self-contained async function that reads from ChatState and writes back to it. SUMMARY_NODE \u2014 Replace the stub. Logic: (1) Fetch the 'executive' summary from SummaryModel for the requested document(s). For scope='single' and one document: query SummaryModel where document_id=state.doc_ids[0] and mode='executive', return content. For scope='all': fetch the most recent LibrarySummaryModel where mode='executive'. (2) If no summary found: fall through to search_node by setting state.intent='factual' and returning state (the graph will re-route on the next step \u2014 add a conditional edge: if summary_node finds nothing, re-route to search_node). (3) If found: set state.chunks=[] (no retrieval needed), state.section_context=summary_content, state.answer='' (synthesize_node will generate the final answer using section_context as primary input). GRAPH_NODE \u2014 Replace the stub. Logic: (1) Extract entity names from the question using a simple regex approach: find capitalized words and quoted strings. (2) Query Kuzu: for each extracted entity, MATCH (e:Entity {name: $name})-[r]-(related) RETURN e.name, type(r), related.name LIMIT 20. (3) Format graph results as a structured string: 'Knowledge graph connections:\\n{entity} --{relation}--> {related}\\n...'. (4) If Kuzu is unavailable or returns 0 results: set state.section_context=None and fall through (return state with intent overridden to 'factual', triggering re-route to search_node). (5) Also run hybrid retrieval (k=5) as a grounding supplement \u2014 set state.chunks to these 5 chunks. State.section_context = graph_result_string. Wrap all Kuzu calls in try/except \u2014 graph failure must never fail the answer. COMPARATIVE_NODE \u2014 Replace the stub. Logic: (1) Run hybrid retrieval twice: once for each side of the comparison (detect sides by splitting on 'compare', 'versus', 'vs.', 'difference between', 'vs'). If two sides cannot be detected, run a single retrieval. (2) Interleave results: [side_a_chunk_1, side_b_chunk_1, side_a_chunk_2, side_b_chunk_2, ...]. (3) Write interleaved chunks to state.chunks. Set state.section_context to a header: 'Comparison query: {question}'. The synthesize_node will format the answer structurally. SEARCH_NODE \u2014 Replace the stub with full implementation. Logic: (1) Use rewritten_question if available, else question. (2) Call existing hybrid retrieval (HybridRetriever, k=10). (3) For each retrieved chunk that has a section_id: look up its SectionSummaryModel row by section_id (batch DB lookup, not per-chunk). (4) For each chunk, prepend section summary if found: format as '### {section_heading}\\n{section_summary}\\n---\\n{chunk_text}'. This gives the LLM section-level context without retrieving entire sections. (5) Write augmented chunks to state.chunks. SYNTHESIZE_NODE \u2014 Replace the stub. This node is the LLM call. Logic: (1) Build context string from state.chunks (augmented) and state.section_context (graph results, section summary, or library summary). (2) Build system prompt based on intent: 'summary' -> 'Answer using the provided document summary. Be concise and structured.'; 'relational' -> 'Answer using the knowledge graph connections and the supporting passages. Name the entities clearly.'; 'comparative' -> 'Compare the two subjects using the provided passages. Structure as: Subject A: ... Subject B: ...'; default -> existing QA system prompt. (3) Call LiteLLM with streaming=True. (4) Accumulate tokens and write to state.stream_tokens. (5) Parse final accumulated text using existing _split_response() to extract answer, citations, confidence, not_found. (6) Write these fields to state. The SSE streaming in stream_answer() reads from state.stream_tokens as the graph runs (use LangGraph's streaming API or astream_events). IMPORTANT: Do not duplicate any existing logic. Import _split_response, _enrich_citation_titles, _maybe_rewrite_query from existing qa.py locations. Refactor them into app/services/ if they are currently defined inside stream_answer() (move to module level). TESTS \u2014 tests/test_chat_graph_nodes.py: (a) test_summary_node_returns_executive_summary: insert a SummaryModel row with mode='executive', run summary_node, assert state.section_context == that content. (b) test_summary_node_falls_through_when_no_summary: no summary in DB, assert intent overridden to 'factual'. (c) test_graph_node_falls_through_on_kuzu_error: mock Kuzu to raise exception, assert no exception raised and intent overridden to 'factual'. (d) test_search_node_augments_chunks_with_section_summaries: insert SectionSummaryModel rows, run search_node with mock retrieval returning chunks with matching section_ids, assert output chunk texts contain '---' separator and section heading. (e) test_comparative_node_interleaves_results: mock retrieval to return 3 chunks per side, assert state.chunks alternates sides. (f) test_synthesize_node_calls_litellm: mock LiteLLM, run synthesize_node, assert LiteLLM called with system prompt containing intent-appropriate text. SMOKE \u2014 scripts/smoke/S78.sh: (1) POST /qa with question='Who is Achilles?', assert HTTP 200 and answer non-empty. (2) POST /qa with question='Summarize this document', assert HTTP 200. (3) Both responses must return within 30 seconds. Exit 0.",
      "acceptanceCriteria": [
        "summary_node fetches executive summary from SummaryModel; falls through to search when absent",
        "graph_node queries Kuzu for entity relationships; falls through to search on Kuzu error without raising",
        "comparative_node detects comparison sides and interleaves chunks from each side",
        "search_node augments retrieved chunks with section summaries from SectionSummaryModel",
        "Section-augmented chunks formatted as '### {heading}\\n{summary}\\n---\\n{chunk_text}'",
        "synthesize_node builds intent-appropriate system prompt and calls LiteLLM with streaming",
        "synthesize_node parses answer/citations/confidence using existing _split_response()",
        "_split_response and _maybe_rewrite_query are module-level functions (not nested in stream_answer)",
        "Kuzu failure in graph_node: no exception propagates, intent overrides to factual",
        "All stub nodes from S77 are replaced",
        "test_summary_node_falls_through_when_no_summary passes",
        "test_graph_node_falls_through_on_kuzu_error passes",
        "test_search_node_augments_chunks_with_section_summaries passes",
        "uv run pytest tests/test_chat_graph_nodes.py exits 0",
        "scripts/smoke/S78.sh exits 0 \u2014 both /qa calls return HTTP 200 within 30 seconds",
        "summary_node populates state['answer'] directly from executive summary text (not state['section_context']) when the summary exists \u2014 the pass-through check in synthesize_node (if existing_answer: return {}) prevents a redundant second LLM call for summary intent",
        "summary_node sets confidence='high' explicitly when it populates the answer from an executive summary",
        "synthesize_node's pass-through (existing_answer non-empty \u2192 return {}) works correctly with the answer set by summary_node \u2014 no LLM call is made for summary intent when executive summary is cached",
        "test_summary_node_sets_answer_directly: mock session returns an executive summary row; assert summary_node returns dict containing non-empty 'answer' key and confidence='high'; assert synthesize_node is NOT called (0 LLM invocations)",
        "test_summary_intent_end_to_end: POST /qa with scope='single' and question='summarize this document' returns HTTP 200, done event has non-empty answer and confidence='high'"
      ],
      "passes": true
    },
    {
      "id": "S79",
      "title": "Pure context packer: smart context window assembly with deduplication and section grouping",
      "phase": "V2B \u2014 Agentic Chat",
      "priority": 79,
      "featureRef": "F-CHAT-V2",
      "description": "CONTEXT: The synthesize_node currently concatenates augmented chunks in retrieval order. For long documents with many matching chunks, this wastes the context window with redundant text (chunks from the same section repeat the same ideas). This story adds a pure context packer that groups chunks by section, deduplicates near-duplicate content, and respects a strict token budget. Per Core Belief #19, this is a pure function with no I/O. PURE FUNCTION \u2014 Create app/services/context_packer.py. Define: pack_context(chunks: list[dict], token_budget: int = 3000, dedup_ratio: float = 0.8) -> str. The `chunks` list contains dicts with keys: text (str), section_id (str | None), section_heading (str | None), section_summary (str | None), relevance_score (float). Algorithm: (1) Group chunks by section_id (chunks with section_id=None form their own group keyed by chunk index). (2) Sort section groups by their highest relevance_score descending. (3) For each section group (most relevant first): (a) If a section_summary is present and not yet included, emit '### {section_heading}\\n{section_summary}\\n' (count tokens). (b) For each chunk in the group (sorted by relevance descending): check if this chunk's text is more than dedup_ratio (80%) similar to any already-emitted text. Similarity = len(longest_common_substring(a, b)) / max(len(a), len(b)). If similar, skip it. Otherwise emit '---\\n{chunk_text}\\n'. (c) Stop adding chunks when cumulative token count >= token_budget. (d) Token count estimate: len(text.split()) * 1.3 (fast approximation, no tokenizer dependency). (4) Return the assembled string. EDGE CASES: if chunks is empty, return ''. If all chunks exceed budget individually, include at least the first chunk truncated to token_budget. If dedup_ratio=1.0, deduplication is disabled (always include). INTEGRATION: In synthesize_node in chat_graph.py, replace the ad-hoc chunk concatenation with a call to pack_context(state.chunks, token_budget=3000). The state.section_context (graph results, library summary) is prepended to the pack_context output, capped at 1000 tokens. TESTS \u2014 tests/test_context_packer.py: all tests are pure unit tests with no DB or mock setup. (a) test_sections_ordered_by_relevance: chunks from section B have higher score than section A, assert B appears first in output. (b) test_deduplication_skips_similar_chunks: two chunks with > 80% overlap, assert only one appears in output. (c) test_token_budget_enforced: chunks totaling 6000 tokens with budget=3000, assert output token estimate <= 3300 (10% tolerance). (d) test_section_summary_emitted_once: section with 3 chunks and a section_summary, assert section_summary appears exactly once in output. (e) test_empty_chunks_returns_empty_string. (f) test_no_section_id_chunks_handled: all chunks have section_id=None, assert output is non-empty and no exception raised. (g) test_dedup_ratio_1_includes_all: with dedup_ratio=1.0, all chunks within budget are included even if near-duplicate. SMOKE \u2014 scripts/smoke/S79.sh: POST /qa with question='What happens in book 1?' for a book document. Assert HTTP 200, answer non-empty, response time < 15 seconds. Exit 0.",
      "acceptanceCriteria": [
        "app/services/context_packer.py defines pack_context() as a pure function with no imports from repos, models, or external services",
        "pack_context() groups chunks by section_id and orders section groups by max relevance score",
        "Section summary emitted once per section group (not once per chunk)",
        "Chunks with > dedup_ratio text overlap with already-emitted text are skipped",
        "Token budget enforced: output token estimate does not exceed token_budget * 1.1",
        "Empty chunks input returns empty string without error",
        "section_context prepended to pack_context output in synthesize_node, capped at 1000 tokens",
        "synthesize_node uses pack_context() instead of raw chunk concatenation",
        "test_sections_ordered_by_relevance passes",
        "test_deduplication_skips_similar_chunks passes",
        "test_token_budget_enforced passes",
        "test_section_summary_emitted_once passes",
        "uv run pytest tests/test_context_packer.py exits 0",
        "scripts/smoke/S79.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S80",
      "title": "V2 integration tests, performance benchmarks, and QUALITY_SCORE update",
      "phase": "V2C \u2014 Quality Gates",
      "priority": 80,
      "featureRef": "F-V2-QUALITY",
      "description": "CONTEXT: Stories S75-S79 implement the v2 architecture. This story adds integration tests that validate the full v2 pipeline end-to-end on real documents, enforces the performance budget, and updates the quality documentation. Without enforced CI gates, the performance and routing improvements are invisible to future ralph iterations. INTEGRATION TESTS \u2014 tests/test_v2_pipeline.py. This test file uses the same session-scoped fixture pattern as test_e2e_book.py (reads from conftest_books.py). Tests assume the three corpus books are already ingested. (a) test_section_summaries_generated_for_all_books: for each of the three books, assert len(SectionSummaryModel rows where document_id=doc.id) >= 10. This verifies section_summarize_node ran successfully. (b) test_document_summary_uses_fast_path: for each book with section summaries, fetch SummaryModel where mode='_section_reduce', assert it exists. This proves pregenerate() used the fast path. (c) test_intent_routing_summary: POST /qa with body {question: 'Give me an overview of this book', document_ids: [alice_id], scope: 'single'}. Assert HTTP 200. Assert response.answer is non-empty. Assert response.confidence in ('high', 'medium'). (d) test_intent_routing_factual: POST /qa with body {question: 'Who is the White Rabbit?', document_ids: [alice_id], scope: 'single'}. Assert HTTP 200 and answer non-empty and at least one citation returned. (e) test_intent_routing_comparative: POST /qa with body {question: 'Compare Alice and Arjuna', document_ids: [], scope: 'all'}. Assert HTTP 200 and answer non-empty. (f) test_intent_routing_relational: POST /qa with body {question: 'How are Achilles and Hector related?', document_ids: [iliad_id], scope: 'single'}. Assert HTTP 200 and answer non-empty. (g) test_library_overview_includes_all_three_books: POST /summarize/all with mode=executive and force_refresh=true. Assert HTTP 200 and streaming response contains content referencing at least two of the three book titles. PERFORMANCE TEST \u2014 tests/test_v2_ingestion_perf.py. This test is slow and skipped in CI unless LUMINARY_PERF_TESTS=1 env var is set (use pytest.mark.skipif). When enabled: (a) test_section_summarize_node_under_5_minutes: reset section_summaries for the Iliad, run SectionSummarizerService.generate() on the Iliad document, time it, assert completion in under 300 seconds. (b) test_pregenerate_fast_path_under_3_minutes: ensure section summaries exist, delete existing SummaryModel rows for Iliad, call pregenerate() on Iliad, time it, assert completion in under 180 seconds. MAKEFILE \u2014 Add target 'test-v2': runs 'cd backend && uv run pytest tests/test_v2_pipeline.py -v'. Add to Makefile alongside existing 'test-books-all'. The perf tests are NOT part of 'make test-v2' \u2014 they require LUMINARY_PERF_TESTS=1. QUALITY SCORE UPDATE \u2014 After all tests pass, invoke the luminary-docs agent to update docs/QUALITY_SCORE.md. Changes required: Q&A row: grade B -> A, notes 'v2 agentic router: intent classification, 4 strategy nodes, section-aware context packing'. Ingestion row: notes 'v2: section-level summarization, fast-path document summary, sub-5min for Iliad'. Add a new 'Last Updated' line for v2. TECH DEBT \u2014 Add TD-011 to tech-debt-tracker.md: 'pack_context() uses a word-count token approximation (word_count * 1.3). Replace with tiktoken or litellm.token_counter for exact counts, especially if context windows larger than 4k are used.' Priority: low. SMOKE \u2014 scripts/smoke/S80.sh: sequence of 4 POST /qa calls with different intent types (summary, factual, relational, comparative), assert all return HTTP 200 and non-empty answer. Assert GET /summarize/{iliad_id}/sections returns non-empty array. Exit 0.",
      "acceptanceCriteria": [
        "tests/test_v2_pipeline.py contains all 7 integration tests listed above",
        "test_section_summaries_generated_for_all_books: each of the 3 corpus books has >= 10 section summary rows",
        "test_document_summary_uses_fast_path: each book has a '_section_reduce' SummaryModel row",
        "test_intent_routing_comparative: POST /qa comparative question returns HTTP 200",
        "test_intent_routing_relational: POST /qa relational question returns HTTP 200",
        "test_library_overview_includes_all_three_books passes",
        "tests/test_v2_ingestion_perf.py has both perf tests with LUMINARY_PERF_TESTS guard",
        "make test-v2 target added to Makefile, runs test_v2_pipeline.py",
        "docs/QUALITY_SCORE.md updated: Q&A grade A, ingestion notes updated",
        "TD-011 added to tech-debt-tracker.md",
        "uv run pytest tests/test_v2_pipeline.py exits 0 (with real books ingested)",
        "scripts/smoke/S80.sh exits 0",
        "test_intent_routing_factual: mock LLM returns a response string that includes the citation JSON block (e.g. '\\n{\"citations\":[{\"section_heading\":\"Chapter 1\",\"page\":1,\"excerpt\":\"...\"}],\"confidence\":\"high\"}') \u2014 done event assertions check that citations list is non-empty and first citation has a non-empty section_heading",
        "test_intent_routing_summary: POST /qa with question='summarize this document' and a document that has a cached executive summary returns HTTP 200, done event has non-empty answer and confidence='high' (no LLM call needed when summary is cached)",
        "test_keyword_summary_word: classify_intent_heuristic('Please give me a summary of this') returns ('summary', 0.9) \u2014 unit test, no DB required",
        "test_streaming_not_buffered: with mock LLM yielding tokens one-by-one via async generator, first SSE 'token' event is yielded by stream_answer() before the mock generator has yielded all its tokens \u2014 verifies streaming is not batched"
      ],
      "passes": true
    },
    {
      "id": "S81",
      "title": "Confidence-adaptive retry: augment context with complementary strategies when confidence is low",
      "phase": "V2",
      "priority": 81,
      "featureRef": "V2-adaptive-retrieval",
      "description": "CONTEXT: The current chat graph always ends after one synthesize call. If the LLM returns confidence='low' (meaning it lacked sufficient context), the answer is returned as-is. This story adds a confidence check gate after synthesize_node that triggers a second retrieval pass using a complementary strategy, then re-synthesizes with the combined richer context.\n\nCURRENT GRAPH TOPOLOGY:\n  classify_node \u2192 route_node \u2192 [summary|graph|comparative|search]_node \u2192 synthesize_node \u2192 END\n\nNEW TOPOLOGY AFTER THIS STORY:\n  classify_node \u2192 route_node \u2192 [strategy]_node \u2192 synthesize_node\n    \u2192 confidence_gate_node\n      \u2192 (high|medium confidence, OR retry_attempted=True) \u2192 END\n      \u2192 (low confidence, retry_attempted=False) \u2192 augment_node\n        \u2192 synthesize_node (re-runs with appended context)\n        \u2192 confidence_gate_node \u2192 END (always, retry_attempted=True)\n\nCOMPLEMENTARY STRATEGY MAPPING (augment_node selects based on primary_strategy):\n  'search' or 'factual' or 'exploratory' \u2192 add Kuzu graph entity relationships\n    (run _query_kuzu_for_entity for entities extracted from the question)\n  'relational' \u2192 add hybrid search results (k=15, broader)\n  'summary' \u2192 add hybrid search results (k=10)\n  'comparative' \u2192 add hybrid search results with no doc_id filter (k=10)\n\nCONTEXT MERGING: augment_node APPENDS to state['chunks'] and state['section_context'] (does not replace). pack_context() in synthesize_node handles deduplication and token budget enforcement automatically \u2014 no special merge logic needed.\n\nLOOP PREVENTION: ChatState gains retry_attempted: bool (default False). route_node sets primary_strategy in state. augment_node sets retry_attempted=True. confidence_gate_node checks retry_attempted before re-routing \u2014 guarantees at most 1 retry.\n\nGRAPH KNOWLEDGE WHEREVER NECESSARY: This story ensures graph entity context supplements every factual/exploratory/search answer when the LLM signals insufficient context, not just when the user explicitly asks a relational question.",
      "acceptanceCriteria": [
        "ChatState in app/types.py gains two new fields: retry_attempted: bool (default False) and primary_strategy: str | None (default None)",
        "route_node (the conditional edge function) sets state['primary_strategy'] to the node name it dispatches to (e.g. 'search_node', 'graph_node', 'summary_node', 'comparative_node')",
        "confidence_gate_node added as a real graph node (not just a conditional edge function): reads state['confidence'] and state['retry_attempted']; returns {} to proceed to END or {} to proceed to augment_node",
        "confidence_gate_node routes to END when: confidence in ('high', 'medium') OR retry_attempted is True",
        "confidence_gate_node routes to augment_node when: confidence == 'low' AND retry_attempted is False",
        "augment_node selects complementary strategy based on primary_strategy: search|factual|exploratory \u2192 Kuzu entity graph lines appended to section_context; relational \u2192 hybrid search k=15 appended to chunks; summary \u2192 hybrid search k=10 appended to chunks; comparative \u2192 hybrid search k=10 no doc_id filter appended to chunks",
        "augment_node sets retry_attempted=True in its return dict before returning",
        "augment_node APPENDS to existing state['chunks'] (list concatenation) and APPENDS to state['section_context'] (string join with separator) \u2014 never overwrites",
        "augment_node is non-fatal: all errors (Kuzu offline, retrieval failure) are caught and logged; returns {retry_attempted: True} with no context changes rather than raising",
        "synthesize_node is re-used unchanged \u2014 it is wired to both the first-pass and retry-pass edges; pack_context() handles deduplication of appended chunks automatically",
        "Graph topology: synthesize_node \u2192 confidence_gate_node \u2192 (conditional) \u2192 [END | augment_node \u2192 synthesize_node \u2192 confidence_gate_node \u2192 END]",
        "Singleton _compiled_graph is reset to None when build_chat_graph() is called \u2014 changing graph topology without restarting the server works correctly in tests via monkeypatching get_chat_graph",
        "test_low_confidence_triggers_retry: first synthesize_node call returns confidence='low'; assert augment_node runs and second synthesize_node call occurs; final response has retry_attempted=True in state",
        "test_medium_confidence_skips_retry: first synthesize_node call returns confidence='medium'; assert augment_node does NOT run; END reached after first synthesis",
        "test_no_double_retry: second synthesize_node call (post-augment) returns confidence='low'; assert flow ends at confidence_gate_node without a third synthesis call",
        "test_augment_appends_chunks: augment_node for primary_strategy='search_node' returns dict with chunks list that is LONGER than the original state chunks (graph context added)",
        "test_augment_non_fatal: Kuzu connection raises Exception; augment_node returns {retry_attempted: True} without raising; test asserts no exception propagates",
        "test_graph_knowledge_supplements_factual: end-to-end mock test where search intent returns confidence='low'; augment_node adds Kuzu entity lines to section_context; re-synthesis receives richer context",
        "uv run pytest tests/test_confidence_retry.py exits 0",
        "uv run pytest tests/test_chat_graph.py exits 0 (no regression in existing graph tests)",
        "scripts/smoke/S81.sh: POST /qa with a question that triggers low confidence (mock) returns HTTP 200 and done event with retry_attempted visible in response metadata or logs",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S82",
      "title": "Bug: Key Points tab shows per-passage summaries instead of thematic key points",
      "phase": "V2 Phase 2 - Quality Fixes",
      "priority": 82,
      "featureRef": "F-SUMMARY-QUALITY",
      "description": "CONTEXT: The 'Key Points' tab in the document reader (mode='executive') calls GET /summarize/{doc_id} with mode=executive. Since S76, the fast path feeds the concatenated section summaries from SectionSummaryModel as input to the executive LLM call. Each section summary is a literal 1-2 sentence description of that passage. When the LLM receives 50-100 such mini-summaries as input, it lists them as individual bullet points (e.g. '1. Holmes investigates a missing coronet... 2. Miss Hunter seeks help... 3. Project Gutenberg is a non-profit...') rather than synthesising overarching thematic key points. Additionally, metadata/licensing sections (Project Gutenberg terms, copyright notices, disclaimers) are included in section_summaries even though the summarisation prompt already says to ignore them -- this is because the section summary for those sections explicitly mentions Gutenberg, so when it becomes the LLM input the instruction is ineffective. ROOT CAUSE: Two layers of failure: (1) SectionSummarizerService.generate() does not filter out metadata/legal sections before generating per-section summaries. (2) The executive mode prompt does not clearly instruct the LLM to synthesise across summaries rather than list them. FIX LAYER 1 -- Metadata section filter in SectionSummarizerService: Add a pure function _is_metadata_section(heading: str, text: str) -> bool to app/services/section_summarizer.py. It returns True if the heading or the first 500 chars of text contain ANY of these case-insensitive signals: ['project gutenberg', 'terms of use', 'license', 'disclaimer', 'copyright', 'trademark', 'legal', 'distribution', 'reproduction', 'permitted use', 'electronic work', 'archive foundation']. In generate(), skip any qualifying section where _is_metadata_section(section.heading, section.preview) is True, BEFORE the preview length check. Add a log line at DEBUG level: 'Skipping metadata section: %s', heading. FIX LAYER 2 -- Metadata filter in _build_section_summary_input(): In SummarizationService._build_section_summary_input(), after fetching all SectionSummaryModel rows, filter out any row where _is_metadata_section(row.heading, row.content) is True. Import _is_metadata_section from app/services/section_summarizer. If the filtered list has fewer than 3 rows, return None (fall through to slow path -- avoids generating an executive summary from metadata only). FIX LAYER 3 -- Executive prompt improvement: In summarizer.py MODE_INSTRUCTIONS['executive'], replace the existing instruction with: 'Identify the 3 to 5 most important overarching themes, arguments, or narrative ideas that run through the entire work. Write each as a concise bullet point. Do NOT list individual chapter or passage summaries -- synthesise across them. Focus on ideas, arguments, character arcs, and findings. Ignore copyright notices, licensing terms, and distribution metadata. ' + _MARKDOWN_INSTRUCTION. IMPORTANT: The _is_metadata_section function must be importable from section_summarizer without circular imports. It is a pure function at module level with no imports from other app layers. TESTS -- tests/test_summary_quality.py: (a) test_metadata_section_filtered_in_generate: create sections including one with heading='Terms of Use' and text containing 'Project Gutenberg'; call generate() with mock LiteLLM; assert mock was NOT called for the metadata section. (b) test_metadata_section_filtered_in_build_input: insert SectionSummaryModel rows including one with heading='License' and content='Project Gutenberg terms...'; call _build_section_summary_input(); assert the returned string does NOT contain 'Project Gutenberg'. (c) test_build_input_returns_none_when_only_metadata: insert 2 non-metadata rows and 3 metadata rows; assert return value is None (fewer than 3 qualifying rows). (d) test_executive_prompt_not_listing_summaries: mock LiteLLM to capture the system prompt sent; call pregenerate() with mock section summaries; assert the system prompt contains 'synthesise' or 'overarching' (case-insensitive). (e) test_is_metadata_section_true_for_gutenberg: assert _is_metadata_section('Project Gutenberg License', 'This eBook is for use of anyone...') returns True. (f) test_is_metadata_section_false_for_content: assert _is_metadata_section('Chapter 1', 'Holmes examined the room carefully...') returns False. SMOKE -- scripts/smoke/S82.sh: (1) POST /summarize/{sherlock_doc_id} with mode=executive and force_refresh=true, stream response. (2) Assert HTTP 200. (3) Assert the streaming output does NOT contain 'Project Gutenberg' or 'distribution'. (4) Exit 0.",
      "acceptanceCriteria": [
        "_is_metadata_section(heading, text) -> bool pure function in section_summarizer.py; returns True for Gutenberg/license/copyright signals",
        "SectionSummarizerService.generate() skips sections where _is_metadata_section() returns True, before the preview length check",
        "SummarizationService._build_section_summary_input() filters out SectionSummaryModel rows matching _is_metadata_section()",
        "_build_section_summary_input() returns None when fewer than 3 non-metadata rows remain after filtering",
        "MODE_INSTRUCTIONS['executive'] updated to instruct LLM to synthesise overarching themes, NOT list per-passage summaries",
        "test_metadata_section_filtered_in_generate passes",
        "test_metadata_section_filtered_in_build_input passes",
        "test_build_input_returns_none_when_only_metadata passes",
        "test_is_metadata_section_true_for_gutenberg passes",
        "test_is_metadata_section_false_for_content passes",
        "uv run pytest tests/test_summary_quality.py exits 0",
        "scripts/smoke/S82.sh exits 0 -- streaming executive summary does not contain 'Project Gutenberg' or 'distribution'",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S83",
      "title": "Bug: Low confidence on multi-doc scope queries and missing library summary fallback",
      "phase": "V2 Phase 2 - Quality Fixes",
      "priority": 83,
      "featureRef": "F-CHAT-CONFIDENCE",
      "description": "CONTEXT: Two distinct low-confidence scenarios reported: (1) Multi-doc scope (scope='all') factual queries retrieve chunks from all documents, producing a scattered context the LLM cannot synthesise well, leading to confidence='low'. (2) Summary intent with scope='all' when no LibrarySummaryModel exists: summary_node currently falls through to search_node, which performs a library-wide retrieval -- the LLM receives a huge context window of mixed content and returns low confidence. DIAGNOSIS: In app/runtime/chat_graph.py, the summary_node for scope='all' queries LibrarySummaryModel. If no row exists, it overrides intent to 'factual' and returns, re-routing to search_node. search_node runs hybrid retrieval across all documents (no doc_id filter), retrieving up to k=10 chunks from potentially dozens of documents. The synthesize_node system prompt for 'factual' intent does not tell the LLM it is answering a library-wide question. Additionally, _split_response() in qa.py defaults confidence to 'low' when no JSON block is found in the LLM response -- any response that does not append the citation JSON block is penalised. FIX 1 -- Library summary missing: In summary_node, when scope='all' and no LibrarySummaryModel exists: instead of falling through to search, fire a background task to call POST /summarize/all with mode=executive (using fire_and_forget from the patterns in main.py), then write state['answer'] = 'The library summary is being generated. Please ask again in a moment.', state['confidence'] = 'medium', state['not_found'] = False. Return this state directly -- synthesize_node skips the LLM call when answer is already set (existing pass-through logic from S78 handles this). FIX 2 -- Multi-doc factual context: In search_node, when scope='all', reduce k from 10 to 6 and add a diversify step: ensure at most 2 chunks per document_id in the result set. Implement _cap_per_document(chunks: list[dict], max_per_doc: int = 2) -> list[dict] as a pure function in context_packer.py. FIX 3 -- Confidence default: In qa.py _split_response(), when no JSON block is present but the answer text is non-empty (len(answer) > 80 chars), default confidence to 'medium' (not 'low'). Only return 'low' when answer is empty or len(answer) <= 80 chars. FIX 4 -- Synthesize prompt for multi-doc: In synthesize_node, when scope == 'all' and intent in ('factual', 'exploratory'), append to the system prompt: 'The user is asking about their entire library. Answer using only the provided passages. If the passages come from multiple documents, synthesise across them. Be explicit about which document each point comes from.' TESTS -- tests/test_confidence_fixes.py: (a) test_library_summary_missing_returns_medium: mock LibrarySummaryModel query to return None; run summary_node with scope='all'; assert state['confidence'] == 'medium' and 'being generated' in state['answer']. (b) test_split_response_defaults_medium_for_long_answer: call _split_response with a long answer string (>80 chars) containing no JSON block; assert confidence == 'medium'. (c) test_split_response_defaults_low_for_short_answer: call _split_response('I do not know.'); assert confidence == 'low'. (d) test_cap_per_document_limits_chunks: call _cap_per_document with 6 chunks from 2 documents (3 each), max_per_doc=2; assert len(result) == 4 and each doc_id appears at most twice. (e) test_search_node_caps_multi_doc_chunks: run search_node with scope='all' and mock retrieval returning 5 chunks from the same doc_id; assert output chunks contain at most 2 from that document. SMOKE -- scripts/smoke/S83.sh: (1) POST /qa with scope='all', question='Who is the main character?'; assert HTTP 200, done event confidence != 'low'. (2) POST /qa with scope='all', question='Summarize all documents' when no library summary cached; assert HTTP 200, done event answer is non-empty. Exit 0.",
      "acceptanceCriteria": [
        "summary_node with scope='all' and no LibrarySummaryModel: returns answer='...being generated...' with confidence='medium', fires background /summarize/all",
        "_split_response() returns confidence='medium' (not 'low') when answer text len > 80 chars and no JSON block present",
        "_split_response() returns confidence='low' only when answer is empty or len <= 80 chars",
        "_cap_per_document(chunks, max_per_doc=2) pure function added to context_packer.py",
        "search_node uses _cap_per_document when scope='all' to ensure at most 2 chunks per document_id",
        "search_node reduces k to 6 when scope='all'",
        "synthesize_node system prompt for scope='all' factual/exploratory includes multi-doc attribution instruction",
        "test_library_summary_missing_returns_medium passes",
        "test_split_response_defaults_medium_for_long_answer passes",
        "test_split_response_defaults_low_for_short_answer passes",
        "test_cap_per_document_limits_chunks passes",
        "test_search_node_caps_multi_doc_chunks passes",
        "uv run pytest tests/test_confidence_fixes.py exits 0",
        "scripts/smoke/S83.sh exits 0",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S84",
      "title": "Bug: UI responsiveness -- lazy-load core pages, fix Notes stale time, remove duplicate SSE calls",
      "phase": "V2 Phase 2 - Quality Fixes",
      "priority": 84,
      "featureRef": "F-UI-PERFORMANCE",
      "description": "CONTEXT: Navigation between tabs feels sluggish and some UI elements take visible time to load. Root causes identified by reading App.tsx and Notes.tsx: (1) Chat, Learning, Notes, Study are imported eagerly in App.tsx (lines 18-21) -- only Viz and Monitoring use React.lazy. Chat and Learning have heavy dependency trees (SSE streaming, document reader, library components). (2) Notes.tsx overrides the global staleTime (60_000) with staleTime: 10_000 on both its useQuery calls, causing a refetch on almost every tab switch. (3) LibraryOverview in Learning.tsx calls generate() on mount with no guard -- in React StrictMode (development), double-mount fires two parallel SSE streams for the same mode. (4) Notes list view TableRow has a dead onClick stub comment that creates a no-op event handler on every row render. FIX 1 -- Lazy-load Chat, Learning, Notes, Study in App.tsx: Replace the four direct imports with React.lazy + Suspense. Use the exact pattern already applied to Viz and Monitoring. Each page uses the existing PageSkeleton fallback. The four replacements: const Chat = lazy(() => import('./pages/Chat')), const Learning = lazy(() => import('./pages/Learning')), const Notes = lazy(() => import('./pages/Notes')), const Study = lazy(() => import('./pages/Study')). Wrap each Route element in <Suspense fallback={<PageSkeleton />}>. Add a comment in App.tsx listing all lazy-loaded pages. FIX 2 -- Notes stale time: In Notes.tsx, remove the explicit staleTime: 10_000 override from the notes list useQuery and the notes-groups useQuery. Let them inherit the global 60_000 default from QueryClient defaultOptions. FIX 3 -- LibraryOverview mount guard: In Learning.tsx LibraryOverview component, add const generatedModes = useRef(new Set<string>()). In the generate() function, at the top: if (generatedModes.current.has(mode) && !forceRefresh) return. After starting the stream: generatedModes.current.add(mode). This prevents duplicate SSE calls on React StrictMode double-mount. The forceRefresh parameter bypasses the guard so manual refresh still works. FIX 4 -- Notes list view onClick stub: In Notes.tsx, the TableRow onClick is a dead comment stub. Replace it with onClick={() => setEditingNote(note)} where editingNote is new state const [editingNote, setEditingNote] = useState<Note | null>(null). When editingNote is set, render a NoteEditorTabs component below the table in a bordered card with a close button. This is a minimal fix to remove the dead stub -- the full focused dialog is S87. TESTS -- No new pytest tests required (UI-only changes). TypeScript must pass: npx tsc --noEmit exits 0. npm run build from frontend/ exits 0. SMOKE -- scripts/smoke/S84.sh: (1) npm run build from frontend/ -- exit 0 required. (2) npx tsc --noEmit from frontend/ -- exit 0 required. Exit 0.",
      "acceptanceCriteria": [
        "Chat, Learning, Notes, Study converted to React.lazy with Suspense and PageSkeleton fallback in App.tsx",
        "Notes.tsx staleTime: 10_000 override removed from notes list and notes-groups useQuery calls",
        "LibraryOverview generate() guarded with useRef Set -- duplicate SSE call on double-mount is prevented; forceRefresh bypasses guard",
        "Notes list view TableRow onClick no longer a dead stub -- sets editingNote state",
        "npx tsc --noEmit exits 0",
        "npm run build exits 0",
        "scripts/smoke/S84.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S85",
      "title": "Feature: RAGAS eval results panel in Monitoring tab with backend trigger endpoint",
      "phase": "V2 Phase 2 - Evaluation",
      "priority": 85,
      "featureRef": "F-RAGAS-MONITORING",
      "description": "CONTEXT: RAGAS evaluation already exists as a standalone script at evals/run_eval.py. It reads golden datasets from evals/golden/, runs RAGAS metrics (answer_relevancy, context_precision, context_recall, faithfulness), and appends scores to evals/scores_history.jsonl. This story integrates these results into the Monitoring tab so users can see eval quality at a glance and trigger re-runs from the UI. The existing evals/ setup must not be modified -- the backend reads from the same files the CLI script writes to. BACKEND -- Add two endpoints to a new router app/routers/evals.py. Register the router in app/main.py with prefix='/evals'. Endpoint 1: GET /evals/results. Reads REPO_ROOT/evals/scores_history.jsonl where REPO_ROOT = Path(__file__).resolve().parent.parent.parent.parent (four levels up from app/routers/evals.py reaches the monorepo root). Returns the most recent result per dataset as a JSON array: [{dataset: str, run_at: str (ISO), hit_rate_5: float | null, mrr: float | null, faithfulness: float | null, context_precision: float | null, context_recall: float | null, answer_relevancy: float | null, passed_thresholds: bool | null}]. If the file does not exist, return []. Parse each line as JSON and skip malformed lines. Group by dataset, take the row with the latest run_at. Endpoint 2: POST /evals/run. Accepts Pydantic body: EvalRunRequest with fields dataset: Literal['book', 'paper', 'conversation', 'notes', 'code'] and assert_thresholds: bool = False. Returns HTTP 202 immediately with {status: 'started', dataset: dataset}. Fires a background task using fire_and_forget that calls asyncio.to_thread(subprocess.run, cmd, cwd=..., capture_output=True) where cmd = ['uv', 'run', 'python', 'run_eval.py', '--dataset', dataset] optionally appending ['--assert-thresholds']. On completion, log stdout/stderr at DEBUG level. IMPORTANT: asyncio.to_thread is required -- subprocess.run blocks the event loop; never use it directly in an async function without to_thread. FRONTEND -- In Monitoring.tsx, add an 'Eval Results' section. Component EvalPanel: (1) useQuery(['eval-results'], fetchEvalResults, {staleTime: 120_000}) where fetchEvalResults calls GET /evals/results. (2) Render a table: columns Dataset | Hit Rate @5 | MRR | Faithfulness | Context Precision | Last Run | Actions. Each numeric score rendered with a coloured indicator: >= threshold shows a green check icon + value, < threshold shows an amber warning icon + value, null shows a grey dash. Thresholds: hit_rate_5 >= 0.60, mrr >= 0.45, faithfulness >= 0.65 (match evals/run_eval.py THRESHOLDS). (3) 'Run Eval' button per row calls POST /evals/run {dataset: row.dataset}. Shows Loader2 spinner when mutation isPending. On success, show a sonner toast 'Evaluation started for {dataset}'. After 5 seconds, invalidate ['eval-results'] query via setTimeout + queryClient.invalidateQueries. (4) Loading state: 3 Skeleton rows. Error state: amber alert 'Could not load eval results'. Empty state: 'No evaluations run yet. Click Run Eval to start.' TESTS -- tests/test_evals_router.py: (a) test_get_results_empty_when_no_file: GET /evals/results when scores_history.jsonl does not exist returns HTTP 200 and []. (b) test_get_results_returns_latest_per_dataset: write a scores_history.jsonl with 2 book entries (different run_at) and 1 paper entry to a tmp dir; mock REPO_ROOT; GET /evals/results returns 2 items (one per dataset, most recent book entry selected). (c) test_post_run_returns_202: POST /evals/run {dataset: 'book'} returns HTTP 202 and body {status: 'started'}. (d) test_post_run_invalid_dataset: POST /evals/run {dataset: 'invalid'} returns HTTP 422. SMOKE -- scripts/smoke/S85.sh: (1) GET /evals/results returns HTTP 200 and a JSON array. (2) POST /evals/run {dataset: 'book'} returns HTTP 202. Exit 0.",
      "acceptanceCriteria": [
        "GET /evals/results returns HTTP 200 with latest result per dataset; returns [] when scores_history.jsonl absent",
        "POST /evals/run returns HTTP 202 immediately and fires background asyncio.to_thread(subprocess.run, ...) -- never blocks event loop",
        "POST /evals/run with invalid dataset returns HTTP 422",
        "evals router registered in app/main.py with prefix='/evals'",
        "EvalPanel in Monitoring.tsx renders table with Hit Rate @5, MRR, Faithfulness, Context Precision columns",
        "Scores coloured green/amber/grey based on threshold comparison (thresholds match evals/run_eval.py values)",
        "'Run Eval' button per row calls POST /evals/run and shows sonner toast on success",
        "Loading / error / empty states present in EvalPanel",
        "test_get_results_empty_when_no_file passes",
        "test_get_results_returns_latest_per_dataset passes",
        "test_post_run_returns_202 passes",
        "test_post_run_invalid_dataset returns HTTP 422",
        "uv run pytest tests/test_evals_router.py exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S85.sh exits 0",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S86",
      "title": "Feature: Entity disambiguation -- canonical name normalisation before Kuzu upsert",
      "phase": "V2 Phase 2 - Knowledge Graph",
      "priority": 86,
      "featureRef": "F-ENTITY-DISAMBIGUATION",
      "description": "CONTEXT: The knowledge graph contains duplicate nodes for the same real-world entity because GLiNER extracts surface-form variants: 'Holmes', 'Sherlock Holmes', 'Mr. Holmes' become three separate PERSON nodes. These duplicates fragment the graph, lower retrieval relevance, and make entity-based navigation confusing. The disambiguation pass handles three cases: honorific prefixes (Mr., Mrs., Dr., Sir, Miss, Lady, Lord, Prof.), substring containment (shorter name contained in longer), and token overlap (2+ shared tokens). NOTE: 'Sr. Holmes' is intentionally distinct from 'Sherlock Holmes' -- 'Sr.' is not an honorific in _HONORIFICS; only substring containment or token overlap would merge it. NEW FILE -- Create app/services/entity_disambiguator.py. HONORIFIC STRIPPING: Define _HONORIFICS = frozenset({'mr', 'mrs', 'ms', 'dr', 'sir', 'miss', 'lady', 'lord', 'prof', 'professor', 'rev', 'captain', 'capt', 'col', 'gen', 'sgt', 'lt'}). Pure function _strip_honorifics(name: str) -> str: lowercase the name, split on whitespace, drop any leading token that is in _HONORIFICS after stripping trailing punctuation. Return remaining tokens joined by space. CANONICAL SELECTION: Method find_canonical(name: str, entity_type: str, existing_names: list[str]) -> str: (1) Compute stripped = _strip_honorifics(name). (2) For each existing in existing_names: compute stripped_existing = _strip_honorifics(existing). (3) RULE A -- Exact stripped match (case-insensitive): return existing. (4) RULE B -- Substring containment: if stripped is a substring of stripped_existing OR vice versa, AND (both have >= 2 tokens OR the shorter has >= 5 chars): return the longer of (name, existing). (5) RULE C -- Token overlap: if the sets of lowercase tokens share >= 2 tokens: return the longer of (name, existing). (6) If no match: return name as its own canonical. Method canonicalize_batch(entities: list[dict], entity_type: str) -> list[dict]: process entities of the given type in order. Maintain running dict canonical_map keyed by _strip_honorifics(canonical_name). For each entity, call find_canonical against canonical_map.values(); update canonical_map; update entity['name'] to resolved canonical. Return updated list. INTEGRATION -- In app/services/graph.py, in the method that writes entities to Kuzu (find the upsert/MERGE loop), after receiving the extracted entity list and before any Kuzu write, instantiate EntityDisambiguator and call canonicalize_batch for each of PERSON, ORGANIZATION, PLACE. The Kuzu MERGE must use the canonical name. ALIASES -- When a non-canonical name is merged to a canonical, append the non-canonical name to a node aliases property stored as a comma-separated string (since Kuzu embedded may not support array properties natively -- check existing Kuzu schema and follow the same pattern used for other multi-value fields). Add the aliases property to the entity node schema in db_init.py or wherever the Kuzu schema is initialised. TESTS -- tests/test_entity_disambiguator.py: (a) test_honorific_stripped_resolves_to_existing: find_canonical('Mr. Holmes', 'PERSON', ['Sherlock Holmes']) -- stripped 'holmes' is substring of 'sherlock holmes' (Rule B) -- returns 'Sherlock Holmes'. (b) test_direct_substring: find_canonical('Holmes', 'PERSON', ['Sherlock Holmes']) returns 'Sherlock Holmes'. (c) test_longer_name_wins: find_canonical('Sherlock Holmes', 'PERSON', ['Holmes']) returns 'Sherlock Holmes'. (d) test_no_match_different_person: find_canonical('Watson', 'PERSON', ['Holmes']) returns 'Watson'. (e) test_sr_not_in_honorifics: assert 'sr' not in _HONORIFICS; find_canonical('Sr. Holmes', 'PERSON', ['Sherlock Holmes', 'Holmes']) -- 'sr. holmes' is not a substring of 'sherlock holmes'; token overlap {'sr.', 'holmes'} vs {'sherlock', 'holmes'} = 1 shared token (less than 2) -- returns 'Sr. Holmes' (distinct). (f) test_canonicalize_batch_deduplicates_variants: input [{'name': 'Holmes', 'entity_type': 'PERSON'}, {'name': 'Mr. Holmes', 'entity_type': 'PERSON'}, {'name': 'Sherlock Holmes', 'entity_type': 'PERSON'}]; assert all three resolve to 'Sherlock Holmes'. (g) test_entity_type_boundary: PLACE 'London' does not merge with PERSON 'Jack London' -- entity_type filter prevents cross-type matches. SMOKE -- scripts/smoke/S86.sh: (1) Ingest a small fixture doc containing 'Holmes', 'Mr. Holmes', 'Sherlock Holmes'. (2) GET /graph/entities?document_id={id}. (3) Assert response JSON does not contain both 'Holmes' and 'Sherlock Holmes' as separate PERSON entries. Exit 0.",
      "acceptanceCriteria": [
        "app/services/entity_disambiguator.py created with _strip_honorifics(), find_canonical(), canonicalize_batch()",
        "_HONORIFICS covers: mr, mrs, ms, dr, sir, miss, lady, lord, prof, professor, rev, captain, capt, col, gen, sgt, lt -- 'sr' is NOT included",
        "find_canonical() applies Rule A (exact stripped), Rule B (substring), Rule C (token overlap >= 2) in that order",
        "Longer name wins when substring containment is detected",
        "canonicalize_batch called in graph.py for PERSON, ORGANIZATION, PLACE before Kuzu MERGE",
        "Kuzu MERGE uses canonical name; non-canonical names appended to aliases property",
        "test_honorific_stripped_resolves_to_existing passes",
        "test_direct_substring passes",
        "test_sr_not_in_honorifics: 'sr' not in _HONORIFICS; 'Sr. Holmes' does not resolve to 'Sherlock Holmes'",
        "test_canonicalize_batch_deduplicates_variants: all three variants resolve to 'Sherlock Holmes'",
        "test_entity_type_boundary: PLACE does not merge with PERSON",
        "uv run pytest tests/test_entity_disambiguator.py exits 0",
        "scripts/smoke/S86.sh exits 0",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S87",
      "title": "Feature: Notes focused editor dialog with side-by-side Write/Preview for list and card views",
      "phase": "V2 Phase 2 - UX",
      "priority": 87,
      "featureRef": "F-NOTES-EDITOR",
      "description": "CONTEXT: The Notes page has two views: card (NoteCard grid with inline edit) and list (Table). Problems: (1) List view TableRow has a dead onClick stub -- clicking a row does nothing. (2) NoteCard inline edit collapses the card into a textarea, hiding all surrounding context. (3) Neither view offers simultaneous Write + Preview. The user wants a focused dialog where typing in the Write pane instantly updates the Preview pane side-by-side. NEW COMPONENT -- Create frontend/src/components/NoteEditorDialog.tsx. Use shadcn/ui Dialog (Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from '@/components/ui/dialog'). Props: { note: Note | null, onClose: () => void, onSaved: (updated: Note) => void }. Dialog is open when note is not null. DialogContent: max-w-4xl, h-[70vh], overflow-hidden. Inside: two-column grid (grid-cols-2 gap-0 h-full). Left column header 'Write', right column header 'Preview', separated by a vertical border. Left pane: textarea full-height, monospace font, controlled by local state const [content, setContent] = useState(note?.content ?? ''). The textarea re-initialises from note.content when note prop changes (useEffect with note?.id as dep). Right pane: MarkdownRenderer rendering content in real time. DialogHeader: title = first 60 chars of note.content or 'Untitled note'. DialogFooter: 'Save' button calls patchNote(note.id, {content}) -- disabled when content === note.content or mutation isPending, shows Loader2 when pending. 'Cancel' button calls onClose(). On save success: call onSaved(updatedNote) then onClose(). On save error: show inline error 'Failed to save. Please try again.' in footer. Keyboard shortcuts: Ctrl+S / Cmd+S triggers Save (useEffect window keydown listener, active only when note is not null). Tags (read-only): displayed below Write pane as small tag chips. Group name (read-only): displayed below tags. No editing of tags or group in this dialog. INTEGRATION -- LIST VIEW: In Notes.tsx, add state: const [editingNote, setEditingNote] = useState<Note | null>(null). In list view TableRow, replace the dead onClick stub with onClick={() => setEditingNote(note)}. Add an Actions column to the table (rightmost): TableHead 'Actions'. In each TableRow's Actions TableCell, render a Pencil icon button (size=14) that also calls setEditingNote(note). Render <NoteEditorDialog note={editingNote} onClose={() => setEditingNote(null)} onSaved={(updated) => { setEditingNote(null); void qc.invalidateQueries({queryKey: ['notes']}) }} /> once at the Notes page level, outside the table. INTEGRATION -- CARD VIEW: Add prop onEdit: () => void to NoteCard interface. In NoteCard, remove local editing state (editing, setEditing) and remove the NoteEditorTabs block entirely. Replace the Pencil button onClick with () => onEdit(). Replace the content div onClick (currently setEditing(true)) with () => onEdit(). The delete confirmation flow (confirming, deleteMut) is preserved unchanged. In Notes page card view rendering, pass onEdit={() => setEditingNote(note)} to each NoteCard. The same editingNote state and NoteEditorDialog used for both views. NoteEditorDialog is rendered once at the Notes page level. IMPORTANT: NoteEditorDialog must never be rendered inside NoteCard -- it is always at page level to avoid nested Dialog stacking issues. TESTS -- TypeScript: npx tsc --noEmit exits 0. npm run build exits 0. If Vitest is configured, add: test_dialog_opens_with_note_content (render dialog with a note, assert textarea value matches note.content) and test_preview_updates_on_type (simulate typing in textarea, assert MarkdownRenderer receives updated content). SMOKE -- scripts/smoke/S87.sh: (1) npm run build from frontend/. (2) npx tsc --noEmit from frontend/. Exit 0.",
      "acceptanceCriteria": [
        "frontend/src/components/NoteEditorDialog.tsx created using shadcn Dialog with two-column Write/Preview layout",
        "Write pane: full-height monospace textarea, controlled state, re-initialises when note prop changes",
        "Preview pane: MarkdownRenderer updates in real time as user types in Write pane",
        "Ctrl+S / Cmd+S triggers Save when dialog is open (note is not null)",
        "Save calls patchNote(), is disabled when content unchanged, shows Loader2 when pending",
        "On save success: onSaved() called then dialog closed; on error: inline footer error shown",
        "Tags and group_name displayed read-only below Write pane",
        "List view TableRow onClick calls setEditingNote(note) -- dead stub removed",
        "List view has Actions column with Pencil icon button per row also opening NoteEditorDialog",
        "NoteCard: local editing state and NoteEditorTabs removed; Pencil button and content click call onEdit() prop",
        "Delete confirmation flow in NoteCard preserved unchanged",
        "NoteEditorDialog rendered once at Notes page level, not inside NoteCard",
        "npx tsc --noEmit exits 0",
        "npm run build exits 0",
        "scripts/smoke/S87.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S88",
      "title": "Feature: Expand golden datasets to Alice in Wonderland and The Odyssey; corpus-driven integration tests",
      "phase": "V2 Phase 2 - Evaluation",
      "priority": 88,
      "featureRef": "F-RAGAS-CORPUS",
      "description": "CONTEXT: The RAGAS golden dataset at evals/golden/book.jsonl has 30 entries, all from The Time Machine. Alice in Wonderland and The Odyssey are registered in DATA/books/manifest.json and used in conftest_books.py ingestion fixtures, but have zero golden Q&A coverage. This means RAGAS measures retrieval quality on a single Victorian sci-fi novella -- it will not catch regressions in literary fiction (Alice) or epic poetry/prose translation (Odyssey). This story adds 20 golden entries per missing book and creates a new integration test file that runs retrieval, summarization, and entity extraction assertions against all three corpus books on real ingested data. IMPORTANT: The context_hint field in every golden entry must be a verbatim lowercase substring that genuinely appears in the source text. Ralph must read each source file before writing context_hint values to verify they match. GOLDEN DATASET EXPANSION -- Add exactly 20 new entries to evals/golden/book.jsonl for Alice in Wonderland (source_file: DATA/books/alice_in_wonderland.txt) and 20 new entries for The Odyssey (source_file: DATA/books/the_odyssey.txt). Use the exact same JSONL schema as existing entries: {question, ground_truth_answer, context_hint, document_id, source_file}. Set document_id to 'TBD' -- the RAGAS runner resolves it at runtime. Alice in Wonderland required golden entries (Ralph must verify each context_hint by reading the source file): (1) question='How does Alice enter Wonderland?', ground_truth='She follows a White Rabbit down a rabbit hole', context_hint='rabbit-hole'. (2) question='What does Alice drink to become smaller?', ground_truth='A bottle labelled DRINK ME', context_hint='drink me'. (3) question='What does Alice eat to grow taller?', ground_truth='A small cake labelled EAT ME', context_hint='eat me'. (4) question='Who is the Cheshire Cat?', ground_truth='A cat that can disappear leaving only its grin', context_hint='cheshire cat'. (5) question='What game does the Queen of Hearts play?', ground_truth='Croquet using flamingos as mallets and hedgehogs as balls', context_hint='croquet'. (6) question='Who is the Mad Hatter?', ground_truth='A character who holds a perpetual tea party', context_hint='hatter'. (7) question='Who is accused of stealing the tarts?', ground_truth='The Knave of Hearts', context_hint='knave of hearts'. (8) question='What does the Queen of Hearts frequently shout?', ground_truth='Off with their heads', context_hint='off with their heads'. (9) question='Who paints the roses red?', ground_truth='Playing card gardeners who accidentally planted white roses and try to hide the mistake', context_hint='painting them red'. (10) question='What is the name of Alice cat?', ground_truth='Dinah', context_hint='dinah'. (11) question='What creature does the Duchess carry as a baby?', ground_truth='A baby that turns into a pig', context_hint='turned into a pig'. (12) question='What is the Caterpillar smoking?', ground_truth='A hookah', context_hint='hookah'. (13) question='What does the Mock Turtle teach?', ground_truth='Reading, Writing, and Arithmetic along with Uglification and Derision', context_hint='mock turtle'. (14) question='How does Alice grow too large for the White Rabbit house?', ground_truth='She drinks from a bottle inside the house and grows until she fills the room', context_hint='too large'. (15) question='Who are at the Mad Tea Party?', ground_truth='The Mad Hatter, the March Hare, and the Dormouse', context_hint='march hare'. (16) question='What verdict does the King try to pronounce before the jury deliberates?', ground_truth='Sentence first, verdict afterwards', context_hint='sentence first'. (17) question='Who is the White Rabbit?', ground_truth='A rabbit carrying a watch who leads Alice into Wonderland', context_hint='white rabbit'. (18) question='What dance do the Mock Turtle and Gryphon demonstrate?', ground_truth='The Lobster Quadrille', context_hint='lobster quadrille'. (19) question='What exclamation does Alice make when things grow strange?', ground_truth='Curiouser and curiouser', context_hint='curiouser and curiouser'. (20) question='Who does the Cheshire Cat say is mad in Wonderland?', ground_truth='Everyone, including Alice', context_hint='we are all mad here'. The Odyssey required golden entries (Ralph must verify each context_hint): (1) question='Who is Odysseus?', ground_truth='King of Ithaca, hero of the Trojan War, trying to sail home', context_hint='ithaca'. (2) question='What is the name of the Cyclops Odysseus blinds?', ground_truth='Polyphemus', context_hint='polyphemus'. (3) question='How does Odysseus escape the Cyclops cave?', ground_truth='By hiding under the bellies of sheep as they leave', context_hint='sheep'. (4) question='Who is Penelope?', ground_truth='Odysseus faithful wife who waits in Ithaca', context_hint='penelope'. (5) question='What trick does Penelope use to delay the suitors?', ground_truth='She weaves a shroud by day and secretly unravels it at night', context_hint='weaving'. (6) question='Who is Telemachus?', ground_truth='The son of Odysseus and Penelope', context_hint='telemachus'. (7) question='Which goddess assists Odysseus throughout his journey?', ground_truth='Athena', context_hint='athena'. (8) question='Which god opposes Odysseus return home?', ground_truth='Poseidon, angry because Odysseus blinded his son Polyphemus', context_hint='poseidon'. (9) question='Who is Circe?', ground_truth='A sorceress on the island of Aeaea who turns men into pigs', context_hint='circe'. (10) question='What does Circe do to Odysseus crew?', ground_truth='Turns them into pigs', context_hint='pigs'. (11) question='What danger do the Sirens pose?', ground_truth='Their irresistible song lures sailors to their deaths', context_hint='sirens'. (12) question='How does Odysseus survive the Sirens song?', ground_truth='He has himself tied to the mast while his crew plugs their ears with wax', context_hint='mast'. (13) question='Where does Calypso keep Odysseus?', ground_truth='On the island of Ogygia for seven years', context_hint='ogygia'. (14) question='Who is Calypso?', ground_truth='A nymph who detains Odysseus on her island and offers him immortality', context_hint='calypso'. (15) question='What is the name of the faithful swineherd who shelters Odysseus?', ground_truth='Eumaeus', context_hint='eumaeus'. (16) question='How does Odysseus disguise himself when he returns to Ithaca?', ground_truth='As a beggar', context_hint='beggar'. (17) question='What contest does Penelope set for the suitors?', ground_truth='To string Odysseus bow and shoot an arrow through twelve axe heads', context_hint='bow'. (18) question='What is the name of Odysseus faithful old dog?', ground_truth='Argos', context_hint='argos'. (19) question='Who are the suitors?', ground_truth='Men who court Penelope and consume Odysseus household while he is away', context_hint='suitors'. (20) question='What wooden device did Odysseus devise to defeat Troy?', ground_truth='The Trojan Horse', context_hint='horse'. INTEGRATION TESTS -- Create backend/tests/test_corpus_qa.py. This file uses pytest_plugins = ['tests.conftest_books'] and the all_books_ingested session fixture. All tests are marked @pytest.mark.slow. Tests: (a) test_alice_factual_retrieval_hr3: for each of 5 selected Alice golden questions (rabbit-hole, drink me, cheshire cat, mad hatter, queen of hearts), run GET /search?q={question}&document_id={alice_doc_id} and assert context_hint substring appears in at least one of the top-3 returned chunk texts. Assert overall HR@3 >= 0.6 (3 of 5 must hit). (b) test_odyssey_factual_retrieval_hr3: same pattern for Odyssey (polyphemus, circe, penelope weaving, argos, sirens). Assert HR@3 >= 0.6. (c) test_alice_known_entities_extracted: GET /graph/entities?document_id={alice_doc_id}, assert response contains PERSON entities including 'Alice' and 'Queen' (case-insensitive). Assert at least 10 distinct entity names total. (d) test_odyssey_known_entities_extracted: assert entities include 'Odysseus', 'Penelope', 'Telemachus'. At least 15 distinct entity names. (e) test_alice_section_summaries_no_gutenberg: GET /summarize/{alice_doc_id}/sections, assert HTTP 200, assert no section summary content contains 'project gutenberg' or 'terms of use' (case-insensitive). Assert at least 5 section summaries returned. (f) test_odyssey_section_summaries_no_gutenberg: same for Odyssey. (g) test_alice_executive_summary_quality: GET /summarize/{alice_doc_id}/cached, assert 'executive' mode summary exists and does not contain numbered list patterns like '1.' at the start of 3+ lines (which would indicate per-passage listing). Assert summary mentions at least one of: 'alice', 'wonderland', 'queen', 'rabbit', 'mad' (case-insensitive). (h) test_odyssey_executive_summary_quality: assert summary mentions at least one of: 'odysseus', 'penelope', 'trojan', 'journey', 'hero' (case-insensitive). (i) test_cross_book_retrieval: GET /search?q=brave hero on a long journey home with no document_id filter; assert results include chunks from at least 2 different document_ids (cross-document retrieval working). (j) test_all_books_have_section_summaries: for each of the 3 books, GET /summarize/{doc_id}/sections and assert at least 10 rows returned (section summaries were generated during ingestion). RAGAS RUNNER UPDATE -- In evals/run_eval.py, update the per-entry document resolution to support multiple source files in the same JSONL. The existing load_manifest()/ensure_ingested() already handles this via the source_file -> document_id map. No change to the runner is needed for multi-book support -- verify this is true by reading the runner code before claiming. If the runner only supports one document per dataset run (e.g. it assumes all entries have the same source_file), fix it to group entries by source_file, resolve each group's document_id separately, and merge results before scoring. SMOKE -- scripts/smoke/S88.sh: (1) Count entries in evals/golden/book.jsonl, assert >= 70. (2) grep alice_in_wonderland evals/golden/book.jsonl, assert at least 20 matches. (3) grep the_odyssey evals/golden/book.jsonl, assert at least 20 matches. Exit 0.",
      "acceptanceCriteria": [
        "evals/golden/book.jsonl contains >= 70 entries total (30 Time Machine + 20 Alice + 20 Odyssey)",
        "All 20 Alice entries have source_file='DATA/books/alice_in_wonderland.txt' and document_id='TBD'",
        "All 20 Odyssey entries have source_file='DATA/books/the_odyssey.txt' and document_id='TBD'",
        "Every context_hint value verified as a verbatim substring in the source file before committing (Ralph reads the file)",
        "test_alice_factual_retrieval_hr3: HR@3 >= 0.6 on 5 Alice factual questions",
        "test_odyssey_factual_retrieval_hr3: HR@3 >= 0.6 on 5 Odyssey factual questions",
        "test_alice_known_entities_extracted: 'Alice' and 'Queen' present in PERSON entities; >= 10 distinct entities",
        "test_odyssey_known_entities_extracted: 'Odysseus', 'Penelope', 'Telemachus' present; >= 15 distinct entities",
        "test_alice_section_summaries_no_gutenberg: no section summary contains 'project gutenberg' or 'terms of use'; >= 5 summaries",
        "test_odyssey_section_summaries_no_gutenberg: same for Odyssey",
        "test_alice_executive_summary_quality: no numbered-list pattern (3+ lines starting '1.', '2.', '3.'); mentions Alice/Wonderland/Queen/rabbit/mad",
        "test_odyssey_executive_summary_quality: mentions Odysseus/Penelope/trojan/journey/hero",
        "test_cross_book_retrieval: broad query returns chunks from >= 2 different document_ids",
        "test_all_books_have_section_summaries: each of 3 books has >= 10 section summary rows",
        "All tests in test_corpus_qa.py marked @pytest.mark.slow",
        "If RAGAS runner assumes single source_file per dataset, it is fixed to support multi-source grouping",
        "uv run pytest tests/test_corpus_qa.py -m slow exits 0 (with books ingested)",
        "scripts/smoke/S88.sh exits 0",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S89",
      "title": "Bug: Notes markdown rendering -- headings and emphasis unstyled, code blocks uncolored",
      "phase": "V2 Phase 3 - Notes Intelligence",
      "priority": 89,
      "featureRef": "F-NOTES-MARKDOWN",
      "description": "CONTEXT: MarkdownRenderer wraps react-markdown with remark-gfm and rehype-highlight, and applies Tailwind prose classes (prose prose-sm dark:prose-invert max-w-none). Markdown headings (# H1, ## H2), bold (**text** or __text__), italic, and blockquotes render as unstyled plain text. Code blocks have token class names but no colors. ROOT CAUSE 1 -- @tailwindcss/typography is absent from frontend/package.json devDependencies and absent from the plugins array in tailwind.config.cjs. Without this plugin, the prose-* classes compile to nothing. Verify: grep -r \"tailwindcss/typography\" frontend/ returns empty. ROOT CAUSE 2 -- highlight.js CSS theme is not imported. frontend/src/main.tsx imports only \"./index.css\". frontend/src/index.css has no highlight.js import. rehype-highlight adds CSS class names but no stylesheet provides the colors. Verify: grep -r \"highlight.js\" frontend/src/ returns empty. FIX STEP 1: from frontend/ run: npm install -D @tailwindcss/typography. FIX STEP 2: in frontend/tailwind.config.cjs change plugins: [] to plugins: [require(\"@tailwindcss/typography\")]. FIX STEP 3: add the following line at the top of frontend/src/index.css: @import \"highlight.js/styles/github-dark.css\"; -- highlight.js is already in dependencies so no npm install needed. FIX STEP 4: run npx tsc --noEmit and npm run build from frontend/ to confirm no regressions. No backend changes. SMOKE -- scripts/smoke/S89.sh: (1) cd frontend && npm run build exits 0. (2) grep \"@tailwindcss/typography\" tailwind.config.cjs asserts match. (3) grep \"highlight.js\" src/index.css asserts match. Exit 0.",
      "acceptanceCriteria": [
        "@tailwindcss/typography added to frontend/package.json devDependencies",
        "tailwind.config.cjs plugins array contains require(\"@tailwindcss/typography\")",
        "@import highlight.js theme added to frontend/src/index.css",
        "npm run build from frontend/ exits 0",
        "npx tsc --noEmit from frontend/ exits 0",
        "scripts/smoke/S89.sh exits 0",
        "No backend files changed"
      ],
      "passes": true
    },
    {
      "id": "S90",
      "title": "Bug+Feature: Notes auto-tagging -- tags editable, suggestions visible, offline graceful",
      "phase": "V2 Phase 3 - Notes Intelligence",
      "priority": 90,
      "featureRef": "F-NOTES-AUTOTAG",
      "description": "CONTEXT: S90 was marked passes=true but user verification found: (1) no tag suggestions ever appeared after saving a note; (2) no progress indicator while suggestions were being fetched; (3) no way to cancel a pending suggestion fetch; (4) existing tags were read-only in the editor -- no way to add or remove a tag from the dialog. ROOT CAUSES identified by reading the implementation: (A) The create note form in Notes.tsx uses a plain tagsRaw text input and calls POST /notes directly -- suggest-tags is never fired after note creation, only after editing in NoteEditorDialog. New notes never receive suggestions. (B) If Ollama is offline, ServiceUnavailableError is caught and [] is returned. The dialog sees novel.length==0 and immediately calls onClose() with no feedback. The user never learns that auto-suggest was attempted and failed. (C) In NoteEditorDialog the tags section renders current note.tags as read-only chips (no X to remove, no input to add). This story fixes all three issues. BACKEND -- No changes needed. The POST /notes/{note_id}/suggest-tags endpoint is correct. FRONTEND CHANGE 1 -- EDITABLE TAGS IN NoteEditorDialog: Replace the read-only tags section (the div below the Write pane) with a TagEditor inline component (no separate file needed, define inside NoteEditorDialog.tsx). TagEditor manages a local list: const [editTags, setEditTags] = useState<string[]>(note?.tags ?? []). Re-initialise editTags when note.id changes (add to the existing useEffect). Display current editTags as chips each with an X button: clicking X removes the tag from editTags (setEditTags(prev => prev.filter(t => t !== tag))). Below the chips add a small inline text input (placeholder 'Add tag...'): on Enter key or comma key, trim the input value, if non-empty and not already in editTags add it to editTags and clear the input. Tags are saved to the note by passing editTags when calling patchNote -- change the save mutation to patchNote(note.id, {content, tags: editTags}). This means save always persists both content and current tags. FRONTEND CHANGE 2 -- AUTO-SUGGEST AFTER SAVE (both create and edit): After a successful save, fire POST /notes/{note_id}/suggest-tags. Show progress explicitly: while isFetchingTags is true, show a small Loader2 spinner with text 'Suggesting tags...' in the tags area (visible even if the dialog is about to show the Done button). Do not close the dialog until either (a) suggestions arrive and user interacts, or (b) user clicks Done/Cancel explicitly. If suggest-tags succeeds and returns novel tags (not already in editTags), add them to a suggestedTags list and display them as dashed-border chips the user can click to accept (clicking moves the tag into editTags and calls patchNote({tags: [...editTags, tag]})). A Dismiss link removes the suggestion row. If suggest-tags returns [] (Ollama offline or no suggestions), show a brief inline message in the tags area: 'No suggestions available' for 2 seconds, then hide it. Do not silently close -- always show the Done button and let the user close explicitly after seeing the save-succeeded state. FRONTEND CHANGE 3 -- AUTO-SUGGEST IN CREATE FORM: In Notes.tsx the create form calls createNote() on success via the createMut mutation. In createMut.onSuccess, after the note is created, fire fetchSuggestedTags(created.id). If tags are returned, call patchNote(created.id, {tags: suggestions}) silently in the background. Then invalidate the notes query. No UI needed for this -- the note will appear in the list with tags already applied. This is a fire-and-forget improvement; if it fails, the note still exists with no tags (acceptable). FRONTEND CHANGE 4 -- ABORT ON CLOSE: Use a ref to store the current suggest-tags AbortController: const abortRef = useRef<AbortController | null>(null). In fetchSuggestedTags, pass the signal. When the dialog closes (onOpenChange fires with open=false, or Cancel is clicked), call abortRef.current?.abort(). This prevents the async setState calls on an unmounted component and avoids React warnings. TESTS -- Update backend/tests/test_note_tagger.py to add: test_suggest_tags_api_returns_empty_not_503: mock get_note_tagger to return [] (Ollama down path); POST /notes/{id}/suggest-tags; assert HTTP 200 and tags=[] (not HTTP 503 -- offline is graceful). This verifies the critical invariant that offline does not break the UX contract. Frontend: npx tsc --noEmit and npm run build must pass. SMOKE -- scripts/smoke/S90.sh: (1) Create note via POST /notes body='{\"content\":\"Alice follows the White Rabbit\"}'; capture note_id. (2) POST /notes/{note_id}/suggest-tags; assert HTTP 200 and response has 'tags' key. (3) POST /notes/nonexistent/suggest-tags; assert HTTP 404. Exit 0.",
      "acceptanceCriteria": [
        "NoteEditorDialog tags section is editable: existing tags show as chips with X to remove; inline input to add tags (Enter or comma confirms)",
        "Save mutation sends both content and current editTags to patchNote -- tags are always persisted on save",
        "After save: isFetchingTags=true shows 'Suggesting tags...' with Loader2 in tags area; dialog stays open",
        "If suggest-tags returns novel tags: dashed-border chip suggestions shown; click adds to editTags and patches note; Dismiss clears row",
        "If suggest-tags returns [] (Ollama offline or no matches): 'No suggestions available' shown briefly; dialog stays open for user to click Done",
        "Dialog never auto-closes after save -- user always closes explicitly via Done or Cancel",
        "AbortController cancels in-flight suggest-tags fetch when dialog closes",
        "Create form (Notes.tsx): after POST /notes succeeds, fires suggest-tags and silently patches tags if suggestions returned",
        "POST /notes/{note_id}/suggest-tags returns HTTP 200 with tags=[] when Ollama is offline (not HTTP 503)",
        "test_suggest_tags_api_returns_empty_not_503 passes",
        "npx tsc --noEmit exits 0",
        "npm run build exits 0",
        "uv run ruff check . exits 0",
        "scripts/smoke/S90.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S91",
      "title": "Feature: Notes full-text and semantic search",
      "phase": "V2 Phase 3 - Notes Intelligence",
      "priority": 91,
      "featureRef": "F-NOTES-SEARCH",
      "description": "CONTEXT: Notes are isolated -- no free-text or semantic search exists. GET /notes supports tag/group filters only. This story adds FTS5 keyword search and LanceDB semantic search for notes, following the exact same patterns used for chunks. BACKEND -- FTS5: In db_init.py add: CREATE VIRTUAL TABLE IF NOT EXISTS notes_fts USING fts5(content, note_id UNINDEXED) after the existing chunks_fts DDL. FTS5 SYNC -- In backend/app/routers/notes.py, add raw SQL sync statements after each write operation (same pattern as chunks_fts sync in documents.py using session.execute(text(...))). On note create (after session.commit()): INSERT INTO notes_fts(rowid, content, note_id) VALUES ((SELECT rowid FROM notes WHERE id=:note_id), :content, :note_id). On note update/patch (after session.commit()): DELETE FROM notes_fts WHERE note_id=:note_id; then INSERT as above. On note delete (before or after session.commit()): DELETE FROM notes_fts WHERE note_id=:note_id. Use the existing _sanitize_fts_query helper from app/services/retriever.py (import it) for query sanitization. LANCEDB -- In backend/app/services/vector_store.py, add alongside the existing chunk_vectors table: NOTE_TABLE_NAME = 'note_vectors'. NOTE_SCHEMA = pa.schema([pa.field('note_id', pa.string()), pa.field('content', pa.string()), pa.field('vector', pa.list_(pa.float32(), 1024))]). Add to LanceDBService: _get_or_create_note_table() -> lancedb.Table (same pattern as _get_or_create_table()). upsert_note_vector(note_id: str, content: str, vector: list[float]) -> None: uses merge_insert on note_id key. delete_note_vector(note_id: str) -> None. EMBEDDING TRIGGER -- In notes.py router, after note create and update, trigger note embedding as a background task using the fire_and_forget helper (import from app.runtime.chat_graph or wherever it is defined -- grep for fire_and_forget first). The coroutine to fire: async def _embed_note(note_id, content) that calls get_embedding_service().encode([content])[0] in a thread executor (run_in_executor, same pattern as ingestion.py line 554), then calls get_lancedb_service().upsert_note_vector(note_id, content, vector). On note delete, call get_lancedb_service().delete_note_vector(note_id) synchronously (no background needed, it's fast). SEARCH SERVICE -- Create backend/app/services/note_search.py. NoteSearchResult: TypedDict with note_id: str, content: str, score: float. @lru_cache def get_note_search_service() -> NoteSearchService. Class NoteSearchService, method: async def search(query: str, k: int = 10, semantic: bool = True) -> list[NoteSearchResult]. If query.strip() == '': return []. FTS5 path: execute 'SELECT note_id, content, bm25(notes_fts) AS score FROM notes_fts WHERE notes_fts MATCH :q ORDER BY score LIMIT :k' with sanitized query. Semantic path (if semantic=True): encode query via get_embedding_service() in executor, search note_vectors table with top-k. Merge with RRF (score = 1/(60+rank) summed per note_id). Return deduplicated list sorted by merged score descending. NoteSearchService takes an AsyncSession in search() -- use get_session_factory() same pattern as other services. API -- GET /notes/search?q=...&k=10&semantic=true. Response: NoteSearchResponse(BaseModel) with results: list[NoteSearchResult model -- create NoteSearchResultResponse(BaseModel) with same fields]. Validate: if q is empty string return HTTP 422 with message 'q must be non-empty'. FRONTEND -- In Notes.tsx, add a search bar in the page header above the view toggle (controlled input, 300ms debounce). When debouncedQ is non-empty, switch to search mode: queryKey=['notes-search', debouncedQ], fetches GET /notes/search?q={debouncedQ}. Display results in existing list/card view. When input cleared, revert to normal GET /notes. Add X button to clear. Loading: skeleton rows. Error: inline message. Empty: 'No notes matching \"query\"'. DOCS -- Update docs/ARCHITECTURE.md to document notes_fts and note_vectors under Data Stores. TESTS -- backend/tests/test_note_search.py: (a) test_fts_finds_exact_match: insert note with content 'The White Rabbit led Alice into Wonderland'; search('White Rabbit', semantic=False); assert note_id in results. (b) test_fts_sync_on_delete: create note, verify FTS hit, delete note, verify no FTS hit. (c) test_fts_sync_on_update: create note content='original', update to 'updated text', search('updated text', semantic=False); assert hit; search('original', semantic=False); assert miss. (d) test_search_empty_query: search('', semantic=False); assert returns []. (e) test_search_api_empty_q: GET /notes/search?q=; assert HTTP 422. (f) test_search_api_returns_results: POST /notes with content='Cheshire Cat can vanish'; GET /notes/search?q=Cheshire+Cat&semantic=false; assert HTTP 200, results non-empty, correct note_id present. (g) test_alice_note_search_slow (marked @pytest.mark.slow, uses all_books_ingested fixture from conftest_books.py): create a note 'In Wonderland the Cheshire Cat can disappear leaving only its grin'; search('Cheshire Cat disappear'); assert note appears in top-3 results. SMOKE -- scripts/smoke/S91.sh: (1) GET http://localhost:8000/notes/search?q=test; assert HTTP 200 and JSON has 'results' key. (2) GET http://localhost:8000/notes/search?q=; assert HTTP 422. Exit 0.",
      "acceptanceCriteria": [
        "notes_fts FTS5 virtual table added to db_init.py",
        "FTS5 sync on create, update, and delete in notes.py router using raw SQL (session.execute(text(...)))",
        "LanceDBService extended with NOTE_TABLE_NAME, NOTE_SCHEMA, upsert_note_vector, delete_note_vector",
        "Note embedding triggered as fire_and_forget on create/update; delete_note_vector called on delete",
        "backend/app/services/note_search.py: NoteSearchService.search with FTS+semantic+RRF merge; @lru_cache get_note_search_service()",
        "GET /notes/search?q= returns HTTP 422",
        "GET /notes/search?q=keyword returns NoteSearchResponse with results list",
        "Notes.tsx: debounced search bar, search mode when query active, reverts on clear; loading/error/empty states",
        "test_fts_finds_exact_match, test_fts_sync_on_delete, test_fts_sync_on_update pass",
        "test_alice_note_search_slow passes (marked @pytest.mark.slow)",
        "docs/ARCHITECTURE.md updated with notes_fts and note_vectors",
        "uv run pytest tests/test_note_search.py exits 0",
        "npx tsc --noEmit exits 0",
        "uv run ruff check . exits 0",
        "scripts/smoke/S91.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S92",
      "title": "Feature: Notes as chat context -- chat graph retrieves from notes on notes-intent queries",
      "phase": "V2 Phase 3 - Notes Intelligence",
      "priority": 92,
      "featureRef": "F-NOTES-CHAT",
      "description": "CONTEXT: The chat LangGraph (runtime/chat_graph.py) routes to strategy nodes that search book chunks only. Notes are invisible to chat. A user asking 'what did I note about the Cheshire Cat?' gets no answer. This story adds a 'notes' intent path using NoteSearchService built in S91. IMPORTANT: Ralph must complete S91 before starting S92 -- NoteSearchService must exist. BACKEND -- TYPES: In backend/app/types.py, update IntentType Literal from Literal['summary', 'factual', 'relational', 'comparative', 'exploratory'] to Literal['summary', 'factual', 'relational', 'comparative', 'exploratory', 'notes']. Read types.py line 41 before editing. INTENT HEURISTIC: In backend/app/services/intent.py (or wherever classify_intent_heuristic lives -- grep for it first), extend the keyword heuristic: if any token or phrase in the lowercased query matches {'note', 'noted', 'wrote', 'my notes', 'i wrote', 'i noted', 'according to my notes', 'in my notes'}, return 'notes'. This heuristic runs before the LLM fallback, so it is fast and deterministic. Update the LLM fallback prompt (if it enumerates valid intents as a list) to include 'notes'. ROUTING: In route_node in chat_graph.py (read the existing conditional logic around line 165 before editing), add: if state['intent'] == 'notes': return 'notes_node'. NOTES NODE: Add async def notes_node(state: ChatState) -> dict in chat_graph.py. Pattern: import get_note_search_service from app.services.note_search inside the function (same lazy import pattern as get_retriever()). Call await get_note_search_service().search(state['query'], k=5, semantic=True, session=...) -- NoteSearchService.search needs an AsyncSession. Get the session using get_session_factory() the same way other nodes obtain sessions (grep for 'get_session_factory' in chat_graph.py to find the pattern). Format results: context_chunks = ['[From your notes] ' + r['content'] for r in results]. Set state key 'context_chunks' to these strings and 'retrieved_chunks' to [] (no ScoredChunk objects for notes). Return the dict. The existing synthesize_node reads context_chunks -- it will work unchanged. GRAPH WIRING: In build_chat_graph(), add g.add_node('notes_node', notes_node). In the conditional edge after route_node, add the 'notes_node' branch. notes_node should route to 'synthesize_node' (same as search_node) via _route_after_strategy -- verify this function handles unknown node names gracefully, or add an explicit case. TESTS -- backend/tests/test_chat_graph_notes.py: (a) test_heuristic_classifies_notes_intent: call classify_intent_heuristic('what did I note about Alice') (import the function); assert result == 'notes'. (b) test_heuristic_classifies_my_notes: query='in my notes about the White Rabbit'; assert 'notes'. (c) test_heuristic_no_false_positive: query='what does Alice find in the garden'; assert result != 'notes'. (d) test_route_node_notes_branch: build a minimal ChatState with intent='notes'; call route_node(state); assert return value == 'notes_node'. (e) test_notes_node_formats_context: mock get_note_search_service to return a service whose search() returns [{'note_id':'n1','content':'Alice meets Cheshire Cat','score':0.9}]; call notes_node(state); assert state dict has context_chunks containing '[From your notes] Alice meets Cheshire Cat'. (f) test_notes_node_empty_results: mock search returns []; assert context_chunks == []. (g) test_notes_chat_integration (marked @pytest.mark.slow, uses all_books_ingested fixture): create two notes via POST /notes with content about Alice in Wonderland characters; POST /chat/stream with body {'query': 'what did I note about Wonderland characters', 'document_ids': [], 'scope': 'all'} with LiteLLM mocked to return a short answer; assert HTTP 200 SSE stream is non-empty. DOCS -- Update docs/ARCHITECTURE.md notes intent in chat graph topology section. SMOKE -- scripts/smoke/S92.sh: POST http://localhost:8000/chat/stream with body '{\"query\":\"what did I note about my reading\",\"document_ids\":[],\"scope\":\"all\"}'; assert HTTP 200. Exit 0.",
      "acceptanceCriteria": [
        "'notes' added to IntentType Literal in types.py (line 41); existing intents unchanged",
        "classify_intent_heuristic returns 'notes' for queries containing note/noted/wrote/my notes/i wrote/i noted tokens",
        "No false positives: factual book queries do not route to notes_node",
        "route_node returns 'notes_node' when intent='notes'",
        "notes_node added to chat_graph.py; calls get_note_search_service().search; sets context_chunks with '[From your notes]' prefix",
        "notes_node wired in build_chat_graph(); routes to synthesize_node after execution",
        "test_heuristic_classifies_notes_intent and test_heuristic_no_false_positive pass",
        "test_notes_node_formats_context and test_notes_node_empty_results pass",
        "test_notes_chat_integration passes (marked @pytest.mark.slow)",
        "docs/ARCHITECTURE.md chat graph topology updated",
        "uv run pytest tests/test_chat_graph_notes.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S92.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S93",
      "title": "Feature: Flashcard generation from notes -- tag or note-ID scoped",
      "phase": "V2 Phase 3 - Notes Intelligence",
      "priority": 93,
      "featureRef": "F-NOTES-FLASHCARDS",
      "description": "CONTEXT: FlashcardService.generate() builds cards from book chunks by document_id. This story adds a parallel path to generate cards from a user's own notes, scoped by tag or explicit note IDs. SCHEMA MIGRATION (read models.py lines 77-96 before editing) -- FlashcardModel currently has document_id: Mapped[str] = mapped_column(String, nullable=False) and chunk_id: Mapped[str] = mapped_column(String, nullable=False). Note-sourced cards have no document or chunk. Required changes: (1) change document_id to Mapped[str | None] = mapped_column(String, nullable=True), (2) change chunk_id to Mapped[str | None] = mapped_column(String, nullable=True), (3) add source: Mapped[str] = mapped_column(String, nullable=False, default='document'). In db_init.py, add three ALTER TABLE statements inside the create_all_tables function (after the CREATE TABLE block for flashcards -- use IF NOT EXISTS guard via PRAGMA table_info or catch OperationalError): ALTER TABLE flashcards ADD COLUMN source TEXT NOT NULL DEFAULT 'document'; do the same for making nullable -- SQLite does not support ALTER COLUMN, so the approach is: the SQLAlchemy model already reflects the new nullable=True in Python, which is sufficient for new rows; existing rows have non-null values so no data loss. Add a migration guard: execute 'PRAGMA table_info(flashcards)' and check if 'source' column exists; if not, execute the ALTER TABLE. BACKEND -- EXTEND FlashcardService (read backend/app/services/flashcard.py fully before editing). Add method: async def generate_from_notes(tag: str | None, note_ids: list[str] | None, count: int, session: AsyncSession) -> list[FlashcardModel]. Validation: if tag is None and (note_ids is None or len(note_ids)==0): raise ValueError('Must provide tag or note_ids'). Fetch notes: if note_ids provided, SELECT * FROM notes WHERE id IN (:ids). Else: SELECT * FROM notes WHERE JSON_EACH trick for tag (same query as GET /notes?tag=). If no notes found, return []. Build combined text via existing _build_text() (cap 5000 chars). Call LLM using same FLASHCARD_SYSTEM and FLASHCARD_USER_TMPL. Parse with _parse_llm_response(). Persist each card with source='note', document_id=None, chunk_id=None, source_excerpt set to first 200 chars of combined note text, and FSRS initial state (same as generate()). Handle litellm.ServiceUnavailableError by raising HTTPException(503) from the router. API -- Read backend/app/routers/flashcards.py fully before adding the endpoint. Add to that file: POST /notes/flashcards/generate (note: this URL is under /notes prefix for discoverability -- either mount it in flashcards.py with an explicit path or add to notes.py router; read which router prefix is cleaner given the existing structure and pick the simpler option). Input: GenerateFromNotesRequest(BaseModel) with tag: str | None = None, note_ids: list[str] | None = None, count: int = Field(default=10, ge=3, le=50). Use @model_validator(mode='after') to validate at least one of tag or note_ids is provided; raise ValueError if both are None/empty. Response: list[FlashcardResponse] (reuse existing FlashcardResponse schema). On ServiceUnavailableError from FlashcardService: return HTTP 503 with detail='Ollama is unreachable. Start it with: ollama serve'. FRONTEND -- In Notes.tsx, add a 'Generate Flashcards' button in the page header. On click, open GenerateFlashcardsDialog (new file: frontend/src/components/GenerateFlashcardsDialog.tsx). Dialog: tag dropdown (from GET /notes/groups tags), note multi-select list (note titles = first 60 chars of content, with checkboxes), count input (default 10, min 3 max 50). Either tag or at least one note must be selected before the Generate button enables. Generate button: calls POST /notes/flashcards/generate, shows Loader2 inline while pending (dialog stays open -- invariant 14). On success: show 'N flashcards created -- visit Study tab to review'. On 503: show 'Ollama is unavailable. Start it with: ollama serve' (invariant 18). On other error: inline error with retry. Close button always available. Loading/error/empty states for the note list inside the dialog (invariant 13). TESTS -- backend/tests/test_notes_flashcards.py: (a) test_generate_from_notes_by_tag: create 2 notes with tag 'alice' (INSERT directly or via POST /notes with tags=['alice']); mock litellm.acompletion to return valid flashcard JSON (3 cards); call generate_from_notes(tag='alice', note_ids=None, count=5, session); assert 3 FlashcardModel returned with source='note' and document_id=None. (b) test_generate_from_notes_by_ids: create 2 notes; call generate_from_notes(tag=None, note_ids=[id1,id2], count=3, session) with mocked LLM; assert cards returned with source='note'. (c) test_generate_from_notes_no_params: call generate_from_notes(None, None, 5, session); assert ValueError raised. (d) test_api_422_both_none: POST /notes/flashcards/generate with body '{}'; assert HTTP 422. (e) test_api_503_ollama_down: mock litellm to raise ServiceUnavailableError; POST /notes/flashcards/generate with valid body; assert HTTP 503 and 'ollama' (case-insensitive) in response detail. (f) test_alice_notes_flashcards (marked @pytest.mark.slow): create a note with content 'Alice follows the White Rabbit down a rabbit-hole into Wonderland where she meets the Cheshire Cat and the Mad Hatter at a tea party'; call generate_from_notes(tag=None, note_ids=[note_id], count=3, session) with LiteLLM mocked to return 3 valid cards; assert 3 FlashcardModel with source='note'. SMOKE -- scripts/smoke/S93.sh: (1) POST /notes/flashcards/generate with body '{}'; assert HTTP 422. (2) POST /notes/flashcards/generate with body '{\"tag\":\"nonexistent-tag-xyz\",\"count\":3}'; assert HTTP 200 (empty list) or 200 (no notes for tag is not an error). Exit 0.",
      "acceptanceCriteria": [
        "FlashcardModel: document_id and chunk_id changed to nullable=True; source column added (default 'document')",
        "db_init.py: ALTER TABLE flashcards ADD COLUMN source ... with existence guard (PRAGMA table_info check)",
        "FlashcardService.generate_from_notes(tag, note_ids, count, session) raises ValueError when both tag and note_ids are absent",
        "Note-sourced cards have source='note', document_id=None, chunk_id=None",
        "POST /notes/flashcards/generate returns HTTP 422 when both tag and note_ids absent",
        "POST /notes/flashcards/generate returns HTTP 503 with 'ollama' in detail when Ollama unreachable",
        "GenerateFlashcardsDialog: tag dropdown, note multi-select, count input, inline Loader2, success/error messages",
        "503 shown as 'Ollama is unavailable. Start it with: ollama serve'",
        "Generate button disabled until tag or at least one note selected",
        "test_generate_from_notes_by_tag, test_generate_from_notes_by_ids pass",
        "test_alice_notes_flashcards passes (marked @pytest.mark.slow)",
        "uv run pytest tests/test_notes_flashcards.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S93.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S94",
      "title": "Feature: Notes vs Book gap detection -- surface book concepts absent from notes",
      "phase": "V2 Phase 3 - Notes Intelligence",
      "priority": 94,
      "featureRef": "F-NOTES-GAP",
      "description": "CONTEXT: A learner writes notes on a book but misses concepts. Knowing what the book covers that your notes do not is high-value feedback. This story adds a gap detection endpoint: given notes (by tag or IDs) and a book document, retrieve the most relevant book passages, then use LLM to identify concepts in the book absent from the notes. BACKEND -- TYPES: In backend/app/types.py add GapReport TypedDict: class GapReport(TypedDict): gaps: list[str]; covered: list[str]; query_used: str. Add at the bottom of types.py (read the file first to find a safe insertion point). SERVICE -- Create backend/app/services/gap_detector.py. Class GapDetectorService, @lru_cache def get_gap_detector() -> GapDetectorService. Method: async def detect_gaps(note_ids: list[str], document_id: str, k: int = 8, session: AsyncSession) -> GapReport. Implementation: (1) Fetch notes by note_ids via session. If none found, raise ValueError('No notes found for given IDs'). Combine content into notes_text (cap 3000 chars). (2) Use first 200 chars of notes_text as query_used. (3) Call get_retriever().retrieve(query=query_used, document_ids=[document_id], k=k, session=session) -- read retriever.py to confirm the exact signature before writing this call. (4) Build book_context from chunk texts (cap 3000 chars). (5) Call litellm.acompletion with system='You are a learning gap analyst. Given learner notes and book passages on the same topic, return JSON: {\"gaps\": [\"...\"], \"covered\": [\"...\"]}. Each item is one sentence max. Gaps are key concepts from the passages absent or barely mentioned in the notes. Covered are concepts the notes address well. Be specific -- name actual concepts.' User message: 'NOTES:\\n{notes_text}\\n\\nBOOK PASSAGES:\\n{book_context}'. (6) Parse JSON; on malformed response return GapReport(gaps=['Gap analysis unavailable -- could not parse LLM response'], covered=[], query_used=query_used). (7) On litellm.ServiceUnavailableError: raise HTTPException(503, detail='Ollama is unreachable. Start it with: ollama serve'). Structured logging only. API -- In backend/app/routers/notes.py add POST /notes/gap-detect. Input: GapDetectRequest(BaseModel) with note_ids: list[str] = Field(min_length=1), document_id: str. Validate note_ids non-empty (422 if empty -- Field(min_length=1) handles this). Response: GapDetectResponse(BaseModel) with gaps: list[str], covered: list[str], query_used: str. Call get_gap_detector().detect_gaps(...). On ValueError from service (no notes found): HTTP 404. On HTTPException 503: propagate. FRONTEND -- In Notes.tsx, add 'Compare with Book' button in page header. On click open GapDetectDialog (new file: frontend/src/components/GapDetectDialog.tsx). Dialog: (a) document selector -- dropdown populated from GET /documents (read documents.py router to confirm the endpoint path and response schema -- the endpoint exists at GET /documents as a paginated list; use title field for display); (b) note multi-select list (same pattern as GenerateFlashcardsDialog from S93 -- consider extracting a shared NoteMultiSelect component if it simplifies both dialogs, otherwise duplicate is acceptable); (c) 'Analyze Gaps' button with inline Loader2 while pending (dialog stays open -- invariant 14). On success: show two sections -- 'Concepts the book covers that your notes miss' (gaps as bullets) and 'Concepts your notes cover well' (covered as bullets). If gaps is empty: 'Your notes cover this material well!'. On 503: 'Ollama is unavailable. Start it with: ollama serve' (invariant 18). Loading/error/empty states required (invariant 13). TESTS -- backend/tests/test_gap_detector.py: (a) test_detect_gaps_returns_report: create 1 note with content 'Alice meets the Cheshire Cat'; mock get_retriever().retrieve to return 3 fake ScoredChunk objects about Wonderland; mock litellm.acompletion to return valid JSON gap report; call detect_gaps([note_id], 'fake-doc-id', k=3, session); assert GapReport has non-empty gaps and covered lists. (b) test_detect_gaps_no_notes_found: call detect_gaps(['nonexistent-id'], 'doc-id', k=3, session); assert ValueError raised. (c) test_detect_gaps_malformed_llm: mock litellm to return non-JSON text; assert GapReport returned with gaps=['Gap analysis unavailable -- could not parse LLM response'] and covered=[]. (d) test_gap_detect_api_422_empty_note_ids: POST /notes/gap-detect with body '{\"note_ids\":[],\"document_id\":\"abc\"}'; assert HTTP 422. (e) test_gap_detect_api_503: mock get_gap_detector().detect_gaps to raise HTTPException(503); POST /notes/gap-detect; assert HTTP 503. (f) test_alice_gap_detection_slow (marked @pytest.mark.slow, uses all_books_ingested fixture from conftest_books.py): create a note 'Alice follows the White Rabbit and meets the Cheshire Cat'; call detect_gaps([note_id], alice_doc_id, k=5, session) with real Retriever (no mock) and LiteLLM mocked to return a valid JSON report; assert retrieve was called (check context contains book passage text) -- verify that book_context in the LLM call prompt contains at least one passage from the Alice corpus. SMOKE -- scripts/smoke/S94.sh: (1) POST /notes/gap-detect with body '{\"note_ids\":[],\"document_id\":\"abc\"}'; assert HTTP 422. (2) POST /notes/gap-detect with body '{\"note_ids\":[\"nonexistent\"],\"document_id\":\"nonexistent\"}'; assert HTTP 404 or HTTP 503 (not 500). Exit 0.",
      "acceptanceCriteria": [
        "GapReport TypedDict (gaps, covered, query_used) added to backend/app/types.py",
        "backend/app/services/gap_detector.py: GapDetectorService.detect_gaps with @lru_cache get_gap_detector()",
        "detect_gaps uses real get_retriever().retrieve for book passage retrieval -- no mocking of retriever in production path",
        "Malformed LLM JSON handled: returns GapReport with gaps=['Gap analysis unavailable -- could not parse LLM response']",
        "detect_gaps raises ValueError when no notes found for given IDs",
        "POST /notes/gap-detect HTTP 422 when note_ids is empty list",
        "POST /notes/gap-detect HTTP 503 when Ollama unreachable",
        "GapDetectDialog: document selector, note multi-select, Analyze button with inline Loader2, gaps/covered result sections",
        "503 shown as 'Ollama is unavailable. Start it with: ollama serve'",
        "Loading, error, and empty states in GapDetectDialog",
        "test_detect_gaps_returns_report, test_detect_gaps_no_notes_found, test_detect_gaps_malformed_llm pass",
        "test_alice_gap_detection_slow passes (marked @pytest.mark.slow)",
        "uv run pytest tests/test_gap_detector.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S94.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S95",
      "title": "Smart Start: contextual suggestion pills in Chat based on active document and FSRS state",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 95,
      "featureRef": "F-CHAT-SMART-START",
      "description": "CONTEXT: The Chat tab starts empty, forcing the user to think of what to ask. Most productive learning interactions are predictable from context: which document is open and how many FSRS cards are due. This story adds a row of contextual suggestion pills above the chat input whenever an active document is set. Pills are derived from a pure function -- no backend AI call needed. USER JOURNEY: User opens Chat tab with a document selected -> sees contextual suggestion pills above the input -> clicks a pill -> question submits immediately and routes to the correct chat node. CROSS-STORY IMPACT: None. S101 adds a 4th '__plan__' pill to getContextualSuggestions() -- do not add it here. Every pill label introduced here must be covered by a parametrized intent routing test (per story-authoring.md invariant 21).",
      "acceptanceCriteria": [
        "ChatSuggestions renders only when activeDocumentId is non-null",
        "Calls GET /study/due?document_id={id} (existing endpoint); no new backend endpoints created",
        "getContextualSuggestions pure function: dueCount>0 yields due pill; always yields gap + summarize + quiz pills; returns first 3",
        "Pills are outline rounded-full buttons, truncated to max-w-[200px]",
        "Clicking a pill injects text into chat input and submits immediately without duplicating submit logic",
        "Pill row hidden after messages.length > 0",
        "Loading: 2 animate-pulse skeleton pill shapes",
        "Error: no pills rendered (silent fail -- ChatSuggestions is decorative, not load-critical)",
        "No '__plan__' pill -- that is added in S101",
        "Parametrized test test_suggestion_pills_route_correctly: 'Find gaps in my notes' -> intent='notes_gap', 'Summarize this for me' -> intent='summary', 'Quiz me on the key concepts' -> intent='socratic'; each asserted via classify_intent_heuristic with confidence >= 0.9",
        "End-to-end flow: clicking each non-plan pill results in a message submission that reaches the correct chat graph node (verified in smoke test)",
        "npx tsc --noEmit exits 0",
        "npm run build exits 0",
        "scripts/smoke/S95.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S96",
      "title": "Inline Gap Analysis Card in Chat: notes-vs-book comparison via chat intent, establishes shared card SSE contract",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 96,
      "featureRef": "F-CHAT-GAP-CARD",
      "description": "CONTEXT: The GapDetectDialog (S94) is a multi-step modal. This story moves gap detection into the chat stream using the '__card__' SSE protocol that S97, S98, and S99 all build on. Read this description carefully; the card contract defined here must not be changed in subsequent stories. USER JOURNEY: User is in Notes tab -> clicks 'Compare with Book' -> GapDetectDialog opens with a document picker (not a navigation to /chat) -> OR user types 'find gaps in my notes' in Chat with scope='single' and a document selected -> gap result card appears inline in the chat stream. CROSS-STORY IMPACT: S94 added GapDetectDialog and a 'Compare with Book' button in Notes.tsx that navigated to /chat?q=. This story changes the backend gap flow. The Notes.tsx button must open GapDetectDialog directly (not navigate), because navigating loses the user's current scope and document context. The useNavigate() import in Notes.tsx must be removed. Do NOT delete GapDetectDialog.tsx -- the dialog and the chat-inline card serve different entry points. SPAWN luminary-planner before writing any code: this story touches types.py, intent.py, chat_graph.py, the SSE stream loop, Chat.tsx, and Notes.tsx.",
      "acceptanceCriteria": [
        "'notes_gap' added to IntentType Literal in types.py; existing intents unchanged",
        "classify_intent_heuristic returns ('notes_gap', 0.95) for 'find gaps in my notes' and all phrases in _NOTES_GAP_KWS",
        "notes_gap_node: fetches notes for document_id, calls GapDetectorService, returns '__card__' JSON",
        "Error case -- no document_id in state: returns card with error 'Switch to This document scope and select a book to compare against your notes.'",
        "Error case -- no notes for document: returns card with error 'No notes linked to this document. Create notes for this document first, then ask again.'",
        "Error case -- Ollama offline: returns card with error field; never raises to SSE stream",
        "SSE loop: '__card__' prefix triggers single 'card' SSE event; no 'token' events emitted for card answers",
        "SSE loop has comment block documenting the __card__ protocol for S97-S99",
        "route_node dispatches 'notes_gap' -> 'notes_gap_node'; node routes to END",
        "Chat.tsx Message interface has optional type and cardData fields; existing messages default to type='text'",
        "SSE parser: 'card' key in payload creates card-type message, does not append text",
        "GapResultCard.tsx: error state (amber box), gaps section (red X list), covered section (green check list), empty gaps message, disabled Generate button (enabled in S97)",
        "Notes.tsx 'Compare with Book' button calls setShowGapDetect(true) to open GapDetectDialog directly; useNavigate() import removed from Notes.tsx; GapDetectDialog.tsx not deleted",
        "End-to-end: user with scope='single' and a document selected, sends 'find gaps in my notes' -> SSE stream delivers a 'card' event -> GapResultCard renders with type='gap_result'",
        "test_notes_gap_intent_heuristic, test_notes_gap_returns_gap_result_card, test_notes_gap_ollama_offline, test_notes_gap_no_document_returns_error_card pass",
        "uv run pytest tests/test_chat_graph_gap.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S96.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S97",
      "title": "One-Click Bridge: generate flashcards from gap card in chat with no forms",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 97,
      "featureRef": "F-CHAT-GAP-BRIDGE",
      "description": "CONTEXT: After S96, GapResultCard shows what the user is missing but the [Generate Flashcards] button is disabled. This story enables it with a new endpoint that generates one LLM-authored flashcard per gap with no additional dialogs. This endpoint is also used by S99 (TeachBackResultCard) and S100 (ConfusionBanner) -- its API contract must remain stable. USER JOURNEY: User sees gap result card in chat -> clicks 'Generate Flashcards' -> Loader2 spinner while in flight -> 'N flashcards added' with a link to Study tab appears in the card. CROSS-STORY IMPACT: GapResultCard from S96 has a disabled Generate button -- this story enables it. POST /flashcards/from-gaps is introduced here and reused by S99 and S100; do not change its contract in those stories.",
      "acceptanceCriteria": [
        "POST /flashcards/from-gaps accepts {gaps: list[str], document_id: str} where document_id defaults to '' (empty string allowed, no min_length)",
        "422 when gaps is empty list; 200 with {created: int} on success",
        "generate_from_gaps uses asyncio.Semaphore(5) for bounded LLM concurrency",
        "Malformed LLM JSON: logs warning, skips that gap, does not raise",
        "Ollama offline: HTTP 503 with detail 'Ollama is unreachable. Start it with: ollama serve'",
        "FlashcardModel rows inserted with deck='gaps', FSRS default state, datetime.now(UTC)",
        "GapResultCard [Generate Flashcards] button: idle -> loading (Loader2) -> done ('{N} flashcards added' + Go to Study link) | error (inline + retry)",
        "Button disabled when gaps.length === 0",
        "503 renders 'Ollama is unavailable. Start it with: ollama serve' inline in card",
        "End-to-end: test_gap_card_to_flashcards sends gaps list to POST /flashcards/from-gaps and asserts FlashcardModel rows in DB with deck='gaps'",
        "test_generate_from_gaps_creates_flashcards, test_generate_from_gaps_skips_malformed_llm, test_from_gaps_api_empty_document_id_allowed pass",
        "uv run pytest tests/test_from_gaps.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S97.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S98",
      "title": "Socratic Quiz Mode: chat generates targeted recall questions from active document",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 98,
      "featureRef": "F-CHAT-SOCRATIC",
      "description": "CONTEXT: Retrieval practice -- being tested before re-reading -- is the highest-leverage learning technique in cognitive science. This story adds a 'socratic' intent: when the user asks to be quizzed, the chat generates one targeted recall question from the document and renders it as a QuizQuestionCard using the __card__ SSE protocol from S96. USER JOURNEY: User with a document selected clicks 'Quiz me on the key concepts' pill (or types a quiz request) -> a quiz question card appears in the chat stream -> user types their answer in the card textarea -> clicks Submit -> answer appears as a new user message in the thread, and the card becomes read-only. CROSS-STORY IMPACT: S95 added 'Quiz me on the key concepts' as a suggestion pill. This story must ensure that exact string routes to 'socratic' intent -- add it to the parametrized test. Do not change the __card__ SSE protocol established in S96.",
      "acceptanceCriteria": [
        "'socratic' added to IntentType Literal; existing intents unchanged",
        "classify_intent_heuristic returns ('socratic', 0.95) for 'quiz me', 'test me', 'ask me a question', and all phrases in _SOCRATIC_KWS",
        "Parametrized socratic test coverage explicitly includes 'Quiz me on the key concepts' -- the exact pill label from getContextualSuggestions() -- asserting intent='socratic'",
        "No false positive: 'what is the main theme?' does not return 'socratic'",
        "socratic_node retrieves 5 chunks, generates Q+CONTEXT via LiteLLM (non-streaming), returns '__card__' JSON",
        "Parse fallback: malformed LLM response yields a default question string; no exception raised",
        "Ollama offline: card with error field returned; no exception propagates to SSE stream",
        "No changes to SSE loop -- existing __card__ protocol from S96 handles this automatically",
        "QuizQuestionCard.tsx: border-l-4 blue, Quiz chip, question text, answer Textarea, Submit button",
        "Submit: appends 'My answer: {text}' as new chat message; card becomes read-only (Textarea + button disabled)",
        "Error prop: shows amber inline error; Textarea and button hidden",
        "Loading state: QuizQuestionCard shows skeleton while card SSE event is in flight",
        "test_socratic_node_returns_card, test_socratic_parse_fallback, test_socratic_ollama_offline, test_socratic_pill_label_routes_correctly pass",
        "uv run pytest tests/test_chat_graph_socratic.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S98.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S99",
      "title": "Teach It Back Mode: user explains a concept, AI evaluates and surfaces misconceptions",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 99,
      "featureRef": "F-CHAT-TEACH-BACK",
      "description": "CONTEXT: The protege effect -- learning by teaching -- is one of the most reliable techniques for consolidating understanding. When the user says 'let me explain X', the chat retrieves authoritative passages and evaluates their explanation precisely: confirming what is right, correcting what is wrong, naming gaps. USER JOURNEY: User types 'let me explain [concept]' followed by their explanation -> AI retrieves relevant passages -> TeachBackResultCard appears with encouragement, any corrections, and gaps the user missed -> user clicks 'Add Gaps to Flashcards' to create cards for the things they got wrong or missed. CROSS-STORY IMPACT: Uses __card__ SSE protocol (S96) and POST /flashcards/from-gaps (S97) -- both must exist before implementing. Do not recreate either. LLM responses may wrap JSON in fenced code blocks (```json...```) -- the parser must strip fences before attempting json.loads.",
      "acceptanceCriteria": [
        "'teach_back' added to IntentType Literal; existing intents unchanged",
        "classify_intent_heuristic returns ('teach_back', 0.95) for 'let me explain', 'my understanding is', and all phrases in _TEACH_BACK_KWS",
        "No false positive: 'explain this to me' (imperative, asking AI to explain) does not return 'teach_back'",
        "teach_back_node retrieves 5 chunks, calls LiteLLM (non-streaming), returns '__card__' JSON",
        "LLM JSON wrapped in fenced code blocks (```json...```) is parsed correctly; fences stripped before json.loads",
        "Malformed LLM JSON: fallback card with encouragement text returned; no exception raised",
        "Ollama offline: card with error field; no exception propagates to SSE stream",
        "No changes to SSE loop -- existing __card__ protocol from S96 handles this automatically",
        "POST /flashcards/from-gaps from S97 used as-is; not recreated",
        "TeachBackResultCard.tsx: encouragement text, three conditional sections (correct/misconceptions/gaps), [Add Gaps] button",
        "[Add Gaps] button: idle -> loading -> done -> error state machine; disabled when gaps list empty; 503 shown inline",
        "Loading state: card renders a skeleton while SSE card event is in flight",
        "test_teach_back_node_returns_card, test_teach_back_malformed_json_fallback, test_teach_back_ollama_offline, test_teach_back_no_false_positive, test_teach_back_fenced_json_parsed pass",
        "uv run pytest tests/test_chat_graph_teach_back.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S99.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S100",
      "title": "Confusion Detection Nudge: repeated questions across sessions surface a flashcard suggestion",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 100,
      "featureRef": "F-CHAT-CONFUSION",
      "description": "CONTEXT: When a learner asks about the same concept multiple times across sessions, they have not consolidated that knowledge. The qa_history table records every question. This story analyzes that history on chat load (DB-only, no LLM) and shows a dismissible amber banner when a stubborn concept is detected. USER JOURNEY: User opens Chat tab -> if they have asked about the same non-trivial concept 3 or more times in the past 30 days, an amber banner appears at the top of the message area -> user clicks 'Add to Flashcards' to address the gap or dismisses it. CROSS-STORY IMPACT: Uses POST /flashcards/from-gaps (S97) with document_id='' -- do not recreate that endpoint. STOPWORDS: _STOPWORDS must be built as frozenset(ENGLISH_STOP_WORDS) | _APP_STOPWORDS where the base comes from sklearn.feature_extraction.text (already in dependencies) and _APP_STOPWORDS is a separately defined frozenset covering UI suggestion words (summarize, quiz, compare, review), generic learning vocabulary (themes, findings, concepts, key, main), and document-structural terms (book, chapter, section, notes, gaps). This separation is required because standard linguistic stopword libraries do not cover app-specific noise words that appear in the UI's own suggested inputs.",
      "acceptanceCriteria": [
        "ConfusionDetectorService.detect: queries qa_history in lookback window, tokenizes questions, counts non-stopword terms, returns terms >= threshold",
        "_STOPWORDS composed as frozenset(ENGLISH_STOP_WORDS) | _APP_STOPWORDS; sklearn base (318 words); _APP_STOPWORDS is a separately defined frozenset with inline comment documenting each word category",
        "Returns top 3 signals ordered by count desc; returns [] when total rows < threshold",
        "GET /chat/confusion-signals returns list[ConfusionSignalResponse]; HTTP 200 on empty DB",
        "POST /flashcards/from-gaps used with document_id='' (S97 endpoint, not recreated)",
        "Chat.tsx: confusion query fetched on mount with staleTime 5min; banner shown only when count >= 3",
        "ConfusionBanner: amber styling, concept + count text, [Add to Flashcards] and dismiss X buttons",
        "Dismiss: component-state Set<string>; no localStorage; banner re-appears on next page load",
        "[Add to Flashcards]: Loader2 while in flight; on success auto-dismisses; on 503 shows inline error + retry",
        "Query loading or error: no banner rendered (silent fail)",
        "Parametrized test test_ui_surface_inputs_produce_no_signal: every string in Chat.tsx EXAMPLE_QUESTIONS and every non-prefixed string from getContextualSuggestions() is sent as a qa_history question threshold times, and detect() returns [] (all tokens must fall in _STOPWORDS)",
        "test_detect_returns_signal_above_threshold, test_detect_below_threshold_returns_empty, test_detect_ignores_stopwords, test_detect_lookback_respected pass",
        "uv run pytest tests/test_confusion_detector.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S100.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S101",
      "title": "Study Session Planner: pre-session agenda from FSRS state, gaps, and document progress",
      "phase": "V2 Phase 4 - Conversational Learning UX",
      "priority": 101,
      "featureRef": "F-CHAT-PLANNER",
      "description": "CONTEXT: Learners open the app not knowing where to start. The data for a prioritized plan already exists: FSRS due cards (urgency), weak flashcard areas (gaps), and recently ingested documents (reading progress). This story adds GET /study/session-plan and renders the result as an inline slide-up panel in Chat triggered by a 4th pill added to S95's ChatSuggestions. USER JOURNEY: User clicks 'Plan my session' pill -> session plan panel slides up in the chat area above the input -> panel shows prioritized items (review, gaps, reading) each with a type icon, title, minute estimate, and action button -> user clicks an action button and navigates to the relevant tab. CROSS-STORY IMPACT: Modifies S95's getContextualSuggestions() by appending '__plan__Plan my session' as the 4th item. After this modification all S95 parametrized pill routing tests must still pass. Action buttons must use React Router navigate() not window.location.href -- hard navigation resets all React state (active document, scope, chat history).",
      "acceptanceCriteria": [
        "GET /study/session-plan?minutes=20: accepts ge=5, le=120; 422 outside bounds",
        "SessionPlanResponse and SessionPlanItem Pydantic models defined in study.py",
        "_build_session_plan pure function: review item (minutes capped at 10), up to 2 gap items, 1 read item, total capped at 5 items",
        "Empty inputs (no due cards, no gaps, no docs) returns items=[]",
        "S95 ChatSuggestions MODIFIED: getContextualSuggestions appends '__plan__Plan my session' as 4th item; slice(0,4) still applies",
        "Pill onClick: '__plan__' prefix triggers openSessionPlan() (setShowPlanPanel(true)); strip prefix for display text",
        "Session plan panel: slide-up CSS transition (max-h-0 -> max-h-96), renders in chat area above input (not a modal)",
        "Panel items: type icon (BookOpen/AlertTriangle/BookMarked), title, minute badge, action button",
        "Action button uses React Router navigate(item.action_target) -- not window.location.href",
        "Panel fetch: enabled only when showPlanPanel=true",
        "Loading: 3 skeleton rows; Error: inline message + retry; Empty: 'You are all caught up!'",
        "After modifying getContextualSuggestions(), S95 parametrized test test_suggestion_pills_route_correctly still passes for all non-plan pill labels",
        "test_build_plan_review_priority, test_build_plan_caps_at_5, test_build_plan_empty_inputs pass",
        "test_session_plan_api_200 and test_session_plan_api_bounds pass",
        "uv run pytest tests/test_session_planner.py exits 0",
        "uv run ruff check . exits 0",
        "npx tsc --noEmit exits 0",
        "scripts/smoke/S101.sh exits 0"
      ],
      "passes": true
    },
    {
      "id": "S102",
      "title": "Tech Debt group 1: datetime.now(UTC), typed settings PATCH, FK pragma, raw file cleanup",
      "phase": "V2 Phase 5 - Hardening",
      "priority": 102,
      "featureRef": "F-DEBT-MECHANICAL",
      "description": "CONTEXT: Four mechanical tech debt items with no UX impact can be resolved together. TD-002: all routers use deprecated datetime.utcnow() which raises DeprecationWarning in Python 3.12+ and is removed in 3.13. TD-005: PATCH /settings body accepts a raw dict violating Invariant #4 (all FastAPI inputs are Pydantic models). TD-010: SQLite FK pragma is not enabled so cascading deletes are implemented manually in every delete endpoint -- missing it in one new endpoint causes orphaned rows. TD-008: raw uploaded files in ~/.luminary/raw/{doc_id}.ext are not deleted when a document is deleted, accumulating disk usage over time. IMPLEMENTATION: Replace datetime.utcnow() with datetime.now(timezone.utc) everywhere. Define SettingsUpdateRequest(BaseModel) with Optional fields for all writable settings keys. Add a SQLAlchemy event listener on engine connect that runs PRAGMA foreign_keys = ON. In the document delete endpoint, glob and remove ~/.luminary/raw/{doc_id}.* after DB deletion.",
      "acceptanceCriteria": [
        "grep -r 'utcnow' backend/app/ returns no results; all replaced with datetime.now(timezone.utc)",
        "PATCH /settings uses SettingsUpdateRequest(BaseModel) with Optional[str] fields; returns 422 on unknown field names",
        "database.py registers a SQLAlchemy 'connect' event that runs PRAGMA foreign_keys = ON for every connection",
        "Document delete endpoint: after DB deletion, any file matching ~/.luminary/raw/{doc_id}.* is deleted",
        "All existing tests pass; uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S103",
      "title": "Tech Debt TD-006: async classify_node and graceful 503 on Ollama unreachable in chat graph",
      "phase": "V2 Phase 5 - Hardening",
      "priority": 103,
      "featureRef": "F-DEBT-ASYNC-GRAPH",
      "description": "CONTEXT: classify_node and other nodes in runtime/chat_graph.py make synchronous litellm.completion() calls inside an async ASGI context (TD-006). If Ollama is unreachable, the exception escapes the graph executor and can crash the ASGI worker rather than returning a structured error to the client. USER JOURNEY: User sends a chat message while Ollama is stopped -> the SSE stream emits a JSON event {\"type\": \"error\", \"message\": \"Ollama is unreachable. Start it with: ollama serve\"} and closes cleanly -- no 500, no silent hang. IMPLEMENTATION: Replace every litellm.completion() call in runtime/ with await litellm.acompletion(). Wrap the top-level graph execution in the /chat/stream handler with try/except for litellm.ServiceUnavailableError and emit a structured SSE error event. Apply the same pattern to any other runtime/ graph that calls LiteLLM (ingestion summarize node, etc.).",
      "acceptanceCriteria": [
        "grep -r 'litellm.completion(' backend/app/runtime/ returns no results -- all replaced with await litellm.acompletion()",
        "Test: mock litellm.acompletion to raise ServiceUnavailableError; POST /qa returns a valid SSE event with type=error and message containing Ollama is unreachable",
        "No HTTP 500 returned when Ollama is unreachable; SSE connection closes cleanly after error event",
        "All existing chat graph tests pass",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S104",
      "title": "Tech Debt TD-004: migrate API keys from plaintext SQLite to OS keychain via keyring",
      "phase": "V2 Phase 5 - Hardening",
      "priority": 104,
      "featureRef": "F-DEBT-KEYCHAIN",
      "description": "CONTEXT: API keys for external LLM providers (OpenAI, Anthropic, Gemini) are stored as plaintext strings in the SQLite settings table (TD-004). Any process that can read ~/.luminary/luminary.db can extract live API keys. IMPLEMENTATION: Add keyring to backend dependencies (uv add keyring). In SettingsService, intercept writes of any field whose name ends in _api_key: store the actual value in the OS keychain via keyring.set_password('luminary', field_name, value) and write the sentinel string '__keychain__' into SQLite. On read, if the stored value equals '__keychain__', fetch from keychain; otherwise return as-is for backward compatibility with existing installs. On first read of a plaintext key, migrate it to the keychain automatically (read -> store in keychain -> write sentinel to SQLite). Frontend Settings page: display saved keys as 'saved securely' instead of the raw value.",
      "acceptanceCriteria": [
        "uv add keyring; keyring listed in pyproject.toml dependencies",
        "SettingsService.set() for *_api_key fields writes '__keychain__' sentinel to SQLite and actual value to OS keychain",
        "SettingsService.get() returns keychain value when SQLite shows '__keychain__' sentinel",
        "Existing plaintext key in SQLite is auto-migrated to keychain on first read",
        "Test using keyring's mock backend: assert SQLite row is '__keychain__' after set(); assert get() returns original value",
        "Settings page UI shows 'saved securely' for key fields that have been stored"
      ],
      "passes": true
    },
    {
      "id": "S105",
      "title": "Tech Debt TD-003 and TD-011: bundle splitting for heavy deps and exact token counting via litellm",
      "phase": "V2 Phase 5 - Hardening",
      "priority": 105,
      "featureRef": "F-DEBT-BUNDLE",
      "description": "CONTEXT: Two performance debt items. TD-003: Vite build reports a chunk size warning (~1.2MB) because Sigma.js/Graphology and Recharts are bundled eagerly even though they are only needed on Viz and Monitoring tabs. TD-011: context_packer.py uses word_count * 1.3 as a token approximation which is inaccurate for non-English text, code, and long documents. IMPLEMENTATION: In App.tsx, confirm Viz and Monitoring are React.lazy() (S84 made Viz/Monitoring lazy but verify no regression). Wrap ProgressDashboard and any other Recharts-heavy component trees in React.lazy() + Suspense boundaries so Recharts is not in the initial bundle. For token counting, replace the word_count * 1.3 formula in context_packer.py with litellm.token_counter(model=model, text=text) which wraps tiktoken and falls back gracefully for unknown models.",
      "acceptanceCriteria": [
        "vite build produces no chunk size warning (all output chunks <= 500KB after splitting)",
        "Viz and Monitoring pages verified as React.lazy(); ProgressDashboard wrapped in React.lazy() + Suspense",
        "context_packer.py pack_context() uses litellm.token_counter() instead of word_count * 1.3",
        "Unit test: pack_context() token count for a 500-word English passage is within 5% of tiktoken cl100k_base reference",
        "Existing context packer tests pass; uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S106",
      "title": "Tech Debt TD-001: add section_id to notes model so DocumentReader indicators persist across reloads",
      "phase": "V2 Phase 5 - Hardening",
      "priority": 106,
      "featureRef": "F-DEBT-NOTES-SECTION",
      "description": "CONTEXT: NoteModel has no section_id column (TD-001). The DocumentReader section list is intended to show a dot indicator on sections that have notes, but because section_id is never stored, the app cannot reconstruct which sections have notes after a page reload -- the indicators disappear. USER JOURNEY: User reads Section 3, opens the note editor from the section, saves a note -> reloads the page -> Section 3 still shows a note indicator dot in the section list. IMPLEMENTATION: Add section_id TEXT column to the notes table (SQLite ALTER TABLE or db_init.py CREATE TABLE update). Add optional section_id field to NoteCreateRequest and NoteUpdateRequest Pydantic models. Include section_id in NoteResponse. DocumentReader section list: after fetching the document's notes (GET /notes?document_id=X), build a Set of section_ids that have notes and render a small dot badge on each matching section row.",
      "acceptanceCriteria": [
        "notes table has section_id TEXT column (nullable); existing rows unaffected",
        "POST /notes and PATCH /notes/{id} accept optional section_id in request body and persist it",
        "GET /notes?document_id=X returns section_id in each note object",
        "DocumentReader section list: sections with at least one saved note show a visible dot indicator",
        "Indicator persists after page reload (sourced from API, not component state)",
        "Smoke test: create note with section_id; reload page; indicator visible on correct section"
      ],
      "passes": true
    },
    {
      "id": "S107",
      "title": "Documentation pass: complete all partial-doc domains to reach QUALITY_SCORE A across the board",
      "phase": "V2 Phase 5 - Hardening",
      "priority": 107,
      "featureRef": "F-DOCS-COMPLETE",
      "description": "CONTEXT: Six domains in QUALITY_SCORE.md show documented=partial: Knowledge Graph, Notes, Search, Learning Engine, Study Mode, Monitoring. Per Invariant #8, docs/ is the system of record. Partial documentation means the system of record is incomplete and onboarding a new contributor requires reading source code instead of docs. This story writes the missing sections. SCOPE: docs/ARCHITECTURE.md: add Learning Engine section covering FSRS state machine (New->Learning->Review->Relearning), review flow, confusion detector pattern, and gap detector algorithm. Add Study Mode section covering UI states and session lifecycle. Add Knowledge Graph section covering disambiguation algorithm and Kuzu schema. docs/FRONTEND.md: document all 6 tab UX flows (Learning, Chat, Viz, Study, Notes, Monitoring) with state descriptions and key component names. QUALITY_SCORE.md: update all domains to documented=yes; promote all domains with implemented=yes and tested=yes to grade A.",
      "acceptanceCriteria": [
        "docs/ARCHITECTURE.md has Learning Engine section (>= 200 words covering FSRS states, review flow, confusion detector)",
        "docs/ARCHITECTURE.md has Study Mode section covering UI states and session lifecycle",
        "docs/ARCHITECTURE.md has Knowledge Graph section covering disambiguation algorithm and Kuzu schema",
        "docs/FRONTEND.md documents all 6 tab UX flows with component names",
        "QUALITY_SCORE.md: all domains show documented=yes",
        "QUALITY_SCORE.md: all domains with implemented=yes and tested=yes show grade A",
        "No code changes in this story -- docs only"
      ],
      "passes": true
    },
    {
      "id": "S108",
      "title": "Voice input for Teach-Back Mode via Web Speech API",
      "phase": "V2 Phase 6 - Voice and Proactive Discovery",
      "priority": 108,
      "featureRef": "F-TEACH-VOICE",
      "description": "CONTEXT: The Teach-Back Mode (S99) requires typing a long-form explanation -- high friction for a feature that should feel conversational. The Feynman Technique works best when explanation feels natural, not when it requires composing formal paragraphs. Voice input removes this barrier with zero backend cost and zero new dependencies. USER JOURNEY: User opens Teach-Back panel -> sees a microphone button next to the text area -> clicks it -> button pulses red while recording -> user speaks their explanation aloud -> interim transcript appears in real time in the textarea -> user stops recording -> final transcript in textarea -> user submits for evaluation. IMPLEMENTATION: Use the browser-native SpeechRecognition API (window.SpeechRecognition || window.webkitSpeechRecognition). No backend changes required -- the transcript feeds into the existing POST /study/teachback/{id}. Handle the case where speech recognition is not supported with a visible 'Voice input not supported in this browser' message. No new npm packages needed.",
      "acceptanceCriteria": [
        "TeachbackPanel has a microphone button rendered alongside the textarea",
        "Clicking the button starts SpeechRecognition with interimResults=true; button shows pulsing red indicator",
        "Interim transcript appears in textarea in real time as the user speaks",
        "Clicking button again (or end of speech) stops recognition; final transcript replaces textarea content",
        "If SpeechRecognition is unavailable, button is disabled with tooltip 'Voice input not supported in this browser'",
        "Submitting a voice-sourced transcript to POST /study/teachback/{id} works identically to typed input",
        "npx tsc --noEmit exits 0; no new backend changes"
      ],
      "passes": true
    },
    {
      "id": "S109",
      "title": "Proactive Suggested Explorations in Chat from Kuzu entity graph",
      "phase": "V2 Phase 6 - Voice and Proactive Discovery",
      "priority": 109,
      "featureRef": "F-CHAT-EXPLORATIONS",
      "description": "CONTEXT: The Chat tab starts empty, forcing users to think of questions (the blank canvas problem, reviewed in S95 which added context-aware suggestion pills). S95 pills are text-and-label suggestions. This story adds a deeper layer: when a document is selected, the Kuzu graph for that document is queried for its top entity pairs by RELATED_TO edge weight, and 3-5 proactive exploration suggestions are surface as clickable chips above the input. These are richer than S95 pills -- they are graph-derived and document-specific. USER JOURNEY: User opens Chat, selects a document -> below the S95 suggestion pills, a 'Explore connections' row appears with chips like 'Compare Eloi and Morlocks' or 'How does the Time Traveller relate to the Sphinx?' -> clicking a chip pre-fills and submits the question. IMPLEMENTATION: New endpoint GET /chat/explorations?document_id=X -- queries Kuzu for top-degree entity pairs by shared edge count, formats them as suggestion strings. Frontend: render as a second row of chips in Chat, shown only when a document is selected and the chat history is empty.",
      "acceptanceCriteria": [
        "GET /chat/explorations?document_id=X returns a list of up to 5 ExplorationSuggestion objects with text and entity_names fields",
        "Suggestions are derived from Kuzu RELATED_TO edges for the given document -- not hardcoded",
        "Frontend: 'Explore connections' chip row visible in Chat when document is selected and chat is empty",
        "Clicking a chip pre-fills the chat input and submits the message",
        "Chip row hidden when chat history is non-empty (not shown mid-conversation)",
        "If document has no graph entities, chip row is omitted gracefully",
        "npx tsc --noEmit exits 0; uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S110",
      "title": "Reading progress tracking: IntersectionObserver per section with progress bar in library",
      "phase": "V2 Phase 6 - Voice and Proactive Discovery",
      "priority": 110,
      "featureRef": "F-READING-PROGRESS",
      "description": "CONTEXT: The app has no signal for which document sections the user has actually read. FSRS knows what cards have been reviewed, but not what has been read. The study planner (S101) cannot distinguish 'not reviewed yet' from 'not read yet'. Reading progress data also feeds into S116 (gap heatmap) and S113 (learning goals). USER JOURNEY: User opens a document and reads through it -> sections they scroll past and spend time on are silently marked as read -> library card shows a reading progress bar (e.g. '7 / 12 sections read') -> on next visit, progress bar reflects previous reading. IMPLEMENTATION: Backend: new table reading_progress (id, document_id, section_id, first_seen_at, last_seen_at, view_count). POST /reading/progress upserts by (document_id, section_id). GET /documents/{id} response includes reading_progress_pct (sections_read / total_sections). Frontend: DocumentReader uses IntersectionObserver -- when a section is in viewport for >= 3 continuous seconds, fire POST /reading/progress. Library card shows a thin progress bar under the document title.",
      "acceptanceCriteria": [
        "reading_progress table created in db_init.py with columns id, document_id, section_id, first_seen_at, last_seen_at, view_count",
        "POST /reading/progress: upserts by (document_id, section_id); increments view_count on repeat",
        "GET /documents/{id} response includes reading_progress_pct float (0.0 to 1.0)",
        "DocumentReader: IntersectionObserver fires POST /reading/progress when section in viewport >= 3 seconds",
        "Library card renders a thin progress bar reflecting reading_progress_pct",
        "Progress persists across reloads (stored in DB, not component state)",
        "Test: POST reading/progress twice for same section -> view_count=2; reading_progress_pct reflects correct ratio"
      ],
      "passes": true
    },
    {
      "id": "S111",
      "title": "In-document annotation and text highlighting with persistent storage",
      "phase": "V2 Phase 6 - Voice and Proactive Discovery",
      "priority": 111,
      "featureRef": "F-ANNOTATIONS",
      "description": "CONTEXT: A core reading workflow is missing: users cannot highlight text or add inline annotations. They must switch to the Notes tab and remember the section manually -- high friction that breaks reading flow. Highlights are the primary engagement signal in every serious reading app (Kindle, Readwise, iBooks). USER JOURNEY: User selects text in a document section -> a small popover appears with color swatches (yellow, green, blue, red) and an 'Add note' option -> user picks yellow and optionally types a note -> text appears highlighted in the reader -> on reload, highlight persists in the same position. IMPLEMENTATION: Backend: new table annotations (id, document_id, chunk_id, selected_text, start_offset, end_offset, color, note_text, created_at). CRUD endpoints POST/GET/DELETE /annotations. Frontend: on mouseup in DocumentReader, detect selection; show popover with color swatches; on save, POST annotation; on document load, apply highlight spans via DOM range; FloatingToolbar gains a 'Highlight' action. New 'Highlights' tab in DocumentReader alongside 'Key Points' and 'Notes'.",
      "acceptanceCriteria": [
        "annotations table in db_init.py with id, document_id, chunk_id, selected_text, start_offset, end_offset, color, note_text, created_at",
        "POST /annotations creates an annotation; GET /annotations?document_id=X returns all; DELETE /annotations/{id} removes one",
        "DocumentReader: mouseup on text shows a popover with at least 4 color options and optional note input",
        "Saved highlight persists across page reloads -- highlight span visible at correct text position on reload",
        "FloatingToolbar includes a Highlight action",
        "DocumentReader has a Highlights tab showing all annotations for the current document",
        "npx tsc --noEmit exits 0; uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S112",
      "title": "Graph-driven flashcard generation from Kuzu entity relationship pairs",
      "phase": "V2 Phase 6 - Voice and Proactive Discovery",
      "priority": 112,
      "featureRef": "F-FLASHCARD-GRAPH",
      "description": "CONTEXT: FlashcardService.generate() builds cards from raw text chunks. Entity relationship pairs in Kuzu (RELATED_TO, APPEARS_IN edges) are richer source material -- they produce cards testing conceptual relationships ('What is the relationship between X and Y?') rather than isolated fact recall. Relationship cards are harder to answer but have stronger long-term retention because they require connecting concepts rather than recognizing isolated facts. USER JOURNEY: User opens Learning tab GeneratePanel -> switches a toggle to 'From Entity Relationships' mode -> sees a preview of entity pairs that will be used (e.g., 'Time Traveller <-> Eloi (12 co-appearances)') -> generates flashcards -> reviews cards that ask about relationships, not just definitions. IMPLEMENTATION: FlashcardService.generate_from_graph(document_id, k) fetches top-K entity pairs by shared chunk count from Kuzu; for each pair retrieves shared chunks via GET /search; prompts LLM to generate a relationship-focused card. New endpoint POST /flashcards/generate-from-graph. Frontend: GeneratePanel adds a mode toggle; 'From Entities' mode calls the new endpoint.",
      "acceptanceCriteria": [
        "FlashcardService.generate_from_graph(document_id, k) fetches top-K entity pairs by edge weight from Kuzu",
        "For each pair, shared chunks are retrieved and passed to LLM with a relationship-framing prompt",
        "POST /flashcards/generate-from-graph accepts {document_id: str, k: int} and returns generated flashcards",
        "Generated cards have question framed as a relationship ('How does X relate to Y?' or 'What connects X and Y?') -- not a definition question",
        "Frontend GeneratePanel: mode toggle with 'From Text' (default) and 'From Entities' options",
        "Entity mode shows a preview list of the top entity pairs before generating",
        "FSRS scheduling works identically for graph-generated cards (same review flow)",
        "uv run ruff check . exits 0; npx tsc --noEmit exits 0"
      ],
      "passes": true
    },
    {
      "id": "S113",
      "title": "Learning Goals with target dates and FSRS-based readiness projection",
      "phase": "V2 Phase 7 - Learning Engine Grade A",
      "priority": 113,
      "featureRef": "F-LEARNING-GOALS",
      "description": "CONTEXT: The app has no goal-setting mechanism. Without a target date, learners have no accountability loop and FSRS reviews feel optional rather than urgent. FSRS stores stability and retrievability per card, which means it can project 'how many cards will be retained by date X' -- this is the foundation of a readiness gauge. USER JOURNEY: User creates a goal: 'Master The Time Machine by March 25' -> app shows a readiness gauge: 'On track: 78% cards will be retained by that date, 4 at-risk cards need extra review' -> user clicks an at-risk card to review it now. IMPLEMENTATION: New table learning_goals (id, document_id, title, target_date, created_at). GoalService.compute_readiness(goal_id): uses FSRS projected stability to estimate retrievability at target_date for each card; counts at-risk cards (projected_retrievability < 0.7). Endpoints POST/GET/DELETE /goals; GET /goals/{id}/readiness. Frontend: new Goals panel in Study tab with goal creation form, readiness gauge (green/yellow/red), and at-risk card list.",
      "acceptanceCriteria": [
        "learning_goals table in db_init.py with id, document_id, title, target_date, created_at",
        "POST /goals creates a goal; GET /goals returns list; DELETE /goals/{id} removes",
        "GET /goals/{id}/readiness returns {on_track: bool, projected_retention_pct: float, at_risk_card_count: int, at_risk_cards: list}",
        "GoalService.compute_readiness() uses FSRS stability to project retrievability at target_date (not a random estimate)",
        "Frontend Study tab has a Goals panel with goal creation form (title, document selector, date picker)",
        "Readiness gauge shows green (>= 80%), yellow (60-79%), red (< 60%) based on projected_retention_pct",
        "At-risk cards listed with direct link to review them",
        "uv run ruff check . exits 0; npx tsc --noEmit exits 0"
      ],
      "passes": true
    },
    {
      "id": "S114",
      "title": "Adaptive difficulty signals: surface struggling cards with source section links",
      "phase": "V2 Phase 7 - Learning Engine Grade A",
      "priority": 114,
      "featureRef": "F-ADAPTIVE-DIFFICULTY",
      "description": "CONTEXT: FSRS tracks individual card difficulty and review history but the app never intervenes when a learner is clearly struggling. A learner who has rated the same card 'Again' 3+ times in 14 days is not just forgetting -- they likely do not understand the underlying concept and need to re-read the source material, not just review the card more. The confusion detector (S100) detects repeated questions across sessions; this story detects repeated failure on the same card. USER JOURNEY: User opens Study tab -> sees a 'Struggling' sub-panel distinct from 'Weak Areas' -> each item shows the card question, how many times rated Again recently, and a 'Re-read source' button that opens DocumentReader at the source section. IMPLEMENTATION: FSRSService.get_struggling_cards(document_id, again_threshold=3, days=14): queries review history for cards with again_count >= threshold in the window. New endpoint GET /study/struggling?document_id=X. Frontend: Study tab Struggling panel, separate from WeakAreasPanel.",
      "acceptanceCriteria": [
        "FSRSService.get_struggling_cards(document_id, again_threshold, days) queries review history correctly",
        "GET /study/struggling?document_id=X returns cards with again_count >= 3 in last 14 days",
        "Response includes card question, again_count, and source_section_id for each card",
        "Frontend Study tab has a Struggling panel separate from Weak Areas",
        "Each struggling card shows: question preview, again count badge, 'Re-read source' button",
        "'Re-read source' navigates to DocumentReader at the specific section",
        "Test: simulate 3 Again ratings on a card within 14 days -> GET /study/struggling returns that card",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S115",
      "title": "Study session analytics: per-session history with accuracy and cards reviewed",
      "phase": "V2 Phase 7 - Learning Engine Grade A",
      "priority": 115,
      "featureRef": "F-SESSION-ANALYTICS",
      "description": "CONTEXT: ProgressDashboard shows aggregate charts (retention curve, mastery heatmap, streak calendar) but no per-session breakdown. A learner cannot see 'in yesterday's 10-minute session I reviewed 15 cards at 73% accuracy focused on Chapter 3.' Without session-level data, the progress view feels abstract and does not build the sense of cumulative effort. USER JOURNEY: User completes a review session -> clicks 'End Session' -> session is logged -> opens Study History tab -> sees a table of past sessions with date, duration, card count, accuracy, and document covered -> clicks a row to expand card-level detail. IMPLEMENTATION: New table study_sessions (id, started_at, ended_at, document_id, cards_reviewed, correct_count, accuracy_pct). POST /study/start-session and POST /study/end-session endpoints. GET /study/sessions returns paginated history. Frontend: new History sub-tab in Study tab.",
      "acceptanceCriteria": [
        "study_sessions table in db_init.py with id, started_at, ended_at, document_id, cards_reviewed, correct_count, accuracy_pct",
        "POST /study/start-session returns a session_id",
        "POST /study/end-session/{session_id} finalizes the session with computed stats",
        "GET /study/sessions returns paginated session list sorted by started_at desc",
        "Frontend Study tab has a History sub-tab showing past sessions in a table",
        "Table columns: date, duration, cards reviewed, accuracy %, document name",
        "Clicking a row expands to show card-level breakdown (question, rating given)",
        "uv run ruff check . exits 0; npx tsc --noEmit exits 0"
      ],
      "passes": true
    },
    {
      "id": "S116",
      "title": "FSRS fragility heatmap on DocumentReader section list",
      "phase": "V2 Phase 7 - Learning Engine Grade A",
      "priority": 116,
      "featureRef": "F-HEATMAP",
      "description": "CONTEXT: The DocumentReader section list shows headings and reading progress dots (S110). It does not show which sections have the weakest FSRS retention. Overlaying fragility scores as a color-coded left border would make the reading experience study-aware: the user can see at a glance which chapter they need to re-engage with before their next review. USER JOURNEY: User opens a document they have studied -> section list shows color-coded borders (green = mastered, yellow = needs review, red = critical gap) -> hovering a colored section shows a tooltip: '3 cards due, avg retention 42%' -> user clicks the red section to re-read it before reviewing. IMPLEMENTATION: GET /study/section-heatmap?document_id=X: aggregates FSRS retrievability scores for all cards sourced from each section, returns a map {section_id: fragility_score} (0.0 mastered, 1.0 critical). Frontend: DocumentReader section list applies a colored left-border CSS class based on the heatmap. Heatmap invalidated after a study session ends.",
      "acceptanceCriteria": [
        "GET /study/section-heatmap?document_id=X returns {section_id: fragility_score} map for all sections with flashcards",
        "fragility_score is derived from average FSRS retrievability of cards sourced from that section (not random)",
        "Sections with no cards return fragility_score=null (no border applied)",
        "DocumentReader section list renders a colored left-border: green (<= 0.3), yellow (0.31-0.6), red (> 0.6)",
        "Hovering a colored section shows tooltip with due card count and avg retention %",
        "Heatmap query invalidated (TanStack Query) when a study session ends",
        "Test: card with retrievability 0.2 for section A -> section A fragility_score <= 0.3"
      ],
      "passes": true
    },
    {
      "id": "S117",
      "title": "Concept prerequisite mapping: PREREQUISITE_OF edges in Kuzu with learning-path endpoint",
      "phase": "V2 Phase 7 - Learning Engine Grade A",
      "priority": 117,
      "featureRef": "F-PREREQUISITES",
      "description": "CONTEXT: The Kuzu knowledge graph has RELATED_TO peer edges but no directionality for learning order. Every concept appears flat. In reality, understanding 'Natural Selection' requires 'Variation' and 'Heredity' as prerequisites. Identifying prerequisite language during ingestion and storing directed PREREQUISITE_OF edges enables a learning-path view in the Viz tab and powers goal-based sequencing in S113. IMPLEMENTATION: Ingestion pipeline NER step: after entity extraction, scan chunk text for prerequisite linguistic markers (requires understanding of, builds on, defined as a type of, is a subclass of, depends on, first introduced as). For each match, create a directed PREREQUISITE_OF edge in Kuzu from the dependent concept to the prerequisite. New endpoint GET /graph/learning-path?start_entity=X&document_id=Y returns a topologically sorted list of concepts. Frontend Viz tab: new 'Learning Path' toggle renders PREREQUISITE_OF edges as directed arrows in orange.",
      "acceptanceCriteria": [
        "Ingestion pipeline detects prerequisite markers and creates PREREQUISITE_OF directed edges in Kuzu",
        "Kuzu schema has PREREQUISITE_OF relationship table with from_entity, to_entity, document_id, confidence",
        "GET /graph/learning-path?start_entity=X&document_id=Y returns a topologically sorted concept list",
        "Viz tab has a 'Learning Path' toggle that renders PREREQUISITE_OF edges as directed orange arrows",
        "Clicking a node in learning-path mode shows its prerequisites as a breadcrumb trail",
        "Test: ingest a passage containing 'X requires understanding of Y' -> PREREQUISITE_OF edge from X to Y exists in Kuzu",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S118",
      "title": "Desktop push notifications for FSRS due cards via browser Notification API",
      "phase": "V2 Phase 7 - Learning Engine Grade A",
      "priority": 118,
      "featureRef": "F-DUE-NOTIFICATIONS",
      "description": "CONTEXT: The study planner (S101) surfaces a pre-session agenda but only when the user actively opens the app. Daily FSRS adherence is what makes spaced repetition effective -- but the current app is entirely passive. A browser push notification (no Tauri required, no mobile required) provides a low-effort habit trigger that closes the daily review loop on desktop. USER JOURNEY: User opens Luminary for the first time -> browser prompts for notification permission -> user grants -> on subsequent days, when the user focuses the browser window and has due cards, a desktop notification appears: 'Luminary: 12 cards due for review today' -> clicking it activates the Study tab. IMPLEMENTATION: Frontend only. On app mount, call Notification.requestPermission() once. New backend endpoint GET /study/due-count returns {due_today: int}. On window visibilitychange to 'visible', if due_today > 0 and last notification was > 4 hours ago (localStorage timestamp), fire new Notification(). Settings page includes 'Disable review reminders' toggle.",
      "acceptanceCriteria": [
        "GET /study/due-count returns {due_today: int} counting cards with next_review_date <= today",
        "App requests Notification.requestPermission() once on first mount (not on every load)",
        "On window focus (visibilitychange visible), if due_today > 0 and last-notified > 4h ago, fires browser notification",
        "Notification title: 'Luminary' body: 'X cards due for review today'",
        "Clicking notification activates Study tab",
        "Settings page has 'Review reminders' toggle that disables the notification check",
        "No notification fired if permission denied or toggle off"
      ],
      "passes": true
    },
    {
      "id": "S119",
      "title": "Audio ingestion via faster-whisper: MP3/M4A/WAV transcription with timestamp chunks",
      "phase": "V2 Phase 8 - Multimodal Ingestion",
      "priority": 119,
      "featureRef": "F-AUDIO-INGEST",
      "description": "CONTEXT: A significant share of learning happens through audio: lectures, podcasts, recorded seminars. The ingestion pipeline currently classifies and rejects audio files. Adding local Whisper transcription (via faster-whisper, a CTranslate2-optimized backend that is 4x faster than openai-whisper and runs fully offline) enables audio content to flow through the existing embed-NER-store pipeline unchanged. USER JOURNEY: User drags an MP3 lecture file into the library -> progress bar shows 'Transcribing...' then 'Indexing...' -> document appears in library with an audio icon and duration badge -> user can ask questions in Chat about the lecture content. IMPLEMENTATION: uv add faster-whisper. Add 'audio' to content-type classifier (_classify). New transcribe_node in the LangGraph ingestion graph inserted between parse_node and chunk_node for audio content type. _chunk_audio() creates chunks from Whisper segments, storing start_time and end_time as chunk metadata. All downstream nodes (embed, NER, store) unchanged.",
      "acceptanceCriteria": [
        "uv add faster-whisper; listed in pyproject.toml",
        "Content-type classifier returns 'audio' for .mp3, .m4a, .wav file extensions",
        "LangGraph ingestion graph has transcribe_node for audio content type",
        "Each chunk from _chunk_audio has start_time and end_time fields in metadata",
        "Audio document appears in library with audio icon and total duration",
        "Chat can answer factual questions about the audio content via normal Q&A flow",
        "Test: transcribe a short synthetic audio file (or use a fixture transcript); assert chunks have start_time metadata",
        "uv run ruff check . exits 0"
      ],
      "passes": true
    },
    {
      "id": "S120",
      "title": "Audio timestamp playback in DocumentReader: mini-player with jump-to-chunk",
      "phase": "V2 Phase 8 - Multimodal Ingestion",
      "priority": 120,
      "featureRef": "F-AUDIO-PLAYBACK",
      "description": "CONTEXT: S119 adds start_time/end_time metadata to audio chunks. Without a playback UI, users cannot verify AI answers against the source audio or follow along while reading the transcript. This story adds a persistent audio mini-player in DocumentReader for audio documents and a per-chunk 'Play from here' button. USER JOURNEY: User opens an audio document -> a sticky mini-player appears at the bottom of DocumentReader with play/pause and a scrubber -> each transcript chunk in the reader has a small play button showing the timestamp -> user reads an interesting chunk and clicks its timestamp -> mini-player seeks to that position and plays. IMPLEMENTATION: Frontend only -- no backend changes required. DocumentReader detects document type == 'audio'. Renders HTML5 audio element with document's raw file URL. Each chunk row shows a timestamp badge (MM:SS) formatted from start_time metadata; clicking it calls audioEl.currentTime = start_time.",
      "acceptanceCriteria": [
        "DocumentReader shows a sticky mini-player at the bottom for audio documents",
        "Mini-player has play/pause button, current time display, duration display, and a seek scrubber",
        "Each transcript chunk shows a timestamp badge (MM:SS) from start_time metadata",
        "Clicking a timestamp badge seeks the audio to that position and starts playback",
        "Mini-player is absent for non-audio documents",
        "npx tsc --noEmit exits 0; no backend changes"
      ],
      "passes": true
    },
    {
      "id": "S121",
      "title": "Video ingestion (MP4): ffmpeg audio extraction + Whisper transcription with video player",
      "phase": "V2 Phase 8 - Multimodal Ingestion",
      "priority": 121,
      "featureRef": "F-VIDEO-INGEST",
      "description": "CONTEXT: S119 handles audio files. Video (MP4) is the dominant format for lecture recordings and screen captures. The transcript is identical to audio after extraction -- the only difference is the extraction step (ffmpeg audio strip) and the player UI (HTML5 video instead of audio). USER JOURNEY: User drags an MP4 lecture recording -> progress bar shows 'Extracting audio... Transcribing... Indexing...' -> document appears with a video icon -> DocumentReader shows the video player with jump-to-timestamp on chunk click. IMPLEMENTATION: Detect .mp4 content type. In transcribe_node: run ffmpeg -i input.mp4 -vn -ar 16000 -ac 1 -f wav /tmp/{doc_id}_audio.wav as a subprocess, then transcribe with faster-whisper. Check for ffmpeg availability in lifespan; log a warning if not found. Frontend: DocumentReader renders HTML5 video element for video documents.",
      "acceptanceCriteria": [
        "Content-type classifier returns 'video' for .mp4 extension",
        "transcribe_node runs ffmpeg subprocess to extract audio before Whisper transcription",
        "lifespan startup checks for ffmpeg via shutil.which('ffmpeg'); logs a warning if not found",
        "Ingestion returns a user-friendly error if ffmpeg is unavailable and a video file is uploaded",
        "DocumentReader renders an HTML5 video element with jump-to-timestamp for video documents",
        "Chat can answer questions about video transcript content",
        "uv run ruff check . exits 0"
      ],
      "passes": false
    },
    {
      "id": "S122",
      "title": "YouTube URL ingestion via yt-dlp: audio download, Whisper transcription, timestamp deep-links",
      "phase": "V2 Phase 8 - Multimodal Ingestion",
      "priority": 122,
      "featureRef": "F-YOUTUBE-INGEST",
      "description": "CONTEXT: The most common source of lecture content is YouTube. Users routinely watch conference talks, MIT lectures, and tutorial videos. Accepting a YouTube URL in the upload dialog -- downloading audio with yt-dlp, transcribing with Whisper (S119), and then linking back to the source video at the correct timestamp -- closes the loop between Luminary's knowledge base and the original content. USER JOURNEY: User pastes a YouTube URL in the upload dialog -> Luminary downloads the audio, transcribes, indexes -> document appears with a YouTube icon and video title -> 'Play from here' on any chunk opens YouTube at that exact timestamp in a new tab. IMPLEMENTATION: uv add yt-dlp. Upload dialog detects YouTube URL pattern (youtube.com/watch or youtu.be/). Backend: download audio to temp file, route through S119 transcribe_node. Store video URL and video_id as document metadata. Frontend: 'Play from here' opens {url}&t={start_seconds} in a new tab.",
      "acceptanceCriteria": [
        "uv add yt-dlp; listed in pyproject.toml",
        "Upload dialog accepts YouTube URLs (youtube.com/watch?v=, youtu.be/)",
        "Backend downloads audio via yt-dlp subprocess, routes through transcribe_node",
        "Document stores source_url (YouTube URL) and video_title from yt-dlp metadata",
        "Library card shows YouTube icon and video title",
        "'Play from here' on a chunk opens {source_url}&t={start_seconds} in a new tab",
        "If yt-dlp is unavailable, upload returns a 503 with 'yt-dlp not installed' message",
        "uv run ruff check . exits 0"
      ],
      "passes": false
    },
    {
      "id": "S123",
      "title": "EPUB and Kindle ingestion: ebooklib chapter parsing and My Clippings.txt highlight import",
      "phase": "V2 Phase 8 - Multimodal Ingestion",
      "priority": 123,
      "featureRef": "F-EPUB-KINDLE",
      "description": "CONTEXT: Many learners use Kindle or read EPUB books outside Luminary and have years of highlights. EPUB ingestion lets users bring their reading library into Luminary without PDF conversion. Kindle My Clippings.txt import lets users bring their existing highlight corpus into Luminary's search and flashcard pipeline instantly. USER JOURNEY (EPUB): User uploads an .epub file -> chapters become sections in DocumentReader -> full ingestion pipeline (embed, NER, FSRS cards) works identically. USER JOURNEY (Kindle): User drags My Clippings.txt -> each book's highlights become a document with the book title, highlights as chunks, and creation dates -> flashcards generatable from highlights. IMPLEMENTATION: uv add ebooklib. _classify returns 'epub' for .epub. _parse_epub() extracts chapters as sections with headings. My Clippings.txt parser: split on '========== ' delimiter, group by book title, create one document per book.",
      "acceptanceCriteria": [
        "uv add ebooklib; listed in pyproject.toml",
        "Content-type classifier returns 'epub' for .epub extension",
        "_parse_epub() extracts chapters as sections preserving chapter headings",
        "EPUB document flows through full ingestion pipeline (embed, NER, store)",
        "Content-type classifier returns 'kindle_clippings' for My Clippings.txt (detected by filename pattern)",
        "_parse_kindle_clippings() groups highlights by book title, returns one document per book",
        "Kindle-imported documents marked with source=kindle badge in library",
        "Flashcard generation works on both EPUB and Kindle-imported documents",
        "uv run ruff check . exits 0"
      ],
      "passes": false
    },
    {
      "id": "S124",
      "title": "Obsidian vault export: notes, flashcards, and entity wikilinks as markdown files",
      "phase": "V2 Phase 9 - Portability and Ecosystem",
      "priority": 124,
      "featureRef": "F-EXPORT-OBSIDIAN",
      "description": "CONTEXT: Power users use Obsidian as their primary note-taking and knowledge management system. Exporting Luminary notes and flashcards as Obsidian-compatible markdown files enables the two tools to complement each other: Luminary for structured learning (FSRS, teach-back, gap detection), Obsidian for long-term knowledge management. USER JOURNEY: User opens Settings -> Export section -> clicks 'Export to Obsidian Vault' for a document -> downloads a .zip file -> unzips into their Obsidian vault folder -> notes appear as .md files with YAML frontmatter, flashcards appear in a flashcards.md compatible with the Obsidian Spaced Repetition plugin, entity wikilinks in graph.md enable Obsidian graph view. IMPLEMENTATION: New endpoint GET /export/obsidian?document_id=X. Generates a zip in memory: one .md file per note (YAML frontmatter: tags, document_title, created_at), flashcards.md with #flashcard tag format, graph.md with [[EntityName]] wikilinks.",
      "acceptanceCriteria": [
        "GET /export/obsidian?document_id=X returns a .zip file with correct Content-Type",
        "Zip contains one .md file per note with YAML frontmatter (tags, document_title, created_at)",
        "Zip contains flashcards.md with each card in '{{question}}\\n?\\n{{answer}}\\n#flashcard' format",
        "Zip contains graph.md with [[EntityName]] wikilinks for each entity in the document's Kuzu graph",
        "Frontend Settings page has 'Export to Obsidian' button per document in library",
        "Button triggers zip download",
        "uv run ruff check . exits 0; npx tsc --noEmit exits 0"
      ],
      "passes": false
    },
    {
      "id": "S125",
      "title": "Anki deck export via genanki: flashcards as .apkg with FSRS state preservation",
      "phase": "V2 Phase 9 - Portability and Ecosystem",
      "priority": 125,
      "featureRef": "F-EXPORT-ANKI",
      "description": "CONTEXT: Anki is the global standard for spaced repetition with the largest mobile app install base. Exporting Luminary flashcards as an Anki .apkg deck solves the mobile FSRS gap (no mobile app needed) and lets users continue reviews on any device. FSRS difficulty and stability are stored as custom fields in the Anki note type so a future import back into Luminary can restore scheduling state. USER JOURNEY: User opens Learning tab -> clicks 'Export to Anki' in the flashcard panel -> downloads a .apkg file -> imports into Anki desktop or AnkiDroid -> all cards present with front (question) / back (answer + source citation). IMPLEMENTATION: uv add genanki. New endpoint GET /export/anki?document_id=X generates a .apkg. Custom Anki note model with fields: Front, Back, Source, FSRS_Difficulty, FSRS_Stability. Cards grouped into a deck named after the document title.",
      "acceptanceCriteria": [
        "uv add genanki; listed in pyproject.toml",
        "GET /export/anki?document_id=X returns a .apkg file",
        "Deck name matches the document title",
        "Each card has Front (question), Back (answer + source chunk citation), FSRS_Difficulty, FSRS_Stability fields",
        "Anki package is valid: importable into Anki 2.1+ without errors",
        "Frontend Learning tab flashcard panel has 'Export to Anki' button",
        "Button triggers .apkg download",
        "uv run ruff check . exits 0"
      ],
      "passes": false
    },
    {
      "id": "S126",
      "title": "Local network relay for mobile FSRS review: LAN binding, QR code, and mobile study view",
      "phase": "V2 Phase 9 - Portability and Ecosystem",
      "priority": 126,
      "featureRef": "F-LAN-RELAY",
      "description": "CONTEXT: FSRS tethered to a desktop is the single biggest gap in the learning science model. Daily review adherence is what makes spaced repetition effective -- but the current app is only accessible from the computer where it runs. This story enables mobile browser access over a local WiFi network without any cloud infrastructure. USER JOURNEY: User opens Settings -> sees a QR code labeled 'Review on mobile' -> scans with phone on same WiFi -> phone browser opens a minimal mobile-optimized study page -> user reviews due cards with flip animation and FSRS rating buttons -> ratings sync instantly to the desktop database. IMPLEMENTATION: Backend: add --host 0.0.0.0 mode triggered by a LAN_MODE=true env var or Settings toggle. A new minimal frontend route /mobile/study renders a stripped-down FSRS review UI (no sidebar, no library, dark-mode safe). Settings page generates a QR code using a pure-JS library for the LAN URL.",
      "acceptanceCriteria": [
        "Backend supports binding to 0.0.0.0 when LAN_MODE env var is set or Settings toggle is on",
        "GET /study/mobile-session returns due cards formatted for the mobile view",
        "Frontend /mobile/study route: card flip animation, FSRS rating buttons (Again/Hard/Good/Easy), no sidebar",
        "FSRS ratings from mobile view persist in the same database as desktop sessions",
        "Settings page shows a QR code with the LAN URL (http://192.168.x.x:8000/mobile/study)",
        "CORS updated to allow LAN IP origin",
        "Mobile view loads in < 3s on a mid-range phone over WiFi (no heavy bundle deps)",
        "npx tsc --noEmit exits 0; uv run ruff check . exits 0"
      ],
      "passes": false
    },
    {
      "id": "S127",
      "title": "Tauri v2 desktop packaging: single installer bundling frontend and Python sidecar",
      "phase": "V2 Phase 9 - Portability and Ecosystem",
      "priority": 127,
      "featureRef": "F-TAURI-PACKAGING",
      "description": "CONTEXT: Currently the app requires the user to run uvicorn and npm run dev manually from a terminal. This is a significant barrier to adoption for non-developer users. Tauri v2 bundles the Vite frontend into a native desktop shell; the Python backend is packaged as a standalone binary (PyInstaller sidecar) spawned by Tauri on launch. The result is a single distributable installer for macOS (.dmg), Windows (.exe), and Linux (.deb). USER JOURNEY: User downloads Luminary.dmg, drags to Applications, opens it -- no Python or Node required. IMPLEMENTATION: uv add pyinstaller as a dev dependency. PyInstaller spec file that packages the uvicorn app as a standalone binary. Tauri v2 config: sidecar entry pointing to the binary, spawned on app launch and killed on app exit. GitHub Actions workflow builds and attaches installers on version tag push.",
      "acceptanceCriteria": [
        "PyInstaller spec produces a standalone binary that runs the uvicorn app with no Python installation required",
        "Tauri v2 tauri.conf.json configured with sidecar pointing to the Python binary",
        "App launches from the installer: backend starts, frontend opens -- no terminal required",
        "Sidecar process is killed cleanly when the Tauri window closes",
        "GitHub Actions workflow: on tag push, builds .dmg (macOS), .exe (Windows), .deb (Linux) and attaches as release artifacts",
        "Install on a clean macOS machine with no Python/Node: Luminary opens and basic ingestion works"
      ],
      "passes": false
    },
    {
      "id": "S128",
      "title": "Readwise CSV and Kindle Clippings import with auto-ingestion pipeline",
      "phase": "V2 Phase 9 - Portability and Ecosystem",
      "priority": 128,
      "featureRef": "F-READWISE-IMPORT",
      "description": "CONTEXT: Many learners have years of book highlights stored in Readwise or Kindle. Importing them creates immediate value on first use -- the library is populated with their existing knowledge corpus before they upload a single new document. S123 adds basic Kindle Clippings parsing; this story adds the Readwise CSV format and ensures that imported highlights flow through the full ingestion pipeline (embedding, NER, FSRS card generation) so they are immediately searchable and learnable. USER JOURNEY: User opens Library -> Import -> 'Import from Readwise' -> selects a Readwise CSV export -> each book becomes a document with highlights as chunks, tags from Readwise preserved, author and book title populated -> user can immediately generate flashcards from their highlight library. IMPLEMENTATION: Readwise CSV columns: Title, Author, URL, Highlight, Note, Location, Date, Tags. Parse into document + chunk records. Run embed + NER pipeline on highlights. If document_title matches an existing library document, link highlights to it.",
      "acceptanceCriteria": [
        "Import dialog in Library accepts Readwise CSV format",
        "Parser extracts Title, Author, Highlight, Note, Tags from Readwise CSV columns",
        "Each unique book title becomes one document; each highlight becomes one chunk",
        "Imported documents flow through the full embed + NER ingestion pipeline",
        "Tags from Readwise CSV are mapped to the Luminary document tags field",
        "If document title matches an existing library document, imported highlights are linked to it",
        "Flashcard generation works on Readwise-imported documents",
        "Import progress shown in the library upload progress indicator",
        "uv run ruff check . exits 0"
      ],
      "passes": false
    },
    {
      "id": "S129",
      "priority": 129,
      "title": "Tech Debt TD-014: Fix RAGAS eval pipeline -- HR@5 and MRR always zero",
      "type": "tech-debt",
      "description": "USER JOURNEY: User clicks Run Eval (book dataset) in Monitoring tab -> eval completes -> RAGAS Eval Results table shows non-zero HR@5 and MRR scores reflecting actual retrieval quality.\n\nROOT CAUSES:\n1. CRITICAL: run_eval.py::search_chunks() reads match.get(\"text\", \"\") but SearchResult model field is named text_excerpt -- all chunks are empty strings, so context_hint can never match, giving HR@5=MRR=0.0 always.\n2. SECONDARY: text_excerpt truncates chunk to 200 chars -- relevant passage beyond char 200 never matches even with the correct field name.\n3. BUG: passed field in scores_history.jsonl is always True regardless of threshold violations; threshold check only runs when --assert-thresholds CLI flag is set, making the field a misleading quality indicator.\n\nCROSS-STORY IMPACT: S85 (RAGAS eval panel UI) -- Monitoring tab displays scores from scores_history.jsonl; fixing the scorer makes those scores meaningful. S88 (golden datasets) -- Alice and Odyssey entries were added but never validated against real retrieval because the scorer was broken from day one.\n\nFIXES:\n1. Add text field (full chunk text, capped at 2000 chars) to SearchResult in search.py alongside text_excerpt (kept at 200 chars for UI backward compat).\n2. Fix search_chunks() in run_eval.py to use match.get(\"text\", \"\") -- which now returns full chunk text.\n3. Fix passed logic: always compute threshold compliance (hr5 >= 0.60 AND mrr >= 0.45); --assert-thresholds only controls exit code.\n4. Add pytest unit tests for HR@5, MRR computation and passed threshold logic (backend/tests/test_eval_metrics.py).\n5. Add smoke test scripts/smoke/S129.sh.\n\nQUALITY GATE: After fix, running the eval against a backend with the three books ingested must yield HR@5 > 0 and MRR > 0.",
      "acceptanceCriteria": [
        "search_chunks() in run_eval.py reads the text field (not a missing field), so chunks contain actual chunk text rather than empty strings",
        "SearchResult in search.py includes a text field with full chunk text (capped at 2000 chars, not 200) so context_hint substring matching works for long passages",
        "passed field in scores_history.jsonl correctly reflects threshold compliance (hr5 >= 0.60 AND mrr >= 0.45) regardless of whether --assert-thresholds flag is passed",
        "pytest unit tests verify compute_hit_rate_5() returns correct fraction when context_hint appears in retrieved chunks",
        "pytest unit tests verify compute_mrr() returns correct reciprocal rank scores",
        "pytest unit test verifies passed=True when metrics meet thresholds and passed=False when below thresholds",
        "smoke test scripts/smoke/S129.sh calls GET /evals/results and asserts HTTP 200"
      ],
      "passes": true
    },
    {
      "id": "S130",
      "priority": 130,
      "title": "Tech Debt TD-015: RAGAS eval -- per-book breakdown and MRR improvement",
      "type": "tech-debt",
      "description": "USER JOURNEY: User opens Monitoring tab -> RAGAS Eval Results table shows one row per book (time_machine, alice, odyssey) not one combined row for all books -> each book row has HR@5 >= 0.60 and MRR >= 0.45 so the user can see which specific corpus is underperforming retrieval.\n\nROOT CAUSES TO FIX:\n1. MANIFEST: evals/golden/manifest.json is never written because ensure_ingested() in run_eval.py re-ingests the source file on every run. The ingestion creates a new duplicate document with a new doc_id each time, and the search is then filtered to that new doc -- whose chunks may not be fully indexed yet. Fix: look up an already-ingested document by filename via GET /documents instead of blindly re-ingesting.\n2. DATASET SPLIT: The three books (time_machine, alice, odyssey) are all in evals/golden/book.jsonl and produce a single \"book\" row in the Monitoring tab. Split into three datasets: book_time_machine.jsonl, book_alice.jsonl, book_odyssey.jsonl. Update the eval runner and Monitoring tab to show one row per dataset. The backend VALID_DATASETS and evals router must accept these new names.\n3. MRR GAP: Observed MRR 0.06 vs threshold 0.45. Diagnose why the relevant passage is not in top-5. Likely cause: txt chunking splits at double-newline boundaries, so a passage spanning a paragraph break is split across two chunks and neither chunk alone satisfies the context_hint substring. Fix: in retriever, increase default k from 5 to 10 when called by the eval runner (or expose a k param to the /search endpoint that the eval passes). Also verify the /search default limit (20) actually returns chunks from all three books when queried without a doc_id filter.\n4. CONTEXT HINT QUALITY: 15 of 70 hints in book.jsonl do not match the source text even with whitespace normalization (from S130 diagnosis run). Audit and fix these 15 hints so the upper bound on HR@5 is 70/70 not 55/70.\n\nCROSS-STORY IMPACT: S85 (RAGAS eval panel) -- Monitoring tab query GET /evals/results must render the new per-book dataset names. S129 (HR@5/MRR fix) -- whitespace normalization fix in run_eval.py is a prerequisite; keep it. S88 (golden datasets) -- alice and odyssey entries were added to book.jsonl; this story moves them to their own files.",
      "acceptanceCriteria": [
        "evals/golden/ contains three separate dataset files: book_time_machine.jsonl (30 entries), book_alice.jsonl (20 entries), book_odyssey.jsonl (20 entries); the combined book.jsonl is removed or kept only as a backward-compat symlink",
        "VALID_DATASETS in run_eval.py and evals router accepts book_time_machine, book_alice, book_odyssey as valid dataset names",
        "GET /evals/results returns one row per dataset; Monitoring tab shows three separate book rows instead of one combined book row",
        "ensure_ingested() in run_eval.py checks GET /documents for an existing document with matching filename before attempting re-ingest; evals/golden/manifest.json is written and reused across runs",
        "Running the eval against a backend with all 3 books ingested yields HR@5 >= 0.60 and MRR >= 0.45 for at least two of the three book datasets",
        "All 15 context_hints that failed whitespace-normalized substring match against their source file are audited and corrected so that normalized match rate is 70/70",
        "pytest unit test verifies that ensure_ingested() skips re-ingest when GET /documents already returns a matching document",
        "smoke test scripts/smoke/S130.sh calls GET /evals/results and asserts HTTP 200 with at least one result row"
      ],
      "passes": false
    }
  ],
  "regressionBaseline": "423 tests pass on branch ralph/luminary-v2 before re-implementation begins (run: uv run pytest --ignore=tests/test_v2_pipeline.py --ignore=tests/test_v2_ingestion_perf.py --ignore=tests/test_ingestion_perf.py --ignore=tests/test_performance.py). After each story, the test count must be >= 423. Any drop is a regression -- stop and fix before marking passes:true."
}