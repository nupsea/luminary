services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7820:7820"
    volumes:
      - luminary-data:/data
    environment:
      - LUMINARY_MODE=public
      - DATA_DIR=/data
      # Point the backend at the ollama service. The backend reads OLLAMA_URL
      # (NOT OLLAMA_HOST); its default 127.0.0.1 would resolve to this container.
      - OLLAMA_URL=http://ollama:11434
    depends_on:
      ollama:
        condition: service_started
    restart: unless-stopped
    profiles:
      - ""
      - ai

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama-models:/root/.ollama
    environment:
      # Keep the text (llama3.2) and vision (llava) models resident together so
      # enrichment does not thrash reloading them, and let one server batch
      # concurrent requests. Defaults suit a ~16GB host; lower both to 1 on an
      # 8GB host, raise on a bigger box. See install.sh profile prompt.
      - OLLAMA_MAX_LOADED_MODELS=${OLLAMA_MAX_LOADED_MODELS:-2}
      - OLLAMA_NUM_PARALLEL=${OLLAMA_NUM_PARALLEL:-2}
    restart: unless-stopped
    profiles:
      - ai

  # One-shot: pull the default chat model into the ollama volume on first run,
  # then exit. Without this the sidecar starts empty and chat/generation fail.
  # OLLAMA_HOST here is the ollama CLI's own variable (where the server lives).
  ollama-pull:
    image: ollama/ollama:latest
    depends_on:
      ollama:
        condition: service_started
    environment:
      - OLLAMA_HOST=http://ollama:11434
    entrypoint:
      - sh
      - -c
      - 'until ollama list >/dev/null 2>&1; do echo "waiting for ollama..."; sleep 2; done; ollama pull llama3.2'
    restart: "no"
    profiles:
      - ai

volumes:
  luminary-data:
  ollama-models:
